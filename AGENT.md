# AGENT.md — AI 舞萌制谱工作流（aimai-chartmaker）

> 本文件面向所有参与本项目开发的 coding agent。开始任何工作前请先完整阅读本文件。

## 项目概述

本项目旨在搭建一套**软工作流 + 制谱经验知识体系**——**不训练、不开发独立的谱面生成模型**。核心形态：**强 AI 模型（coding agent）**在拿到一首 mp3 后，依据本项目的分析工具与制谱经验，**直接上手创作**符合要求的谱面。

项目三大组成：

1. **分析工具**（`docs/audio-analysis.md`）：把 mp3 变成结构化特征（节拍/分轨 onset 踩音目标/结构分段/强度曲线），让模型"看懂"乐曲；
2. **制谱经验**（`.agent/knowledge/`）：从官方谱与教程沉淀的制谱意识与规范，模型创作时的依据（**已建库 94 条**：001–032 用户逐条讲授 + 官方谱实证与实测规律；033–046 来自 B 站 wasllll《配置术语》第二期、047–063 来自第三期星星篇——**星星配置的讲授来源即该视频**（用户 2026-09-19 指定），置信度按"社区教程"标、用户可覆盖；**064–067 用户手序讲授**（舒适区与出张 / 星星头与条可分属两手 / 手法非写谱导向 / 段首出手与位置连续），分配器以此为准；**068–094 agent 观察条目**（采音方式/采音跟着音乐走/密度与 note 种类两轴/定数与 BPM 规律/配置词汇量与段落分区/配置两层/手序模型/双押手分配/两轴正交/出张与侧边双押实测/术语对照/侧边双押引导八型/菱形写法骨架/绕手手级形态/三角两种形态/似踩非踩/出张官谱场景；088–094 种类配比/舍哪一半/星星族段落用法/note 总数不等于难度/一笔画接力的头要落在轨道之前，均标"未经用户确认"）；
3. **手序模型**（`docs/hand-sequencing.md` v0.2，`tools/chart_analysis/hands.py`）：左右手分配器与手序合理性/无理判定，**已按用户讲授（知识 064–067）对齐**——**任何配置都按左右手定义**（用户 2026-09-19），配置识别（`configs_hand.py`）、硬度、写谱规划都建立在它之上；
4. **工作流指引**（`docs/workflow-sop.md` v0.1 + 谱面检查器 `tools/chart_check/`）：拿到 mp3 后的完整操作 SOP（输入→分析→读知识→段落规划→手序规划→写谱→检查→迭代→交付）；已用 ロストワンの号哭 端到端试写一次（`samples/trial-01`、`docs/research/e2e-trial-01.md`），暴露的认识缺口在 note 063 线补。

- **输入**：一个 MP3 格式的 track 音频文件（BPM/offset/变速点等元数据可由用户直接提供）
- **输出**：一份 `maidata.txt`，内容为 **simai 语法** 的谱面数据（由模型在工作流内创作，非模型产物）

最终交付的谱面必须满足以下全部要求（按优先级排序，全部为硬性验收标准）：

1. **语法正确**：完全符合 simai 语法规范，通过校验器后 **零报错、零警告提示**；**交付文件按官方谱格式出**——一行一小节、不含任何 `||` 注释（Visual Maimai 会吞掉注释行之后一行行首的 `{x}`，2026-09-20 实测，检查器硬规则 `SYN-VM-COMMENT`），并过社区无理口径（`chart_check` 第七层 = MaiMuriDX/MiaCode 静态三类：外键/撞尾/叠键）零命中；
2. **人类可玩**：谱面可以被真人实际游玩；
3. **手序合理**：符合人类手序（左右手交替、可及范围等）设计；
4. **符合制谱规范**：包括但不限于——
   - 难度分布与官方谱面近似；
   - 情感循序渐进（谱面密度/复杂度随音乐情绪演进）；
   - 节奏对拍准确（note 与音频 onsets 对齐）；
   - 段落结构、休息段、高潮段安排合理。

## 当前阶段：前期准备（Phase 0）

### 已完成

- [x] 建立 GitHub 公开仓库 `aimai-chartmaker` 并推送初始提交
- [x] **官方谱制谱经验挖掘（第一批，2026-09-10）**：
  - **配置分布扫描**：脚本扫描本地 388 个官方 ST 谱，用已讲授的 14 类配置当透镜量化分布 → `docs/research/config-usage-survey.md`（**agent 观察，未经用户确认**）；
  - **逐谱精读**：按用户要求"必须逐个谱去看"，把本地 14 级（14.0–14.5）**87 个谱面文件全部读完**，逐谱记录「配置构成 / 强度难度分布 / 可复用手法 / 存疑」→ `docs/research/level14-readings.md`（含各组小结、总总结、跨谱候选装置 46+ 条、账目对账）；
  - **知识库建库**：`.agent/knowledge/` 现有 **30 条**原子条目（用户逐条讲授为来源，官方谱为验证）；
  - **过程记录**：`.agent/notes/` 现有 **33 篇**（编号 001–033）。
- [x] **音频分析攻坚（2026-09-11）**：在"BPM / offset / 变速点由用户直接提供"的前提下，攻克"AI 通过分析 mp3 了解哪里是副歌、哪里是高潮、采鼓点还是人声"（对应 MMFC 第五章强度/踩音/配置理论）：
  - **调研第二轮** → `docs/research/audio-analysis-research-v2.md`（onset→分音网格量化规则、offset 反向校验、J-pop 结构分段证据与日式标签词表、无官方音频的标定协议、切轨可计算代理、song sheet 表示、M4/py3.12 可行性、评测数据；24 条修订建议）；
  - **官方谱逐小节密度基准** → `tools/chart_analysis/`（simai 时间轴解析器，387/388 零错，manifest 对账 97.2%）+ `docs/research/official-chart-density-curves.md`（388 谱密度曲线：五段模板实测、密度地板、末段最强、休息段规律；结论入库为知识 **031**，知识 001 相应修订）；
  - **设计定稿 v1.1** → `docs/audio-analysis.md`（合并上述全部证据与原型实测；当前实现状态见其 §11）；
  - **原型** → `tools/audio_analysis/`（mp3 + BPM/first → 统一解码 → offset 校验 → Demucs 分轨 → 逐 stem onset 量化网格串 → 结构分段 → 强度曲线 → song sheet 双格式 + 复核图；3 首测试曲实跑，合成音频单测全绿）；
  - **官方音频配对标定（n=8 → 40 → 160）**（用户经 AWMC-MaiArchive 提供 160 首官方 ST 谱 zip → 本机 `official-mp3/`，不入库；**只用 ST，DX 不用**；新 120 首按官方六曲风分层、定数 13.0–14.5）→ `tools/calibration/` + `docs/research/audio-chart-calibration-n160.md`（n=8/n=40 版保留作对照）：同权重口径逐小节 ρ 0.30（四倍样本不变）、器乐 0.42 vs 人声 0.28（差三成，n=40 的 2.7× 是小样本放大）、六曲风 0.19–0.39 且可由人声曲占比解释；强度融合权重 n=40 按数据替换后在 n=160 上确认不再换（`[0,0.19,0.07,0,0.74]`）；鼓骨架 recall 0.65、piano 轨 lift 2.44 为第二有效踩音轨、候选池 recall 0.84 / precision 0.55；副歌人声领先仅 7/261 段但"人声作重音点缀"36% → 知识 002 该条维持存疑；结尾尾杀 65.6% / 渐弱 1.9% / 其他 32.5% 与 388 谱语料重合（知识 031 §10）；
  - **句读假设检验**（note 045）：LRC 时间轴当乐句边界的路线被阴性对照证伪（对齐器分不开真假 LRC）；免对齐的纯音频句读特征在 note 级无效、小节级只有小而真的增量（人声曲 R² +0.036±0.008，中位 +0.005）→ 未接进管线；
  - **分轨细化**（note 043）：htdemucs_6s 六路只有 piano 有净增益，guitar 与六路 other 劣于四路，basic-pitch 有音高 melody 轨是候选池灌水，人声曲 ρ 不因此改善 → 人声侧瓶颈在"人声活动"标量本身，需要乐句/句读边界一层结构；
  - 过程记录：notes 034–045。
- [x] **配置知识重开与采音钻研（2026-09-19，用户指令）**：
  - **B 站《配置术语》第二/三期消化**（note 046/048/051）：视频无人声，匿名取流 480p → 关键帧 OCR → 逐术语"教程说明（转述）/ 官方谱例（定位到小节）/ agent 理解（手级）"→ 知识 033–063；回指第一期的术语只在 006/007/010/011/013/014/017/018/019/024/028/029/030 追加"教程视角"，用户讲授原文一字未改；
  - **左右手分配器**（note 050）：`tools/chart_analysis/hands.py`，388 谱基线绝对无理 0.54‰、定式复现 15/18、手序难度与配置硬度正交；`docs/hand-sequencing.md` §5 = 各配置的手级定义规格；
  - **采音钻研**（note 047/049）：采音模式（全采/近全采/半采/稀采空音/加花/静默）**纯按音乐事件**判定（用户：采音根据音乐不能只看 BPM）；越难的地方踩得越省；"密"与"难"是两个正交旋钮；事件密度×强度→配置矩阵；
  - **定数×BPM×配置 谱面级对照**（note 052）：用户裁定难度只能按每张谱的定数、BPM、配置组合对照看，**agent 自造的 (D+H)/2 内在难度排序已撤回**；升定数 = 加配置种类与时长、靠密度与细分音；升 BPM = 只换分音与块长；一张谱中位用 10 类、无一张用满；
  - **配置识别器按手重写**（note 053）：`configs_hand.py` 以分配器输出为输入实现 41 个配置名（017–030 全部 + 第二期 10 + 星星篇 13），43 段 ground truth 全中；三张表用手序版重跑，核心结论站住（双押纵翻到全采族、侧边双押最偏爱 Bメロ）；
  - **用户手序讲授入库与分配器重调**（note 054/055）：知识 064–067 + 019/030 补充；`hands.py` v0.2（舒适区/出张、撤换手与末尾半边扣分、短纵连 ≤3 免拆、就近起手、侧边双押红线 = 突然∧¬引导，"突然"间隔阈值由官谱标定 140 ms）；388 谱绝对无理不变、出张落点 −47.5%；全部重跑并新旧口径并列。
  - **去量化与术语回归**（note 056/057，2026-09-20）：agent 自造的阈值/分档/分类全部撤销；采音三词按社区用语改为 全踩 / 舍音 / 留白。
  - **自主完善与落实**（note 058–061）：知识 068–087 agent 观察条目；侧边双押引导八型官谱普查（乐句级无"真突然"）；菱形/绕手/三角/似踩非踩普查。
  - **写谱工作流 v0.1→v0.2 与试写**（note 062/063）：`tools/chart_check` 检查器、`docs/workflow-sop.md`、`samples/trial-01`/`trial-02`（ロストワンの号哭）；知识 088–093。
  - **真实出题测试《Dear Player 2》**（note 064–067，2026-09-20 晚）：B 站官方 MV 匿名取音频 → 自动 BPM/offset → 分析 → 写谱 `samples/test-01/` → 桌面包交付；用户在 Visual Maimai 里看到"很多无理 + 全都没对齐"。返工过程：三路 tempo map（滑窗 onset-fit / librosa / beat_this）先判 **205 恒定**（一度提出看 MV 画面定相位被用户否决——**拍相位只能听音乐**）→ **被用户推翻**：用户给的别处抄谱 BPM 表（只读 `(bpm)` 标记，**禁止抄配置**）显示前奏 195→逐小节减速→125→155、尾奏 221→197→190→165→175→135→108，205 恒定只在中段 104 小节成立；错因 = 把无鼓的前奏/尾奏标"不可测"后仍按 205 外推并宣称恒定、把尾奏窗里量到的先升后降当成检测失效 → tempo map 按音频逐段复核重做，前奏/尾奏小节重排（note 067 更正 + note 068）；用户随后评价「写的实在是太烂了」并授权把那份抄谱当教材（touch/新版星星不计、仍禁抄 note 序列）→ 同曲逐段并排对照（note 069：密度没攒起来 / 分音只有一档 / 长条几乎没写 / 双押只用两个把位 / 不会同键重复 → 知识 095–104）；对照中发现两谱**下拍相位差一拍**，用户提醒参考谱与其 BGM 并未对齐、其相位不作证据 → 只凭我们音频裁定（205 段第一记 kick 起振点 24.470 s、薄 build 段拍手 backbeat、beat_this 下拍、chroma 变化点四路一致）：**小节线整体早一拍，`&first` 0.4279→0.1352，bar 1 整小节空拍、乐曲从 bar 2 第 1 拍切入**（note 067 第三次更正，grid.py 新增 downbeat_phase 四相位裁定）→ 按新网格与 069 教训重写 v2（note 070）；"歪"的真因 = **VM 吞掉 `||` 注释行之后的行首 `{x}`**（官谱 405/414 行首 `{x}`、0 份含 `||`）→ 交付版改官方格式无注释；复现 MaiMuriDX/MiaCode 静态无理为检查器第七层（160 首官谱逐首与上游 CLI 比对一致），本谱 24 处撞尾（病根：接力头排到第二拍，知识 094）改 17 小节清零、note 数不变；修 `&first≠0` 采音时钟、hands CLI 吃 meta 头、lv 半角 `+` 误报；旧 offset 复核对整格错位与 >0.4% BPM 误差不敏感 → `grid.py` 复核法重做。
- [x] 调研 simai 语法规范，产出文档 `docs/simai-syntax.md`（v1.0 定稿，2026-09-10）：
  - 三路调研合流：官方 simai wiki（记法定义者 Celeca 规范）+ 社区解析器源码 + 中文社区教程；
  - 原始调研报告存档于 `docs/research/`（official-spec-research.md / parser-source-analysis.md / chinese-community-research.md）；
  - 结论要点：note 全类型语法、slide 12 形状与端点约束、启动拍机制（60/BPM 固定一拍）、meta 转义规则、零报错安全子集与禁用清单（§6）。
- [x] 调研 simai 报错/警告的判定逻辑，产出文档 `docs/simai-error-checking.md`（v1.0 定稿，2026-09-10）：
  - 调研原则（用户决策）：报错判定逻辑以**成熟解析器的实际实现行为**为准（majdata、visual maimai、miacode 等）；
  - 已收录：MajdataEdit SyntaxCheck（15 组判定，源码行号级）、MajdataView 报错消息、SimaiSharp 5 类异常、MaiLib/maidata-rs、**MiaCode 34 类规则+双模式架构+Muri 检测**（源码级）、Visual Maimai（闭源，文档侧取证，置信度低）；
  - 校验器实现蓝本见该文档 §10（四层校验 + 双检 + MiaCode slide_data.json 白名单与 SimaiParserSpec 回归用例借鉴）；
  - 原始报告：`docs/research/parser-source-analysis.md`、`docs/research/miacode-vm-research.md`。

### 待办（按用户安排推进）

> 注意：以下各项仅在用户明确要求时启动，agent 不得擅自开工。

- [ ] **跨谱通用装置整理进 `.agent/knowledge/`**（用户已决定：14 级谱面读完后再一起写）
  - 星星（slide）配置的判断标准已由用户指定以 B 站 wasllll《配置术语第三期（星星篇）》为讲授来源（知识 047–063，2026-09-19）；`level14-readings.md` 中的星星类观察可据此对照归纳，但仍须标明来源与置信度。
- [ ] **逐谱精读向 13 级及以下扩展**（待用户安排；14 级已读完）
- [ ] **分析工具迭代与标定**（原型已可用；待做：`basic-pitch` 装包、结构标签与人声侧归因的人工听审复核、RWC-Pop / osu2beat2025 评测协议 A/B 档、更多官方音频下的权重标定；清单见 `docs/audio-analysis.md` §9/§10）
- [ ] **待用户拍板**：① CC BY 4.0 是否纳入依赖白名单（决定 SongFormer 去留）；② 知识 002「副歌全踩人声」仍存疑需听审；③ 手序对齐余项：064 口径下 `403-最終鬼畜妹・一部声` MAS m060–063 / `314-FLOWER` Re:MAS m071–072 算不算出张/反手（用户 2026-09-20 指示暂不问，agent 按两口径并列记录）；④ 知识 068–094 为 agent 观察条目，用户随时可覆盖或废弃；⑤ 测试谱 `samples/test-01/`（《Dear Player 2》Master，变速修正后）待用户评谱，评语按 AGENT 准则 5 记入 notes/knowledge。

  > **2026-09-20 清洗**（用户五条批评）：本项原先还挂着"出张是否分档""侧边双押位移阈值""稍快上界""≤3 是否含 hold/星星头""采音踩七成算不算全采"等**量化问题**，已全部删除——制谱判断是谱师的定性判断，**agent 自造的阈值/分档/合成分不得作为问题抛给用户**，量化只能留在工具内部当诊断。其中"≤3 是否含 hold/星星头"用户已答（「只要需要击打按键的不就算吗？」）。逐条处置见 `.agent/notes/056-去量化清理-术语回归用户口径.md`。
- [ ] **人声侧结构（暂缓，用户 2026-09-14 决定）**：LRC 歌词时间轴路线已证伪（note 045），剩余两条路（原曲完整音频做音频×音频对齐 / 人工听审标乐句边界）用户表示暂不投入；**在用户重新安排前不得启动**。现状=密度侧在 n=160 上饱和，人声曲 ρ 0.28 为已知上限。
- [ ] **工作流指引迭代**：`docs/workflow-sop.md` v0.1 已建，按端到端试写暴露的缺口持续修订（note 062/063）
- [ ] **test-01 后续**：① 音频分析按变速 tempo map 重跑（现 `out/test-01/song_analysis.json` 仍是恒速 205 网格，检查器采音层已自动跳过；管线需接受 `tempo_map.json` 的逐小节起点表，含小节中途换速）；② 减速段与尾奏 15 段 BPM 为「采抄谱值」，音频分不开，待用户评谱时一并核对；③ 真机（Visual Maimai）重新导入复核。

## 技术路线（v1.1，2026-09-11；详见 `docs/audio-analysis.md`）

**路线决策**（2026-09-10，用户）：特征分析先行、生成器后置——不采用"全频谱直接进 Transformer"的端到端路线。四层管线（v1.1）：

1. **L1 节拍层**：用户提供的 BPM/offset/变速点为最高优先级；管线只做 **offset 反向校验**（网格相位搜索 + BPM 漂移回归，只报告不覆盖）；全程 ffmpeg 统一转 WAV；beat_this/librosa 仅作缺省与交叉验证
2. **L2 音轨层**：Demucs htdemucs（MIT）四 stems → 逐 stem onset → **拍网格量化**（每小节单一分音、容差 `τ=clamp(0.25·slot,12,30)ms`、三连判别、**`{32}` 红线**）+ 鼓件三频带启发式 + 人声 VAD → 逐小节特征清单 = 踩音候选池
3. **L3 结构层**：all-in-one-infer（MIT）+ libfmp/SSM 重复段三路投票分段、**日式(英文)双标签**、pre_chorus 规则派生；强度 = 响度 / 去重 onset 数 / 鼓能量 / 人声活动 / 谱通量融合，五票定高潮；**强度→密度映射用 388 官方谱实测**（`T_density` + 密度地板，知识 031）
4. **L4 规划层**：结构 × 强度 × 音轨 → **song sheet 双格式**（`song_sheet.md` 给 LLM、`song_analysis.json` 给程序；逐小节 4 轨网格串是候选池不是谱面）+「段落类型 × 特征 → 主踩音轨」规则表（规则来自 `.agent/knowledge/` 001–005/031）；生成端只消费结构化特征（LLM 不接触原始音频）
5. **语法与可玩性校验**（`docs/simai-error-checking.md` §10）：四层校验 + SimaiSharp/MajdataEdit/MiaCode 三检，不通过自动迭代修复
6. **输出**：合法 `maidata.txt`（零报错安全子集，`docs/simai-syntax.md` §6）

工具选型红线：默认依赖仅 MIT/Apache-2.0/ISC/BSD；AGPL/GPL/CC BY-NC 权重不进默认管线（已排除 ADTOF / LarsNet / omnizart / pop-music-highlighter；SongFormer 为 CC BY 4.0 但其 MuQ 权重 NC，仅作非商用第二意见；详见 `docs/audio-analysis.md` §7）。

## 工作准则（coding agent 必须遵守）

1. **校验优先**：任何生成谱面的代码路径，必须带校验环节；未通过校验的输出不得视为完成。
2. **文档同步**：Phase 0 的调研结论必须先落到 `docs/` 下的 markdown 文档，再进入实现。
3. **语言约定**：文档与代码注释使用中文；提交信息（commit message）可使用中文或英文，需简洁描述变更。
4. **仓库结构约定**：
   - `docs/` — 调研文档与设计文档
   - `.agent/knowledge/` — 制谱意识知识库（谱面设计的稳定认知，跨上下文记忆，见该目录 README.md）
   - `.agent/notes/` — 项目变更记录（变更内容 + 理由，见该目录 README.md）
   - `resource/` — 用户提供的参考资料（本机留存，默认不入库，见该目录 README.md）
   - `tools/` — 分析工具代码（`tools/audio_analysis/` 音频分析管线原型；`tools/chart_analysis/` simai 解析器与官方谱密度统计），依赖装在仓库根 `.venv/`（python3.12，不入库）
   - `tests/` — 单元测试（只用自写 simai 片段与 numpy 合成音频，不放真实音频/官方谱原文）
   - `samples/` — 本项目自创的试写谱面文本（无音频；`samples/trial-NN/maidata.txt`）
   - `out/` — 分析输出（stems/特征/图，含版权音频衍生物，不入库）
   - `official-mp3/` — 用户提供的官方谱 zip（官方 maidata + 版权 mp3），仅本机标定用，不入库
   - `AGENT.md` — 本文件（项目对 agent 的说明）
   - 样例谱面等其余目录在需要时再统一规划
5. **版权与素材**：仓库中不得提交有版权的音频文件；测试用音频使用自创或无版权素材。
6. **社区合规**：调研 simai 语法与校验器时，参考社区公开资料并注明来源；若参考了特定开源项目，需遵守其许可证要求。
7. **变更记录**：每次对项目做实质变更（增删改文件、方向性决策）后，必须在 `.agent/notes/` 下新增一条编号笔记，说明**变更内容与理由**，并与变更在同一提交中推送；格式约定见 `.agent/notes/README.md`。
8. **制谱意识知识库（跨上下文记忆）**：模型上下文长度有限，**绝不允许依赖对话上下文来维持对制谱意识（谱面设计认知）的理解**——跨上下文的理解不合理、不可靠且会产生幻觉。规则：
   - 所有关于谱面设计的稳定认知（无论大小）必须沉淀到 `.agent/knowledge/` 下，作为长期维护的知识条目；
   - **生成谱面、评审谱面、设计生成器规则之前，必须先读取 `.agent/knowledge/` 中相关条目**，不得凭对话中的"印象"工作；
   - 从资料/调研/实验中获得的任何新制谱意识小点，**立即**新增条目并标注置信度与来源；
   - **知识不是金科玉律**：历史条目只是"当前最佳认知"，长期维护中会被新证据推翻——读取时保持批判（`存疑` 条目不得当作硬约束），与新证据冲突时以证据为准，修订或废弃旧条目并记录依据；
   - `.agent/knowledge/` 存稳定认知，`.agent/notes/` 存变更过程，二者勿混淆；格式约定见 `.agent/knowledge/README.md`。
9. **用户讲授制谱经验时——引文即边界**：用户给出的谱面引文（片段）**就是该配置的完整边界**。只允许在引文范围内核对与归类，**不得向引文之外延伸推理**（引文之后的谱面内容属于其他配置，未经讲授不得擅自归类）；发现疑问时先向用户确认，不得据此质疑用户提供的数据。
10. **不擅自开工**：待办/研究方向仅在用户明确要求时启动；宣布"即将开始某项工作"同样视为越界——先问，再动。
11. **谱面参考只看官方谱**（用户 2026-09-11 明确）：谱面设计的参考、统计与评测 ground truth 一律只用 `resource/official-chart/` 的官方谱；用户的自制谱（`~/Desktop/self-charts` 等）**不得**作为谱面参考或评测依据，其 `track.mp3` 仅可作音频分析的测试音频，且 `maidata.txt` 只读头部 `&first` 与首个 `(BPM)`，正文不读不引用不评价。官方谱目前没有对应音频，"音频 × 官方谱密度"的配对标定须等用户提供音频后再做。

12. **任何配置都按左右手定义**（用户 2026-09-19）：配置识别、硬度、无理判定、写谱规划一律建立在 `hands.py` 的左右手分配之上，不得按键位序列识别配置；**采音按音乐事件判定**（目标音轨实际发出的音 vs 谱面踩了哪些），不得按网格/分音/BPM 数格子。
13. **不得自造评分给用户下结论**（用户 2026-09-19「谁让你这么排的」）：难度只能按"每张谱的定数 × BPM × 它实际用的配置组合"对照着看；agent 定义的合成分、权重、阈值只能作解释变量，报给用户前先自问"谁让我这么定的"，并明示是 agent 操作化。
14. **交付前复核以社区工具口径为准**（2026-09-20 交付事故）：`chart_check` 全层零错零警 + MiaCode 静态无理零命中 + 官方谱格式（无 `||` 注释）三者齐备才可交付，自家按官谱校准的宽口径"无理 0"不算数；BPM/offset 的"恒定/不变"结论必须由允许变速的多路 tempo map（`tools/audio_analysis/grid.py` 的无相位滑窗 BPM 扫描 + 8 分量程相位 + 分带鼓件判半拍）支撑，不得只凭恒速网格回归转述；**拍相位与节拍只能用音乐证据，不得看 MV 画面**（用户 2026-09-20「必须听音乐」）。
## 参考资源（持续补充）

- 官方 simai wiki（记法定义者 Celeca 的规范）：https://w.atwiki.jp/simai/pages/1003.html （Notations of simai）、pages/1002.html（日文）、pages/25.html（旧版全量手册）、pages/510.html（meta 变量）。⚠️ 该站有 Cloudflare 防护，调研时经 Wayback Machine 快照获取。
- SimaiSharp（astrodx 官方解析器，本项目零报错校验基准）：https://github.com/reflektone-games/SimaiSharp
- MajdataView / MajdataEdit：https://github.com/LingFeng-bbben/MajdataView ；wiki《怎样写谱？》
- MaiLib：https://github.com/Neskol/MaiLib ；maidata-rs：https://github.com/xen0n/maidata-rs
- 中文社区：MMFC《谱面创作基础学》（用户提供 PDF，`resource/` 本机）；无理综述系列（B站）；详见 `docs/research/chinese-community-research.md` 来源清单
- 本仓库三份调研报告：`docs/research/` 下 official-spec-research.md / parser-source-analysis.md / chinese-community-research.md
