# 手序模型（左右手分配与手序合理性）v0.3

> **状态**：**v0.3（2026-09-20，去量化）**。在 v0.2 的基础上按用户 2026-09-20 的批评
> 把 agent 自造的量化框架清掉，逐项见 §9：
> ① **出张不分档**——它就是「左手落 2/3 / 右手落 6/7」这一个布尔判定加一个计数
> （「出张本来是一件很小的事啊，怎么就分档了？」）；
> ② **侧边双押删掉"位移/间隔"两个阈值门**——红线只保留知识 030 的原话逻辑
> 「突然（＝没有引导）的侧边双押」，"突然"不用数字定义，且结果**只作复核清单、不记无理**
> （「什么是侧边双押位移大的阈值……正常写谱谁去特意算这些事情」）；
> ③ **纵连「3 个以内」数的是所有需要击打按键的 note**（tap / hold 头 / 星星头）
> （「只要需要击打按键的不就算吗？」）。
> 工具里保留的数值一律是**内部诊断口径**，不是术语、不分级、不拿去问用户。
>
> **状态（上一版）**：**v0.2（2026-09-19，按用户讲授对齐）**。v0.1 的代价项与参数是 agent 拟的；
> 用户 2026-09-19 就手序给了一段讲授（逐字原话入库为知识 **064 / 065 / 066 / 067**，
> 以及知识 **019** 与 **030** 的用户补充），本版按那段原话重调，逐项对照见 §8「用户对齐记录」
> 与 `docs/research/hand-alignment-user-2026-09-19.md`。
> **v0.1 的参数集整体保留在 `hands.PARAMS_LEGACY` 里**，可一键切回做新旧对照。
>
> 实现见 `tools/chart_analysis/hands.py`，
> 验证见 `docs/research/hand-assignment-validation.md`，数据见
> `docs/research/data/hand-assignment-summary.csv`。
>
> **为什么有这份文档**：用户 2026-09-19 指出——
> 「你应该还要有左右手的意识啊，针对一张谱面要设计的谱面，左右手分别怎么打，
> 这会涉及到谱面的合理性这是个很严重的问题。」
> 以及「不可能只有定拍、错位、大宇宙是按手定义的啊，**任何配置都应该按手定义**吧。」
>
> 在此之前，本项目的配置识别（`configs.py`）、硬度分、无理检测都只看**键位序列**，
> 没有"手"这个对象。AGENT.md 的验收标准 2（人类可玩）与 3（手序合理）因此没有可计算的模型。
> 本文档定义这个模型，并把 `.agent/knowledge/` 017–030（配置）与 047–063（星星篇）
> 逐条改写成**手级定义**，作为"按手重写配置识别器"那条线的规格。

---

## 0. 定义来源与边界（读之前先看）

| 层 | 内容 | 可靠性 |
|---|---|---|
| **知识层** | `.agent/knowledge/` 条目原文：006 分页与正规手法、007 换手定式与一笔画、008–012 无理、014 启动拍、015 速度红线、023 可及范围、017–030 各配置、047–063 星星篇 | 用户讲授 / 教程明文 / 官谱实证 |
| **操作化层** | 把上面的文字变成代价函数、阈值、搜索策略。**全部集中在 `hands.py` 的 `PARAMS`**，可整体替换 | **agent 设定**；每个数字的来源写在 §4 表里 |
| **近似层** | slide 轨道几何（途经哪些 A 区）、Touch 分组、同头多 slide 的占手 | **agent 推断，存疑**；§7 列出 |

凡本文出现**超出知识条目原文**的规则，一律标 `（agent 推断，存疑）`。

---

## 1. 核心对象：任务（task），不是 note

"手"分配的最小单位不是 note，而是**需要占用一只手的原子任务**。
一个 note 可能产生 0、1 或 2 个任务，两个任务也可能被一只手一次吃掉。

| 任务类型 | 来自 | 开始时刻 | 占手到 | 依据 |
|---|---|---|---|---|
| `tap` | `tap` | note 时刻 | +1 帧（16.67 ms） | 006 |
| `hold` | `hold` | note 时刻 | 尾 +1 帧（尾部放宽 `hold_release_slack`） | 006 / 012 |
| `star` | `slide_star` | note 时刻 | +1 帧（星星头**按 Tap 处理**） | 006 |
| `slide` | `slide_track` | **头 + 60/BPM（启动拍）** | 名义终点（可提前 `slide_release_slack` 放手） | 014 / 048 |
| `wifi` | `w` 形滑条 | 同上 | 同上，但**同时占用两只手** | 006 |
| `touch` / `touch_hold` | Touch | note 时刻 | +1 帧 / 尾 +1 帧 | 006（TouchGroup 未实现，见 §7） |

**两条合并规则**（都来自知识 006「启动拍上的 Tap（拍划）并入划动手法，不单独占用一只手」）：

1. **拍划（tap）**：落在某条轨道启动拍上、与该轨道**头键相同**的 Tap → 并入该轨道任务；
2. **拍划（星星头）**：落在启动拍上、与头键相同的**另一条 slide 的星星头** → 同样并入。
   知识 006 同时规定"星星头按 Tap 处理"，所以星链（一圈星星头连续飞出、049 连续拍滑、
   057 一笔画）里"划到某键的同时按下该键上的新星星头"是**一次按压兼两件事**。
   ⚠️ 这条是把 006 的两句话合起来推出的（agent 推断，存疑），但**它是官方谱能跑通的前提**：
   不并入时 388 谱出现 4 455 次"占用冲突"，并入后降到 136 次（见验证报告 §3）。

**★ 拍星星头的手 ≠ 划轨道的手**（知识 054）。本模型把 `star` 与 `slide` 建成两个**独立可分配**
的任务，二者落到不同手只是一项代价（`w_star_switch`），不是禁止。知识 054 的换手阶梯
（11.8 → 13.5）与知识 007 定式 3（死镰）都要求这一点。

**★ slide 的占手是可伸缩区间**（知识 014 + 048）：

```
[ 最早开始 = 星星头 + 一个四分音符(60/BPM) ,  最晚结束 = 引导星到终点 + 15 帧(≈250 ms) ]
```

任务上记三个量：`t`（启动拍）、`t_end_nominal`（匀速到达终点）、`t_end_max`（名义尾 + 容错）。
**占用冲突判定用弹性下界**（允许"提前快滑" `slide_release_slack` = 250 ms 放手），
**移动代价仍按名义尾算**（保守）。知识 048 说这一窗口是星星篇一半以上"邪道手法"
（提前快滑 / 留尾 / 撞过去）的物理基础。

---

## 2. 状态与搜索

**状态**（每一步之后）：

```
(左手: 键位 K_L, 名义就绪时刻 R_L, 弹性余量 E_L)
(右手: 键位 K_R, 名义就绪时刻 R_R, 弹性余量 E_R)
last_hand        # 上一个"单任务时刻"归谁（交替先验用）
pending          # {轨道任务 → 打下它星星头的那只手}（一笔画/换手代价用）
```

**推进**：把任务按时间分组（容差 `simul_tol` = 4 ms，伪 each 的 1 ms 偏移算同刻），
逐组做 Viterbi 式转移。一组内枚举全部合法分配向量：

- 1 个任务 → `L` / `R`；
- 2 个任务 → `(L,R)` / `(R,L)`；若两键相邻且都是 tap/star，另加 `(L,L)` / `(R,R)`
  （知识 036 的"一只手一次刷过两个相邻键"，代价 `w_sweep_pair`）；
- Wifi → `LR`（双手），同刻其余任务判多押；
- ≥3 个任务 → 只能挑两个给两只手，其余记 **多押无理**（知识 008）。

**剪枝**：按状态去重 + 保留代价最低的 `beam` = 48 条。388 谱全量 45 秒。

**起手自由度**：`force_first_hand` 可强制首个任务的手。全库实测两种起手的**全谱**
代价差中位数只有 0.08%（§验证报告 §6），因为分页先验会在一两小节内把两手重新锚定。
⚠️ 知识 060 说的是**段内**起手奇偶（"相反起手能规避出张"），**逐星星段的起手枚举尚未实现**，
见 §7 扩展点 4。

---

## 3. 代价项（**v0.3，2026-09-20 去量化；主体为 v0.2 按用户 2026-09-19 讲授重调**）

每把一个任务交给一只手，累加以下各项（全部可关：把权重置 0）。
**优先级：用户口径 > 教程口径（B 站二/三期）> agent 操作化。**
凡数值未由用户给出的，一律标 **agent 设定**。

| 记号 | 代价项 | 公式 | 依据 |
|---|---|---|---|
| **P** | **可及性（舒适区）** | 出张 → `w_chuzhang`；否则 0。**出张 = 左手落 {2,3} 或右手落 {6,7}**（布尔判定，**不分档**），共享区 `{1,4,5,8}` 两手均 0 | **064（用户）** |
| P₀ | 几何 tie-breaker | 落在知识 006 分页的对侧 → `+w_home_tiebreak`（0.01） | **agent 操作化**（见下"为什么还要留一个 0.01"） |
| P′ | 轨道尾可及性 | `(reach(手, 尾) − reach(手, 头)) / 2` | 064（划轨手要把整条轨走完） |
| **M** | **位移** | `w_move × 环距 / 4` | 003-2（位移越大越难） |
| **V** | **速度** | 见下；**同键串长度 ≤3 时免除**（长度优先于速度；长度按**需要击打按键的 note** 数——tap / hold 头 / 星星头） | 015 + **019 用户补充** |
| **A** | **交替先验** | 上一个单任务同手 → `+w_same_hand`；同键且在舒适速度内再 `+w_same_key_extra`（负）。**段首（见 T）不计**；**长度 ≤3 的同键串不计、且 `w_same_key_extra` 全额给** | 018 / 027 / **019 用户补充** / **067** |
| **T** | **段首位置连续** | 距上一任务组 ≥ `seg_gap_beats` 拍时，`+w_seg_start_far × 环距 / 4`，并关掉 A | **067（用户）** |
| S | 末尾半边 | 划轨手不在 slide 末尾半边 → `+w_slide_end_side`（**0.02，仅 tie-breaker**） | 007 → 被 **065** 降级 |
| H | 星头换手 | 划轨手 ≠ 拍头手 → `+w_star_switch`（**0，已撤销**） | 007 → 被 **065** 撤销 |
| **G′** | **错位段的星头换手** | 这条轨道的启动拍等待期里**有错位音**，且划轨手 ≠ 拍头手 → `+w_misalign_switch` | **017（用户原文）** |
| C | 一笔画续划 | 上一动作停在本轨道头键且时间首尾相接 → `+w_chain_bonus`（负） | 007 / 057 |
| G | 错位先验 | 落在某 slide 启动拍等待期、贴近其星星头的音符给了非拍头手 → `+w_misalign_other` | 017 |
| U | 无手可用 | `+w_unassigned` 并记多押 | 008 |
| W | 一手扫同刻相邻两键 | `+w_sweep_pair`（**1.20**，高于出张，见下） | 036 + **066** |

### 3.1 舒适区模型（知识 064）——替换 v0.1 的分页二分

用户原话：「左手最好处理左半屏（8765）以及向右方延伸的上方 1 和下方 4、右手最好处理
右半屏（1234）以及 85，如果写出来涉及到右手需要拍 6 或者 7，左手拍 2 或者 3
那你就要好好考虑难度了……就算是难点也要做好引导。」

| 键位 | 1 | 2 | 3 | 4 | 5 | 6 | 7 | 8 |
|---|---|---|---|---|---|---|---|---|
| 左手 | ✅ | ❌ 出张 | ❌ 出张 | ✅ | ✅ | ✅ | ✅ | ✅ |
| 右手 | ✅ | ✅ | ✅ | ✅ | ✅ | ❌ 出张 | ❌ 出张 | ✅ |

- **v0.1 的"越界深度 1"（右手 5/8、左手 1/4）整档取消**；
- **出张不分档**（v0.3，用户 2026-09-20：「出张本来是一件很小的事啊，怎么就分档了？」）——
  它就是 `is_chuzhang(hand, key)` 这**一个布尔判定**加一个**落点计数**；跨到 6 还是 7、
  跨多久、跨多频繁**都不构成档位**，模型不得自造分级，也不把"要不要分档"抛给用户。
  搜索时给它记的 `w_chuzhang = 0.90` 是**内部代价数字**（agent 操作化，用户只说"轻易不要写"），
  唯一作用是让搜索在"能不出张"时不要出张；它明显大于 `w_move` 上限（0.12）、`w_same_hand`（0.20）
  与 `w_seg_start_far` 上限（0.30），所以**"就近起手"与"避免出张"冲突时避免出张优先**；
- 出张**不是无理**，是风格预算（§6.2 的 `budget.chuzhang`）。

**为什么还要留一个 0.01 的 tie-breaker**（`w_home_tiebreak`，**agent 操作化**）：
共享区上两手同价会让"1 该谁打"成为**完全平局**，解由束序偶然决定，还会给出
"左手打 1、右手打 8"这种两手交叉的物理怪解。0.01 只打破平局
（是 `w_move` 的 1/12、`w_chuzhang` 的 1/90），**不表达 064 之外的任何偏好**。

### 3.2 星星头与星星条（知识 065）

用户原话：「理论上来讲，星星头和星星条是两个部分，但是也没有严格规定说拍星星头的手
就一定能或者不能划星星条，也就是 A 拍头可以 A 划条也可以 B 划条。」

→ **H（星头换手）撤销置 0**；**S（末尾半边）降为 0.02 的 tie-breaker**（重调清单取的保守解，
用户只说"无严格规定"，没说完全作废）。

⚠️ **一个例外：知识 017 的错位。** 017 是用户亲授的**配置定义**，原文写死
「错位音**固定由滑动的那只手自己拍**——头 → 错位音 → 启动拍 → 划轨」。
065 撤销的是"头与条同手"的**普遍偏好**，不是 017 这条配置定义。
不给这一条留口子的话，H=0 之后模型会把"头 + 错位音"给 A、轨道给 B，
绕开 G 却并不满足 017（官谱 `141-カゲロウデイズ` Re:MAS m32 上实测发生过）。
故新增 **G′ `w_misalign_switch = 0.45`**，只在"该轨道真有错位音"时生效。

### 3.3 纵连：长度优先于速度（知识 019 用户补充）

用户原话：「纵连其实没什么要求，理论上稍快且长度较小的纵连也可以单手处理
（长度较小指的是 3 个 tap 或以内）。」

→ 同键**连续串**长度 ≤ `vertical_single_hand_max`（3）时：
**免除速度项 V**、**不加 A 的 `w_same_hand`**、`w_same_key_extra` **全额**给出，
即使 `dt < t_req(0) = 83.3 ms`。**硬下界 `t_hard_same = 33.3 ms`（知识 009 叠键红线）仍生效。**
长度 >3 才回到速度线判"拆"。

**长度怎么数（v0.3，用户 2026-09-20）**——主会话问过"≤3 是否含 hold / 星星头"，用户原话：

> 「你这问的什么问题？**只要需要击打按键的不就算吗？**」

→ 同键连续串里 **tap / hold 的头 / 星星头**一律计入（`hands._HIT_KINDS`）。
v0.2 只数 `tap` 是照"3 个 tap 或以内"的字面取的，已按这句话改掉，**这条待裁定项随之删除**。
388 谱实测：同键连续串（≥2）从 10 306 涨到 15 253，其中含 hold/星星头的 5 255 个——
64 个因此由"≤3 免拆"变成按速度线判拆，4 939 个在旧口径下根本不成串（现在够长、拿到免拆资格）；
**绝对无理 146 / 硬无理 118 / 不可行 141 三项一个没变**。

### 3.4 段首起手 = 位置连续（知识 067）

用户原话：「先出手肯定是最好是离上一段结束的手，因为上一个配置把手带到这里了……
其实上一个配置结尾的键的附近的地方也是可以的。」

→ 段边界**不重置两手状态**（本模型本来就是全谱一条 Viterbi，天然满足）；在此之上加两件事：

1. **T 项**：距上一任务组 ≥ `seg_gap_beats`（2 拍，**agent 操作化**）时，
   按环距额外计价 `w_seg_start_far × d/4` → **离得近的那只手起手**；
2. **关掉 A**：隔了一个休止不存在"上一音该换手了"这回事。

不加这两条，休止之后 `w_same_hand`（0.20）会压过 M 的上限（0.12），
把起手推给**远**的那只手——与 067 相反。
**§7 扩展点 4 的"逐段起手奇偶枚举"随之降级为诊断工具**（`force_first_hand` 保留）。

### 3.5 侧边双押：有没有引导（知识 030；**v0.3 删掉阈值框架**）

知识 030 的铁律原话只有一句：「**禁止写突然的侧边双押（23、67）**——除非前期做好了
充分多的引导」。所以**判定只有一件事：有没有引导**。

```
guided(D) = A/B  与前一组双押**共享键**（"双押带过来" / 18·87·76·65·54）
          ∨ C    整串都是相邻键对、锚沿环**等距轮转**（18·23·45·67），且 D 不是串首
          ∨ D    "临近位置"已经落在这两个键上（＝单点铺垫 → 双押串）
红线(D)   = ¬guided(D)        # "突然" = 没有引导，**不用数字定义**
```

- **v0.3 删掉的东西**（用户 2026-09-20：「什么是侧边双押位移大的阈值，不是，你怎么能量化
  来分析这些事情呢？正常写谱谁去特意算这些事情。」）：v0.2 曾把"突然"实现成
  `位移 ≥ side_sudden_disp(4) ∨ 间隔 ≤ side_sudden_gap_sec(0.140 s)`，
  **两个阈值参数与这层判据整体删除**，也不再作为待用户裁定项。
  标定过程留在 `hand-assignment-validation.md` 的方法附录里，**不是规则**。
- **四型都是形状判定，没有数值门**。D 型的依据是知识 030「类型与实例」表里原有的
  「**单点铺垫 → 双押串**」一型（`846-+♂` 谱例），即用户 A 型"双押带过来"的单点版；
  不加这一型，官谱里"6-7 交替颤音直接合成 6/7 双押"这种手已经在键上的形态会被误判。
- **★ 结果是复核清单，不是判决**：`HandAssignment.side_doubles` 里 `guided=False` 的那些
  就是「请人看一眼」的清单，**不记 `muri`**（v0.2 的硬级标记 `突然侧边双押` 整条删除）。
  理由：A/B/C/D 四型是 agent 按用户给的**三个例子**归纳的，覆盖不全——388 官谱上
  685 次侧边双押里有 **307 次**落在四型之外，把它当自动硬无理等于宣称"官方谱写了 307 次
  硬无理"，那是替用户下结论。`chart_summary` 里出 `side_double` / `side_double_unguided` 两个计数。

### 速度项（单手移动时间模型）

```
舒适线   t_req(d)  = t_hit_min + t_per_key · log2(1+d)
扫键舒适线（d=1）  t_sweep_req
硬下界   t_hard(d) = t_hard_same (d=0) ;  max(t_hard_floor, t_hard_floor + t_hard_per_key·log2(1+d)) (d≥1)

dt = 本次出手时刻 − 上一动作名义结束时刻
dt ≥ t_req             → 0
t_hard < dt < t_req    → w_speed × ((t_req−dt)/(t_req−t_hard)) ^ speed_exponent      （凸，越贴线越陡）
dt ≤ t_hard            → 不可行（超速无理）
```

**`t_hit_min = 83.3 ms` 的来源**：知识 015 给出官谱最快同键纵连 = 白系 177.6 BPM 的 32 分
（间隔 42.2 ms）。知识 019 说这种长纵连"绝大多数玩家采用拆"——两手拆打则**单手 84.4 ms**。
取 1/12 s = 83.3 ms 作单手连续击打的下界，正好卡在这条官谱纪录上（比值 1.013）。
**v0.2：这条线只管长度 >3 的同键串**（§3.3）。

**扫键**：知识 015 另给出扫键纪录（怒锤 200 BPM 64 分 = 18.75 ms、白日舞 96 分圈 = 24.5 ms），
与距离无关，所以 `t_hard_per_key = 0`、`t_hard_floor = 18.7 ms`。相邻键在舒适线以下时按
**扫键模式**计价（`w_sweep_step`）。
⚠️ **同刻**相邻两键的"一手扫掉整组"（`w_sweep_pair`）**v0.2 从 0.90 上调到 1.20**：
舒适区模型下 `2/3`、`6/7` 的两手解要付一次出张代价，旧值会让分配器改用"一只手扫掉整组"
绕开——那正是知识 **066** 禁止的"因为有手法所以可以塞难配置"。

### 参数默认值表（v0.3）

> ⚠️ **表里的数值一律是 agent 的内部操作化**，不是术语、不是分级、也不拿去问用户
> （用户 2026-09-20）。凡标 **agent 设定 / 操作化** 的行，只在工具内部起作用。

| 参数 | 默认 | 来源 |
|---|---|---|
| `hand_model` | `"comfort"` | **064（用户）**；`"page"` = v0.1 口径，见 `PARAMS_LEGACY` |
| `w_chuzhang` | **0.90** | **agent 设定的内部代价**（064 只说"轻易不要写"）；出张本身是布尔，**不分档** |
| `w_home_tiebreak` | **0.01** | **agent 操作化**（只打破平局，§3.1） |
| `frame` | 1/60 s | 知识 006 |
| `simul_tol` | 0.004 s | 伪 each 相差 1 ms（语法 §4.3） |
| `hold_release_slack` | 0.0167 s（1 帧） | 知识 012 的保守取值 |
| `slide_tolerance_sec` / `slide_release_slack` | 0.250 s | 知识 048（终点 ±15 帧） |
| `t_hit_min` | 0.0833 s | 知识 015 反推 |
| `t_per_key` | 0.030 s | **agent 推断**（知识 022 LAMIA 校准） |
| `t_sweep_req` | 0.0333 s | 2 帧；知识 015/036 |
| `t_hard_same` | 0.0333 s | 知识 009（同判定区 2 帧） |
| `t_hard_floor` | 0.0187 s | 知识 015（怒锤 18.75 ms） |
| `speed_exponent` | 0.5 | **agent 设定**（凸） |
| `w_move` | 0.12 | **agent 设定** |
| `w_speed` | 0.90 | **agent 设定** |
| `w_same_hand` | 0.20 | **agent 设定**（知识 018） |
| `w_same_key_extra` | **−0.22** | **agent 设定**；**v0.2 从 −0.12 重标定**——舒适区下共享键两手同价，
−0.12 会让知识 027 的定拍段被拆成左右交替 |
| `vertical_length_first` / `vertical_single_hand_max` | `True` / **3** | **019 用户补充**；长度按**需要击打按键的 note** 数（tap/hold 头/星星头，用户 2026-09-20） |
| `seg_gap_beats` / `seg_gap_min_sec` / `w_seg_start_far` / `seg_reset_alternation` | 2.0 / 0.40 s / **0.30** / `True` | **067（用户）** + agent 操作化 |
| `w_star_switch` | **0.0** | **065（用户）撤销** |
| `w_slide_end_side` | **0.02** | **065（用户）降级为 tie-breaker** |
| `w_misalign_switch` | **0.45** | **017（用户原文）**，只在该轨道有错位音时生效 |
| `w_chain_bonus` | −0.30 | **agent 设定**（知识 007/057） |
| `w_misalign_other` / `misalign_ring` | 0.45 / 1 | **agent 设定**（知识 017） |
| `w_sweep_pair` / `w_sweep_step` | **1.20** / 0.35 | **agent 设定**（知识 036 + **066**） |
| `w_unassigned` | 3.0 | **agent 设定**（多押=绝对无理） |
| ~~`side_double_redline`~~ / ~~`side_sudden_gap_sec`~~ / ~~`side_sudden_disp`~~ | — | **v0.3 删除**：判定不用数字（§3.5） |
| `side_near_slots` | 2 | **agent 操作化**（"临近位置"取前几个任务组，只用于 D 型形状判定） |
| `side_guide_gap_beats` / `side_rotation_min_run` | 1.0 / 3 | **agent 操作化**（030 的 B/C 型形状） |
| `one_hand_two_objects` | `False` | **066（用户）** |
| `chain_tol` | 0.020 s | **agent 设定** |
| `beam` | 48 | **agent 设定**（工程量） |
| `force_first_hand` | `""` | **067 之后只作诊断工具** |

<details>
<summary><b>v0.1 旧口径（2026-09-19 用户讲授之前，agent 拟；整体保留在 <code>hands.PARAMS_LEGACY</code>）</b></summary>

| 记号 | 代价项 | 公式 | 知识依据 |
|---|---|---|---|
| P | **分页先验** | `w_page × 越界深度(手, 键)` | 006（右手 1234 / 左手 5678）+ 023（右手够到 5/8，够不到 6/7 → 深度 2） |
| P' | 轨道尾部分页修正 | `w_page × (深度(尾) − 深度(头)) / 2` | 划轨手要把整条轨走完（agent 推断） |
| M | **位移** | `w_move × 环距 / 4` | 003-2 |
| V | **速度** | 同上速度项 | 015 |
| A | **交替先验** | 上一个单任务同手 → `+w_same_hand`；同键且在舒适速度内再 `+w_same_key_extra`（负值） | 018 / 019 / 027 |
| S | **末尾半边** | 划轨手不在 slide 末尾半边 → `+w_slide_end_side` | 007 + ep3 §6.1-3 |
| H | **星头换手** | 划轨手 ≠ 拍头手 → `+w_star_switch` | 007 一笔画偏好；054 说明它**允许**发生 |
| C | **一笔画续划** | 上一动作正好停在本轨道头键、时间首尾相接 → `+w_chain_bonus`（负值），且**免除 S 与 H** | 007 / 057 |
| G | **错位先验** | 落在某 slide 启动拍等待期、且贴近其星星头的音符给了**非划轨手** → `+w_misalign_other` | 017 |
| U | **无手可用** | `+w_unassigned` 并记多押 | 008 |
| W | 一手扫同刻相邻两键 | `+w_sweep_pair` | 036 |

v0.1 的权重：`w_page 0.30` / `w_move 0.12` / `w_speed 0.90` / `w_same_hand 0.20` /
`w_same_key_extra −0.12` / `w_star_switch 0.25` / `w_slide_end_side 0.35` /
`w_chain_bonus −0.30` / `w_misalign_other 0.45` / `w_sweep_pair 0.90` /
`w_sweep_step 0.35` / `w_unassigned 3.0`；无出张项、无段首项、无侧边双押红线。

</details>

---

## 4. 无理判定，以及"蹭的压力"要跟无理分开

知识 048 之后必须区分两件事：

- **`muri`（无理）**：人类正规手法**办不到**或**必须改手法**才办得到 → 硬性验收项（AGENT.md 标准 2/3）；
- **`scrape_pressure`（蹭的压力）**：星星篇里大量的"蹭"是**设计手段**（知识 048「能黄就是好」、
  052 菱形星星"一个字：蹭"、062 防蹭段），**不是无理** → 只作强度/风格指标。

实现口径：**软级**的 `撞尾` / `外键` / `路径蹭键` / `换手拧巴` 归 `scrape`，硬级与绝对级留在 `muri`。

| 标记 | 级别 | 判据 | 知识 |
|---|---|---|---|
| **多押** | 绝对 | 同刻需要 ≥3 只手 | 008 |
| **叠键** | 绝对 | 同一键两 note 间隔 < 33.3 ms | 009 |
| **占用冲突** | 绝对 | 两手都被占住还来任务（整组不可行） | 006/012 |
| **超速** | 绝对/硬 | 单手 `dt ≤ t_hard(d)` / 贴线 > 75% | 015 |
| **Hold 尾多押** | 硬 | Hold 名义尾 ±50 ms 内有同刻 ≥2 note | 012（"Hold 尾判 + 同时双押 = 标准多押"） |
| **外键** | 硬/软 | **启动拍**后 200 ms 内、**同一个键**上出现由**非划轨手**处理的 Tap/Hold；gap ≥150 ms 或 ≤50 ms 或带 Ex → 软 | 010（+ 388 谱校准，见下） |
| **撞尾** | 绝对/硬/软 | 轨道名义到达终点 ±(−50, +200) ms 内该键出现非划轨手的 Tap/Hold；踩在到达时刻=绝对；0–150 ms=硬；正好一个八分且 BPM ≤ 210 → 软 | 011 |
| **路径蹭键** | 软 | 弧形轨道途经的 A 区在经过时刻 ±60 ms 内有非划轨手的 note | **agent 推断，存疑**（033 的"防蹭"） |
| **换手拧巴** | 软 | 同刻左手在右半圈且右手在左半圈、交叉深度 ≥3，且前后 1 s 内两手都没划过 slide | **agent 推断**（007 定式 4 的换位划不算） |
| ~~突然侧边双押~~ | — | **v0.3 撤出无理表**：改为 `side_doubles` 里「无引导」的**复核清单**（§3.5），不记 `muri` | **030** |

> **★ 知识 010 的"同头（同侧）"在 388 官谱上校准为"同一个键"、窗口锚在启动拍。**
> 四种读法全量跑过（验证报告 §3.4）：按"环距 ≤1 + 锚在星星头"读，388 官谱出现 832 次硬外键；
> 按"同键 + 锚在启动拍"读只剩 13 次。官方谱不会系统性地写硬无理，所以取后者。
> 这是对条目 010 的**校准建议**，不是推翻。

---

## 5. 17–30 各配置的手级定义（`configs.py` 按手重写的规格）

> **读法**：「L / R 只是记号」——实际落哪只手由 006 分页与段首起手决定，表里写"守 A 的手 / 走 B 的手"。
> **机检判据**一列直接消费 `hands.assign()` 的输出（`left_seq` / `right_seq` / `pairs` / 逐小节指标），
> **不再看原始键位串**。
> 置信度：**用**＝用户讲授、**谱**＝官谱实证（本轮分配器在条目点名的谱例上复现）、**推**＝agent 推断存疑。

| # | 配置 | 左手做什么 / 右手做什么 | 时序关系 | 可机检判据（手级） | 置信度 |
|---|---|---|---|---|---|
| **017** | 错位 | **划星的那只手一个人干完**：头 → 错位音 → 启动拍（拍划）→ 划轨；另一只手同刻在另一条星星线上干同样的事 | 两条星星线相位差一拍，环环相扣 | 存在 slide 任务 T，其 `wait_of` 窗口内有任务 X，且 `hand(X) == hand(head(T)) == hand(T)`，且 X 的键 ≠ T 的头键 | 用+谱 ✅ |
| 017 注 | **v0.2**：065「头与条可分属两手」**不适用于错位**——017 的用户原文把头 / 错位音 / 轨道写死在同一只手上。模型用 G′（`w_misalign_switch`）守住这条，见 §3.2 | — | — | 用 ✅ |
| **018** | 普通交互 | 两手严格逐音交替，各守本方分页 | 匀拍 | 连续 ≥6 个单任务槽，`alt_rate == 1.0` 且 `max_same_run == 1`；跨半圈数 = 0 | 用+谱 ✅ |
| **019** | 纵连 | **长度优先于速度**（用户补充）：**同键 ≤3 个音允许单手**（即使稍快）；>3 才按速度线判"拆"。**"3 个以内"数的是需要击打按键的 note**（tap / hold 头 / 星星头，用户 2026-09-20） | 同键连续 ≥3 音 | 同一键连续 ≥3 个任务；`single_hand_ok = 长度 ≤3`；长度 >3 时 `一手连打` 与 `两手交替` 由 `t_req(0)=83.3 ms` 分界；长纵连（>5）看 `max_same_run` | 用+谱 ✅ |
| **020** | 三角交互 | 每 3 音一组，组内两手交替（占 2 键时一手打两次，占 3 键时三音三键） | 组间有空拍或匀速 | 按 3 音切组；组内手序为 `XYX`（占 2 键）或 `XYX`/`XY?`（占 3 键）；正/反三角的手级差别**未找到**（见 §7） | 用+谱 ⚠️ |
| **021** | 轴交互 | **一只手钉在轴键**、另一只手打两侧邻键 | 轴音隔位重复（BABCB） | 某手的键序列常量（`len(set)==1`）而另一手在 ≥2 个键上摆动，交替率 = 1 | 用+谱 ✅ |
| **022** | 楼梯交互 | 两手**反向**移动，成对推进、每步只走相邻键 | 成对 | 相邻同手步进 `cstep` 恒为 +1 或 −1，且两手符号**相反**；走向切换处出现一次"飞"（同手 d ≥ 2） | 用 ✅（本地无谱） |
| **023** | 逆楼梯 / 方向盘 | 两手**同向**移动、**始终 180° 对位** | 同步 | 每个同刻/相邻对满足 `cdist(L,R) == 4`，且两手 `cstep` 符号**相同**；必有折返 | 用+谱 ✅ |
| **024** | 大宇宙 | **每只手在自己那对相邻键上横跳** | 两手交替 | 各手键集合大小 = 2 且组内 `cdist == 1`；两手键集合不交；两集合并起来恰 4 键 | 用+谱 ✅ |
| **025** | 散点 | 两手交替但**都不收敛到固定小键集** | 匀拍 | 交替率高但每手键集合 ≥4、同手步进种类 ≥3，且不满足 022/023/024 的手级条件 | 用 ⚠️ |
| **026** | 跳拍 | 手序本身不特殊，**空拍不规则** | — | 任务间隔序列的取值种类 ≥3（手序层无特征）→ **跳拍不是手级配置**，应留在时间层判 | 用（概念版） |
| **027** | 定拍 | **一只手在一个键上从不歇**（节拍器）；另一只手处理别的配置、可以有空拍 | 定拍手密度可达另一手的两倍 | 某手键序列常量且**槽槽都有**，另一手同窗内有空槽；与 021 的区别 = 定拍手**不参与交替结构**（相邻两次之间**不一定**夹着另一手的音） | 用+谱 ✅ |
| **028** | 子弹 | 同一键短时间两次：可同手连打、也可两手各一次 | 间隔 ≤ 半拍 | 同键恰 2 次且间隔 ≤ `bullet_gap`；记录是否同手（同手=真连打，异手=分摊） | 用+谱 ✅ |
| **029** | 单双 / 双单 / 夹心 | 双押时两手同刻；单点时一只手（通常是"变化"的那只）出手 | 双击与单点交替循环 | `pairs` 与单任务槽交替出现；"骨架"= 在双押与单点中**键位不变**的那只手 | 用+谱 ✅ |
| **030** | 连续双押 | 连续 ≥3 个 `pairs`；两手各守一侧 | — | `pairs` 连续 ≥3；**双押纵** = 两手键都常量；**绕圈** = 两手同向逐格推进；**侧边双押（v0.3）** = `{L,R} ∈ {{2,3},{6,7}}`，只标**有没有引导**（§3.5；引导 = A/B 共享键步进 ∨ C 等距轮转 ∨ D 临近同键），无引导的进**复核清单**、不记无理——**侧边双押本身不是禁区，"没有引导"才是** | 用+谱 ✅ |

### 5.1 星星篇（047–063）的手级定义

直接采用 `docs/research/config-terms-ep3.md` §4 总表，本模型的对应实现：

| # | 配置 | 本模型的表达 | 本轮复现 |
|---|---|---|---|
| 049 | 连续拍滑 | 拍头手 == 划轨手；两手相位差一拍（`hand(star_n) == hand(track_n)`，间隔恒一个四分音） | ✅ Contrapasso m090/m092 |
| 050 | 双压连续拍滑 | 两手同刻同相位、段内不换手（每手键序列常量、`pairs` 连续） | ✅ 猫祭り m034–m037 |
| 053 | 夹键拍滑 | 同 049，且同一键连续 3 个八分（首=头、尾=启动拍） | ✅ ECHO m022/m026 |
| 057 | **一笔画** | `end(track_i) == head(track_{i+1})` 且时间首尾相接 → `chain`，同一只手；另一只手专拍头 | ✅ BREAK YOU!! m066/m070/m078（3/3 全对） |
| 058 | 挥手段 | 某手的 slide 头键常量且 ≥3 根，另一手做独立线；段内不换、段落间对调 | ✅ 緋色のDance Re:MAS m041–m046 |
| 060 | 同起点拍滑 | 同一头键放射 ≥4 根、两手交替各领一根；**`出张` 改按知识 064 的键位定义计数**（右手落 6/7 / 左手落 2/3），007「末尾半边」口径作废 | ✅ 深海少女 m073、炎歌 m051–m052 |
| 061 | 三叉戟 | 同一头键恰 3 根**全给一只手**，另一手独立线 | ✅ LANCE m072–m075 |
| 062 | 鼓动段 / 防蹭段 | 拍滑单元等间隔左右交替 ≥6 次 + 统计 `scrape_pressure` | **✅ v0.2 复现**（`352-鼓動` m039/m040 拍头手 == 划轨手）——v0.1 的 ⚠️ 是 `w_slide_end_side` 压过 `w_star_switch` 造成的，065 撤销这两项后自动消失 |
| 054 | 换手（手法） | 不是配置而是**自由度**：`hand(star) != hand(track)` 的根数。**v0.2：它不再有代价**（065），也**不再等同于出张**（出张按 064 的键位定义独立统计） | — |

### 5.2 一条横贯结论

**「几何越规整的配置，手序越省心」** —— 388 谱实测（验证报告 §7）：
方向盘 0.205、大宇宙 0.227、长纵连 0.234、三角 0.244、楼梯 0.256 是**手序难度最低**的一批；
侧边双押 0.515、错位 0.444、单双 0.374 最高。这与 `configs.py` 现有 `CONFIG_WEIGHT`
（大宇宙 1.00 排第一）**几乎相反**，说明二者量的是**两个不同的轴**：
`CONFIG_WEIGHT` 量的是体力/精度负担，手序难度量的是**手的调度余量**。
按手重写 `configs.py` 时，这两个分数应当**并存**而不是互相替换。

---

## 6. 与生成端的对接

### 6.1 铁律：**写谱时必须先做手分配，再落 note**

不能"先把 note 摆好再检查手序"——那样只能事后发现无理、无法修。正确顺序：

```
段落计划（song sheet：结构 × 强度 × 踩音轨）
   ↓
① 选配置（按手级定义选：这一段两只手各承担什么角色）
   ↓
② 分配手角色与起手（哪只手是骨架/定拍/划轨手；段首起手的奇偶）
   ↓
③ 按角色落 note（把每只手的键序列写出来，再交织成 simai）
   ↓
④ hands.assign() 回读校验 → 绝对/硬无理必须为 0，软无理与 scrape_pressure 记录
   ↓
⑤ 语法校验（docs/simai-error-checking.md §10）
```

### 6.2 song sheet / charting plan 的字段建议

在 `song_analysis.json` 的逐段落计划里增加 `hand_plan` 块：

```json
"hand_plan": {
  "config": "定拍",              // 手级配置名（§5 的表）
  "role_a": {"hand": "R", "job": "定拍", "keys": [2], "density": "每槽"},
  "role_b": {"hand": "L", "job": "变化", "keys": [6, 8], "density": "每两槽"},
  "start_hand": "R",             // 段首起手（知识 060：奇偶影响出张）
  "slide_owner": "role_b",       // 划轨手（知识 007 末尾半边 → 打分项）
  "budget": {"chuzhang": 2, "scrape_pressure": 4, "muri": 0}
}
```

生成端的硬约束只有一条：**`budget.muri` 必须为 0**（绝对与硬级）。
`chuzhang` 与 `scrape_pressure` 是**风格预算**，允许按段落强度调高——
知识 048/062 明确"蹭"是设计手段。

### 6.3 对外 API（`tools/chart_analysis/hands.py`）

```python
from chart_analysis.hands import assign, bar_hand_sequences, chart_summary

ha = assign(parse_chart(text))      # 也接受 list[NoteEvent]
ha.rows            # [(t, bar, key, note_type, hand, occupancy_until), ...]
ha.left_seq        # [(t, bar, key, task_kind), ...]
ha.right_seq
ha.pairs           # [(t, bar, 左手键, 右手键), ...] 同刻双手对
ha.bars            # [BarHand] 逐小节：alt_rate / max_same_run / cross_count /
                   #   chuzhang / max_speed_l,r / conflicts / muri / hand_hardness
ha.muri            # 无理（绝对/硬）
ha.scrape          # 蹭的压力（软）
ha.infeasible      # 不可行事件（带原因，绝不静默）
ha.total_cost, ha.hand_hardness
bar_hand_sequences(ha)   # {bar: {"L": [(t,key,kind)], "R": [...], "pairs": [...]}}
chart_summary(ha)        # 全谱一行，CSV 口径
```

CLI：

```
PYTHONPATH=tools python3 -m chart_analysis.hands <chart.txt|关键字> [--bars a-b] [--json]
python3 -m tools.chart_analysis.hands "336-" --bars 30-31      # 仓库根下亦可
```

---

## 7. 已知取舍、扩展点与不可靠环节

| # | 事项 | 现状 |
|---|---|---|
| 1 | **一手吃两个对象**（知识 055b 穿心·同时处理、060② 同起点·同时处理、042 狗刨） | 默认**不开**（`one_hand_two_objects = False`）。**v0.2：理由升级为用户口径 066**——「同时处理 / 狗刨这两个词语是手法的意思……不是说因为可以用这个手法就塞难打配置，写谱也不是以特殊处理的手法为导向这么写的」。手法是**玩家的解法**，不是**谱师的许可**；这不再只是"暂缓实现"，而是**规划期的口径**。只为"同刻相邻两键扫"（036）留的那个口子也随之上调到 `w_sweep_pair = 1.20`（§3.5 末），使它不能用来绕开出张 |
| 2 | **同头多 slide（`*`）** | 视作**一次连续划动**（一手）。agent 推断，存疑；不这样做，官方谱的 5 路放射会被判多押 |
| 3 | **轨道几何** | `-`/`v`/`q`/`p`/`s`/`z` 不经过途中 A 区；`^`/`>`/`<`/`qq`/`pp` 沿圈；`V` 的折点**丢失**（解析器不保留）。agent 推断，存疑 |
| 4 | 逐段起手枚举（知识 060 的"相反起手规避出张"） | **v0.2 降级为诊断工具**。知识 **067**（用户）给出的规则是"段首先出手 = 离上一段结束近的那只手"，已由 §3.4 的 T 项直接表达，**不需要**按段枚举奇偶；`force_first_hand` 保留作 A/B 诊断 |
| 5 | **重叠/汇流轨道一手兼划** | **未实现**，是残留 136 次"占用冲突"的主因（LUCIA 型：`7V53` 与 `5-3` 从中途起路径重合） |
| 6 | **Touch 与 TouchGroup**（知识 006 的最小覆盖圆 18 cm 判单双手） | **未实现**。ST 388 谱无 touch，不影响本轮；写 DX 谱前必须补 |
| 7 | **正/反三角的手级差别**（知识 020 存疑项） | 分配器给出的两手序列**看不出区别**——两者都是 3 音组两手交替。若真有差别，它在"手内顺逆"而不在"哪只手"，本模型表达不了 |
| 8 | **手的碰撞体积** | 只有"两手同时深入对侧"的粗判（`换手拧巴`），没有真实几何 |
| 9 | 代价权重 | 除 `t_hit_min` / `t_hard_*` 有官谱数据锚点外，**其余权重全是 agent 设定**，只做过"官方谱近零绝对无理 + 定式复现"两类校准，**没有人工手序标注对照** |

---

## 8. 用户对齐记录（2026-09-19）

用户 2026-09-19 就手序给了一段讲授。逐字原话入库为知识 **064 / 065 / 066 / 067**
与知识 **019 / 030** 的用户补充；重调清单见
`docs/research/hand-alignment-user-2026-09-19.md`；本表记"改了什么、改成什么、依据是谁"。

| # | v0.1 的做法 | v0.2 改成 | 依据 | 落点 |
|---|---|---|---|---|
| 1 | 分页二分（006）+ 越界深度（023），右手到 5/8 = 深度 1 | **舒适区模型**：共享区 `{1,4,5,8}` 两手 0 代价；**出张 = L→{2,3} / R→{6,7}**，单档 `w_chuzhang` | **064（用户）** | §3.1、`hands.reach_cost` |
| 2 | `w_star_switch = 0.25`（划轨手 ≠ 拍头手加代价） | **撤销（0）** | **065（用户）** | §3.2 |
| 3 | `w_slide_end_side = 0.35`（末尾半边） | **降为 0.02 的 tie-breaker** | **065（用户）**，取重调清单的保守解 | §3.2 |
| 4 | 纵连拆不拆全看速度线 83.3 ms | **长度优先**：同键 ≤3 个 tap 免速度项、免交替代价 | **019 用户补充** | §3.3 |
| 5 | 段首无特殊处理；计划做"逐段起手奇偶枚举" | **段首按位置连续**：T 项 + 关交替；枚举降级为诊断 | **067（用户）** | §3.4、§7-4 |
| 6 | "一手吃两个对象"默认关（理由：实测前不得放宽红线） | **保持关闭**，理由升级为**规划期口径**；`w_sweep_pair` 0.90 → 1.20 | **066（用户）** | §7-1 |
| 7 | 侧边双押红线 = `{2,3}/{6,7}` 且前后无共享键 | **红线 = 突然 ∧ ¬引导**；引导三型 + 两个阈值由 388 官谱标定（**两个阈值已被 v0.3 删除**，见 §9） | **030 用户补充** | §3.5 |
| 8 | 出张 = "划轨手不在 slide 末尾半边"（007） | **出张 = 064 的键位定义**（R→6/7、L→2/3）；旧口径并列输出在 `chuzhang_end_side` | **064 + 065** | `BarHand.chuzhang`、`chart_summary` |

**因为上面 8 条而被迫重标定的两个 agent 参数**（用户没给，属操作化，已在 §3 表里标注）：

- `w_same_key_extra` −0.12 → **−0.22**：舒适区下共享键两手同价，旧值会把知识 **027 定拍**段
  拆成左右交替（`141-カゲロウデイズ` Re:MAS m32 实测）；
- 新增 `w_misalign_switch = 0.45`：`w_star_switch` 归零后模型会把"头 + 错位音"给一只手、
  轨道给另一只手，绕开错位先验却不满足知识 **017** 的用户原文（同一谱例实测）。

**仍待用户裁定**（**2026-09-20 清洗后只剩一条**）：

1. **知识 064 口径下这两段算不算出张 / 反手**——`403-最終鬼畜妹・一部声` MAS m060–063（043 出张）
   与 `314-FLOWER` Re:MAS m071–072（037 反手）：它们跨的全是共享键，064 的键位定义下
   出张落点都是 0。这是**必须拿谱例问的定性问题**，主会话已把两段谱例与左右手表展示给用户。

<details>
<summary><b>2026-09-20 删掉的 7 条待裁定项（全是 agent 自造的量化问题）</b></summary>

用户 2026-09-20 的五条批评把这一整类问题判了死刑——
「怎么就分档了？」「你怎么能量化来分析这些事情呢？正常写谱谁去特意算这些事情。」
「你这问的什么问题？只要需要击打按键的不就算吗？」「根本就没有这些词吧」。
**agent 自造的阈值 / 分档 / 合成分不得作为问题抛给用户**，量化只能留在工具内部当诊断。

| 原第几条 | 内容 | 2026-09-20 的处置 |
|---|---|---|
| 1 | 出张是否分档 | **删**。出张是布尔判定 + 计数，不分档（§3.1） |
| 2 | 「稍快」的上界 | **删**。长度优先已经够用，速度上界由知识 009 的硬下界兜底，不向用户要数 |
| 3 | 「3 个以内」是否含 hold / 星星头 | **用户已答**：「只要需要击打按键的不就算吗？」→ 全算（§3.3） |
| 4 | 「附近」的量化 | **删**。067 ② 是定性写谱建议，不需要"几个键算附近"这种数 |
| 5 | 067 与 060 冲突的裁定 | **删**。两条都在原话范围内，模型按"避免出张优先"实现并注明是 agent 取法 |
| 6 | 「双押带过来」具体形态 | **删**。按"与前一组双押共享键"实现，是形状归纳不是阈值；覆盖不全由复核清单兜住 |
| 7 | 侧边双押"位移"腿的阈值 | **删**。整个阈值框架已删除（§3.5） |

</details>

---

## 9. 去量化清理记录（2026-09-20）

用户 2026-09-20 给了五条批评，本版按其中三条改代码与文档（另两条落在采音术语与
"跨共享键的交叉"谱例上，分别见 `tools/calibration/sampling.py` 与主会话在问的那两段谱例）。

| # | 用户原话 | v0.2 的做法 | **v0.3 改成** | 落点 |
|---|---|---|---|---|
| 1 | 「出张本来是一件很小的事啊，怎么就分档了？」 | 文档把 `w_chuzhang` 说成"出张只有一档"，并把"是否分档"列为待裁定 | **出张 = 布尔判定 + 计数**，文档不再提"档"；`w_chuzhang` 降格为内部代价数字；待裁定项删除 | §3.1、§8、`hands.PARAMS` |
| 2 | 「什么是侧边双押位移大的阈值……正常写谱谁去特意算这些事情。」 | 「突然」= 位移 ≥4 ∨ 间隔 ≤140 ms（388 官谱标定），红线记硬无理 | **删掉两个阈值参数**；红线只留「没有引导」；结果**只作复核清单、不记无理** | §3.5、§4、`side_double_events()` |
| 3 | 「你这问的什么问题？只要需要击打按键的不就算吗？」 | 纵连长度只数 `tap`，并把"是否含 hold/星星头"列为待裁定 | **tap / hold 头 / 星星头都算**；待裁定项删除 | §3.3、`hands._HIT_KINDS` |

**388 谱实测影响**（同一批谱、只换这三处）：

| 量 | **v0.3** | v0.2 |
|---|---:|---:|
| 不可行 / 绝对无理 / 硬无理 | **141 / 146 / 118** | 141 / 146 / 118 |
| 蹭的压力（软） | 859 | 859 |
| 出张落点（064 口径） | **5 739**（21.1 / 1000 任务） | 5 714（21.0 / 1000） |
| 出张 / slide | **0.2322** | 0.232 |
| 侧边双押总数 / 其中无引导 | **685 / 307** | 669 / 307 |
| 「突然侧边双押」硬无理 | **该判据已删除** | 0 |

**一句话**：去量化只动了"怎么说、给不给判决"，没有动"能不能打"——
三项无理计数一个没变，出张与侧边双押的计数只因纵连口径变化而微动 0.4% / 2.4%。
