# 音频分析与踩音规划（管线设计）

> **状态**：v1.5.2（2026-09-20 深夜，**第三次事故修订**）。新增 §2.4.3：
> **下拍相位（哪一拍是第 1 拍）是独立的一步**，必须单独判，且 backbeat 只能在
> 「只有军鼓/拍手」的段落上数。test-01 在这里又错了一拍。
>
> ---
>
> v1.5.1（2026-09-20 晚，**事故修订**）。v1.5 换代后当天就在 test-01 上翻了车：
> 新做法虽然把「整格错位」与「>0.4% 的 BPM 误差」两个盲点堵上了，
> 却仍然**把「无鼓段测不到」当成了「无鼓段也是同一个 BPM」**，
> 于是把一首「前奏 9 小节 195 → 减速 8 小节 → 主体 104 小节 205 → 尾奏 7 小节 221→108」
> 的曲子判成「全曲恒定 205」。
> **v1.5.1 的两条硬规矩写在 §2.4.2，请连着 §2.4.1 一起读。**
>
> ---
>
> v1.5（2026-09-20）。**只改 §2.4 的一件事：BPM 漂移检测换代**
> （实现于 `tools/audio_analysis/grid.py` 的 `tempo_map`，CLI 默认开启，`--no-tempo-map` 关闭）。
> 起因是用户对测试曲 test-01 质问「你确定 BPM 恒定吗」，复查下来
> **v1.0–v1.4 §2.4 第 5 步的漂移检测有两个结构性盲点**，见 §2.4 的「v1.5 换代」小节。
> 其余各节（§2.5 量化、§4 强度、§5 切轨）**一个数都没动**，v1.4 的全部结论仍然有效。
>
> ---
>
> v1.4（2026-09-13）的基础：用户经 AWMC-MaiArchive 再提供 **120 首**官方 ST 曲包，
> 配对标定扩到 **n = 160**（`docs/research/audio-chart-calibration-n160.md`，
> 其 §8 的 R15–R22 已按"已落地/只提建议"分类回执；n=40 的 R1–R14 也在那里逐条回执）。
> 新 120 首按**官方 `&genre` 六曲风**分层选取，曲风标签第一次是**硬标签**而不是音频侧代理。
>
> **v1.4 没有改任何默认值 —— 这是它最重要的结论**：
> ① **§4.5 的融合权重不再换**（LOSO 160 折对照现役默认 −0.0015 / 51.3% 折 / p = 0.81，
> 三条判据全不过；对照 v0.3 初值仍然全过 → **n=40 那次替换被四倍样本确认正确**）；
> ② **§5.2 的 `share_vocals ≥ 0.14` 不动**（1401 段扫出的 Youden 最优是 **0.1424**）；
> ③ **§4.7 的 `INTRO_OUTRO_CAP_RATIO = 0.75` 与 `BAR_DENSITY_FLOOR = 0.25` 确认**。
>
> **v1.4 的四处实质修订（全是下修/推翻，不是新增能力）**：
> ① **§4.5 / §9-A.5' 的"强度管线对人声曲几乎不管用"下修**——器乐/人声的 ρ 倍数
> 从 **2.73× 变成 1.49×**（器乐 0.4196 / 人声 0.2823，51 vs 109 首）；
> ② **§4.7(4) 的"按 BPM 选锚"建议撤回**——NPS 锚在 160 首上于每一个 BPM 档与定数档
> 持平或占优（全样本 MAPE 12.5% vs 15.5%）；
> ③ **§5.2 的三条"全表最高"措辞被推翻**（`other` 最强的是 `intro` 不是 `pre_chorus`；
> `interlude` 的 vocals precision 不再是全表第一；`quiet_chorus` 的 drums recall 略低于 `chorus`）；
> ④ **§5.2 规则 8（`outro` 与 `intro` 同轨）从"存疑"升级为"确认的缺陷"**（32 段仍输常数基线 9.4 pt）。
>
> **v1.4 的新证据**：**`piano` 轨是六路分离里唯一确认的净增益**
> （160 首 recall **0.289** > `other` 0.268 > `vocals` 0.197、precision 0.606、lift **2.44**）；
> **结尾形态与 388 谱语料几乎重合**（末 8 小节 65.6/1.9/32.5 vs 65.1/1.0/33.9）。
>
> **口径警示**：标定样本 **n=160**（定数 13.0–14.5、BPM **79–266**、全 ST MASTER），
> 切轨侧的 ground truth 仍是**代理判据**，而且它的失败模式在 n=160 上更严重
> （器乐曲《疾走あんさんぶる》的 vocals 轨泄漏拿到 recall 0.579 = 160 首最高）。
> ⚠️ **v1.3 的相关系数在 v0.3 权重下算、v1.4 在 v0.4 下算，跨版本比较必须用"同权重口径"**
> （见 n=160 报告的口径警示）。
> 凡与 388 谱全量统计（知识 031）冲突处，**以 388 谱为准**。
>
> v1.3（2026-09-11）的基础：用户再提供 **32 首**官方 ST 曲包，配对标定扩到 **n = 40**
> （`docs/research/audio-chart-calibration-n40.md`，其 §8 的 R1–R14 已按"已落地/只提建议"分类回执）。
> **v1.3 的四处实质变更**：
> ① **§4.5 换掉了强度融合的默认权重**——这是本项目第一次用数据替换手写默认值
> （LOSO 40 折 +0.076、32/40 折胜出、符号检验 p=0.0002，三条判据同时通过）；
> ② **§4.7 的两个 n=1–8 阈值被修正**（段落 floor 的 n=8 最优 0.125 是噪声；intro/outro 封顶 0.60 → 0.75）；
> ③ **§5.2 的人声主导判据换了特征**（`voiced_ratio` 被证明连"这首是不是人声曲"都分不开）；
> ④ **§4.8(E) 的核心数字全部下修**（逐小节 ρ 0.454 → 0.226，换权重后 0.385；逐段 0.523 → 0.320）。
> **v1.3 最重要的新认知**：**强度×密度的相关性按曲目类型强烈分层——器乐曲 ρ=0.399、人声曲只有 0.146**。
> **口径警示**：标定样本 **n=40**（定数 13.0–14.5、BPM 100–260、全 ST MASTER），
> 且切轨侧的 ground truth 仍是**代理判据**（官方 note 是否落在某 stem 的 librosa onset ±30 ms 内），
> 本轮还找到了它的一个明确失败模式（器乐曲的 vocals 轨泄漏被算成"踩了人声"）。
> 凡与 388 谱全量统计（知识 031）冲突处，**以 388 谱为准**。
>
> v1.2（2026-09-11）的基础：合并**第一份配对标定证据**
> `docs/research/audio-chart-calibration.md`（8 首官方 mp3 × 官方谱，其 §8 的 R1–R11 逐条回执）
> + 主会话对该报告的验收裁定。改动集中在 §2.4（offset 反拍 bug）、§4.5–§4.8（权重/高潮/
> 密度锚/标定协议 (E) 项）、§5.2–§5.3（切轨模型换成**骨架 + 点缀**）、§9、§11。
>
> v1.1（2026-09-11）的基础：在 v1.0（2026-09-10，四层工具选型）基础上合并三份新证据：
> ① `docs/research/audio-analysis-research-v2.md`（第二轮调研，落地问题攻关，其 §8 的 R1–R24 逐条采纳）；
> ② `docs/research/official-chart-density-curves.md`（388 个官方 ST 谱逐小节密度实测）；
> ③ `.agent/notes/035-音频分析原型实现.md` + `tools/audio_analysis/README.md`（原型 v0.1 在 3 首真实曲目上的实跑记录）。
> **冲突仲裁原则**：**调研预期与原型实测冲突时，一律以实测为准**，并在原处标注。
> **目标**：攻克"让 AI 通过分析 mp3 了解哪里是副歌、哪里是高潮、要采鼓点还是人声"——对应 MMFC 教程第五章的强度/踩音/配置理论（知识库 001/002/003）。
> **标注约定**：✅ 一手核实（源码/LICENSE/API/论文原文）｜🧪 本项目实测（原型 v0.1 或 388 谱统计）｜⚠️ 部分核实（二手口径）｜❓ 未确证，待实测。

## 0. 设计原则（与项目约定一致）

1. **LLM 不"听"原始音频**：模型上下文有限且不可靠（见 AGENT.md 准则 8），谱面决策一律基于**结构化特征**（分段标注、onset 流、能量曲线），而非把音频或长特征喂给 LLM——特征提取用确定性 DSP/深度学习模型完成，LLM 只做规划与决策。
2. **用户提供的数据优先**：BPM、offset、变速点可由用户直接提供，管线把这些作为**最高优先级输入**；自动检测只做**一致性校验并如实报告差值，绝不覆盖用户值**（🧪 原型已按此实现）。
3. **可校验**：每个环节的输出都要能被人复核（分段/强度曲线出 `plot.png`），踩音计划必须先通过知识库 001-003 的规则检查。
4. **实测优先于设计预期**（v1.1 新增）：本文档中凡标 🧪 的结论来自真实跑通的实验，优先级高于任何调研推测；启发式环节必须在输出里显式标"存疑"，不得让下游把启发式当事实。

## 1. 管线总览（四层）

```
mp3 ──► L1 节拍层 ──► L2 音轨层 ──► L3 结构层 ──► L4 规划层 ──► song sheet 双格式 ──► LLM 创作
        BPM/beats/     stems 分离+     段落/副歌/       结构×强度×音轨→    song_sheet.md
        downbeats      分音网格量化     强度曲线          逐段踩音计划       song_analysis.json
        +offset校验
```

| 层 | 输入 | 输出 | 对应谱师动作 |
|----|------|------|-------------|
| L1 节拍层 | mp3（+用户提供的 BPM/offset/变速点） | 统一 WAV、网格 `t(bar,div,idx)`、offset 反向校验报告 | "确认 BPM、&offset、有没有变速段" |
| §2.5 量化 | L1 网格 + L2 onset | 逐小节 `{x}` 分音判定 + 网格串 | "这一小节是 8 分还是 16 分" |
| L2 音轨层 | 统一 WAV | 4 stems + 各 stem onset 流 + **逐小节特征清单** | "要采什么音：鼓点还是人声"的**候选池** |
| L3 结构层 | WAV + L1 + L2 | 段落边界与类型（日式/英文双标签）、重复段、逐小节强度曲线、高潮定位 | "哪里是副歌、哪里是高潮" |
| L4 规划层 | L1+L2+L3 + 知识库 001/002/003/031 | song sheet（逐段：采音目标、密度、休息安排 + 逐小节网格串） | "按情绪写配置"的蓝图 |

🧪 **整条冷跑 RTF ≈ 0.29**（M4 / 16 GB / macOS 26 / py3.12 / torch 2.14；2 分钟曲约 36 s）；热跑（复用 stems）RTF 0.14–0.15。Demucs + all-in-one 两个深度模型吃掉 >90% 时间，全部 DSP 环节合计不到 2 秒。

## 2. L1 节拍层：时间轴对齐

### 2.1 Step 0 — 统一解码（硬性前置，R2）

**一律 `ffmpeg -i in.mp3 out.wav`（44.1 kHz PCM），全管线只读这一个 WAV。**

- 依据 ✅：all-in-one-infer README「Concerning MP3 Files」实测——**不同 MP3 解码器有约 20–40 ms 的偏移差异**，而 beat tracking 的常规容差只有 70 ms；osu! 官方 timing 指南同样写明"任何音频编辑（包括重新编码）都会改变 timing"。
- 🧪 实测佐证：三首测试曲的 MP3 容器 `start_time` 全部为 **23–25 ms**（ffprobe），正是上述编码器延迟。该值已写入分析单供人工判断。
- 附带收益 ✅：`torchaudio ≥ 2.11` 已不再自带 mp3 解码（需另装 `torchcodec`），统一转 WAV 一次性绕开全部解码器差异与依赖问题。

### 2.2 用户覆盖路径（主线）

用户提供 BPM + offset（`&first`，秒，可为负）+ 变速点 → 直接构造 `t(bar, div, idx)`；变速按**分段线性**（每段常 BPM）处理。参考实现范式：DDC 的 `BeatCalc`（分段 bps 线性积分，`beat_to_time` / `time_to_beat` 双向）✅ MIT，是最简洁的可抄结构。

🧪 原型限制：网格只在**小节边界**换 BPM；若原曲变速点不落在小节线上，其后所有小节时间都会错位（用了变速参数会在 `warnings` 里报警）。变速支持仍是实验性的。

### 2.3 自动路径（缺省 / 交叉验证）

- **主线 = beat_this**（MIT ✅）：beat+downbeat 一次输出、帧级置信度、无 DBN、对变速宽容（Ballroom beat F1 97.5 / downbeat 95.3）。
  - ⚠️ **MPS 用法（R3）**：CLI 只暴露 `--gpu`（CUDA 语义），Apple Silicon 上必须走 Python API：`File2Beats(checkpoint_path="final0", device="mps", dbn=False)` ✅。
- **打底 = librosa**（ISC ✅）；**备选 = all-in-one-infer**（beat/downbeat 与 L3 同源）。
- **排除**：madmom（模型权重 CC BY-NC-SA + 2018 年 Cython 包，py3.12 装不上 ✅）、essentia/tempo-cnn（AGPL ✅）。
- BPM 上下限显式设置：目标 160–220，默认 max_bpm 会在 220 档折叠成 110。
- ⚠️ librosa 1.0 已移除 `librosa.beat.tempo`；`librosa.beat.plp` 仍在（`__all__=["beat_track","plp"]` ✅）。

### 2.4 offset 反向校验（v1.1 新增，R1）

用户给的 offset 错 20 ms 会让全谱皆错，而校验成本极低（🧪 实测 0.85–1.1 s / 曲，RTF ≈ 0.008）。这是**只报告、不覆盖**的环节。

**算法（网格相位搜索）**

```
输入：用户 BPM/offset、统一解码后的 WAV
1) 在 drums stem（或全曲）上做 onset 检测，hop 必须细：sr=44100, hop=256 → 5.8 ms/帧
   （强度曲线用的 hop=512@22050 = 23 ms 太粗，量级与待测偏移相当，会把答案磨平）
2) 取用户网格上的 {8} 点集合 {t_k}（{8} 比 {16} 鲁棒；也可只用 downbeat）
3) 对 φ ∈ [−0.4·beat, +0.4·beat]，步长 ~1–5 ms：S(φ) = Σ_k O(t_k + φ)，φ* = argmax S(φ)
   ⚠️ **搜索半径不得 ≥ 半拍**（v1.2 修订，见下方"反拍 bug"）
4) 判据：|φ*| ≤ 10 ms → 可信直接用；10 < |φ*| ≤ 30 ms → 提示"疑似解码偏移"，
   记录但仍以用户值为准（准则：用户优先）；|φ*| > 30 ms **且** φ* 得分 ≥ 1.15 × φ=0 的得分
   → 报警，交人工确认；显著性不足或落在反拍上 → 判**不可判定**，不报警
5) BPM 漂移检测：**v1.5 起改走 `grid.tempo_map`**（无相位滑窗全域扫描 + 主体段
   高精度拟合 + 前后半一致性 + 8 分量程相位曲线），见下方「v1.5 换代」。
   ~~旧法：全曲按每 16 小节分块逐块求 φ_i，对 (t_i, φ_i) 线性回归，
   斜率 a ≠ 0 表示网格在漂 → BPM_true ≈ BPM_user × (1 − a)；|a| < 2e-4 视为无偏~~
6) 独立第二意见：librosa.beat.plp(prior=围绕用户 BPM 的窄正态) 的峰位相位，与 φ* 对照
```

**🧪 实测修正（三条，均以实测为准，覆盖 v2 §1.2 的原始设计）**

1. **打分量必须用 onset 时间，不能用 onset 强度包络**。原设计的"整曲 onset 强度包络 beat-synchronous 打分"在三首曲子上一律报 +36~+40 ms；在**合成 click 音频**（真值 `first=0.5`）上实测，包络法给 0.5107（**+10.7 ms 系统性滞后**，来自 librosa `onset_strength` 的帧间谱流滞后），而 backtrack 后的 onset 时间只差 −4.9 ms。→ 改用 **onset-fit**（backtrack 后的 onset 时间对拍线的加权命中率）作主打分，包络法结果作为 `envelope_delta_ms` 一并输出。
   反向验证：用包络法的 +40 ms 重建网格后，量化平均误差从 9.47 ms **劣化**到 13.46 ms、分音直方图从"8 分为主"塌成"24/32 分为主"——证明用户给的值才是对的，+40 ms 是伪信号。
2. **offset 只能定到一拍以内**。整拍平移的分数几乎相同，取全局最大值会把结论错移整整一拍（🧪 TransientTears 曾被报成 −272 ms）。→ 必须把同级峰**分组取峰**，选离用户值最近的那个，并把全部候选列进 `peaks`。
3. **三首实测差值 +8.0 / +10.6 / +13.7 ms（z = 3.0–3.5）**，全部落在"可信"或"疑似解码偏移"档，与 MP3 容器 `start_time` 23–25 ms 同量级。

**🧪 v1.2 修订：±半拍「反拍」假警报是一个真 bug（R1/R2，标定报告 §1.2）**

8 首官方曲包（`&first = 0` 是 ground truth）上，v0.2 的实现在 **2 首**上报出 ±半拍的假警报：

| 曲目 | BPM | 半拍 | v0.2 报的 φ* | 反证（用官方谱反查音频的最佳平移） |
|---|---:|---:|---:|---:|
| Signature | 128 | 234.4 ms | **−229.0 ms** | φ* = −17.5 ms |
| 麒麟 | 220 | 136.4 ms | **−129.8 ms** | φ* = +7.5 ms |

- **根因**：onset-fit 的打分函数以**一拍为周期**，而搜索半径设成了 ±1 拍 —— 窗口里除真值峰外
  还含 φ*±半拍的「反拍」峰。鼓点规整（正反拍等强的 8 分 hi-hat）时反拍与正拍并列，
  只要反拍略高就会被选中，真值 φ=0 附近**不是局部极大**。
- **修法（三条同时，已实现于 `grid.check_offset`）**：
  ① 搜索半径收到 **±0.4 拍**（硬上限 0.45 拍），反拍进不了窗口；
  ② 同级峰仍取**离用户值最近**的那个（v0.2 已有，保留）；
  ③ |φ*| 逼近半拍（≥0.35 拍）或出现关于用户值**对称的并列峰** → 判「**反拍歧义 / 不可判定**」，
     **不报警**；|φ*| > 30 ms 还要过 `ratio ≥ 1.15` 的显著性门才允许报警。
- **🧪 修复验证**（11 首，只重跑 offset 步骤）：Signature −229.0 → **+0.6 ms**、
  麒麟 −129.8 → **+10.6 ms**，其余 9 首变化 ≤ 0.4 ms。
- **R2 判词修订**：`15 < |φ*| ≤ 30 ms` 的措辞改为**明确指向 MP3 解码/容器延迟**
  （容器 `start_time` 常见 23 ms），不再说"谱面可能错"。Mare Maris 的 **+27.9 ms 仍被报出**
  （ratio 15.4 ≫ 1.15），但归因正确——官方谱反查也给 +20 ms，同向同量级。
- **代价**：真实偏移**超过 0.4 拍**的曲子测不出来。在"BPM/offset 由用户提供"的前提下可接受。

**方法论旁证** ✅：Osu2MIR（ISMIR 2025 LBD）反向工程 osu! 的未继承 timing point 当 beat/downbeat ground truth，并发现"单个 timing point、或多点间隔 ≥ 5 s 的图标注可靠"——说明**人工确定的 BPM+offset（恒速）可以当 ground truth**，本项目"让用户提供 BPM/offset"的路线在方法论上站得住。

### 2.4.1 v1.5 换代：漂移检测为什么必须重做（🧪 test-01 实测）

**旧法（v1.0–v1.4 第 5 步）**：全曲按 8/16 小节分块，在 **32 分网格**
（205 BPM 下槽长 36.6 ms、搜索半径 ±17 ms）上逐块求相位 φ_i，再对 (t_i, φ_i) 线性回归。

**两个结构性盲点**（不是参数没调好，是方法本身看不见）：

1. **对「整格错位」完全不敏感**。打分函数以**一格为周期**，所以半拍（146 ms）、
   16 分（73 ms）、32 分（36.6 ms）的整体位移在 32 分网格上**全部混叠成 0**。
   → `&first` 整体错半拍、谱面中途插入一个 16 分的接缝、某段乐句错一格，
   旧法的读数一律是「完美对齐」。🧪 合成信号实测：把 onset 整体挪半拍，
   旧法给出的逐块相位与不挪时的**中位差 < 5 ms**，两者分不开。
2. **测不出 >0.4% 的 BPM 误差**。一块 8 小节 ≈ 9.4 s，BPM 差 0.5% 就让相位在块内
   绕过一整格，回归到的只是绕圈后的残值。🧪 合成信号实测（真值比用户值高 0.5%）：
   真实相对误差 5e−3，旧法回归斜率 **< 1e−3**（低估 ≥5 倍），反推的 BPM 也是错的，
   结论会是「无漂移」。🧪 test-01 里「bars 73–80 +13.8 ms / bars 105–112 +15.0 ms
   偏大」就是绕圈的痕迹，换成 8 分量程后这两块是平的（+8.6…+10.3 / +9.9…+12.9 ms）。

**新法（`grid.tempo_map`，v1.5）**：

| 环节 | 做法 | 关键性质 |
|---|---|---|
| BPM 主判据 | `window_bpm_scan`：2 与 4 小节滑窗、步长 1 小节，每窗在 **100–260 全域**上扫（粗 0.25 → 围绕粗峰细 0.02），**得分对相位取最大** | **与相位无关**，不存在绕圈问题；半/倍速也能看见 |
| BPM 精度 | `fine_bpm_fit`：主体段一次高精度拟合，回报 95% 峰宽 | 🧪 test-01：**205.000，峰宽 204.98–205.02** |
| 稳定性 | `half_split_consistency`：主体段一刀两半各自独立拟合（围绕**实测**而非用户 BPM） | 任何缓慢漂移都会让两半分开 |
| 相位 | `bar_phase_curve`：逐小节加权圆均值，**量程 ±半拍**（205 BPM 下 ±73 ms） | 能看见 ≥1 个 16 分的离散跳格 |
| 接缝 | `detect_phase_jumps`：比较接缝前后各 3 小节的相位**中位台阶**（不是相邻差） | 单小节抖动不误报（test-01 的 bar43→44 46 ms 单点跳就是噪声） |
| 半拍归属 | `half_beat_attribution` + `cleanest_drum_bars`：分带 flux（kick/hat 投票，bass 只作反证），**只在 kick 接近 4-on-the-floor 的小节上判** | 🧪 test-01：全曲平均正/反比 **1.03（分不开）**，按干净小节 **2.03（判定明确）** |
| 无鼓段 | 一律标 `no_drum_bars`，**不做 onset 验证**，按主体网格外推 | 见下方「作废声明」 |
| 旁证 | `beat_this`（可选，装了才跑）与 `librosa`（**必须带 hop 帧量化告警**） | 两者都**不得单独作结论** |

判「恒定」要**四条同时成立**：逐窗命中率 ≥0.90、主体拟合落在用户 BPM ±0.3、
前后半差 ≤0.05、相位无跳格（外加斜率仍过 2e−4 的老门槛）。
「曲子稳但不等于用户 BPM」单独成一档 `constant_other_bpm`，并直接报出
**到曲末的累计偏离毫秒数**（🧪 test-01 上谎报 +0.5% → 报 795 ms）。

**⚠️ 作废声明：「无鼓段单独测相位 → 确认全曲同一网格」这条验证不再有效。**
🧪 test-01 实测：全混 onset 列表在混响 pad / 低通渐弱段上**比随机还差**
（到最近 16 分线的 |中位| 19.1 ms，随机基线 9.1 ms；落 ±12 ms 只有 33%，随机 65.6%）——
一个 pad 音符会触发 5–10 次包络涟漪。换成带内 flux 重测，前奏 22 s 只有 27 个真 attack，
onset-fit 曲线前五名峰差不到 8%，**等于没有分辨力**。
这类段落一律报 `unmeasurable`，结论靠结构外推（主体网格外推回去落在乐曲硬切入点）。

**另一处连带修复**：`band_flux` 必须有**动态范围地板**（默认 40 dB）。
🧪 实测一个 8 kHz 的 hat 泄漏到 35–120 Hz 只有 −0.9 dB（比 kick 低 46 dB），
但该带底噪是 −81 dB，**dB 上涨了 63.6 dB**，于是反拍上凭空多出一排「kick」。
真实曲子的 breakdown / 渐弱段同理会造出幻影瞬态。

### 2.4.2 v1.5.1 事故修订：**无鼓段不得外推为恒速，只能报「不可测」**

🧪 test-01 的翻车过程（完整记录在 `.agent/notes/067`）：

1. v1.5 的 `tempo_map` 正确地把 46/138 小节标成 `no_drum_bars`（前奏 1–20、
   breakdown 77–84、尾奏 125–137），也正确地在**可测的 104 小节**上测出 205.000；
2. 但 verdict 直接给了 **`constant`** ——等于宣称那 46 小节也是 205；
3. 后来用户拿出另一份抄谱的 BPM 表：**前奏 9 小节 195、之后 8 小节一路减速到 125 再回 155、
   尾奏 221→197→190→165→175→135→108**。回头在同一份音频上复测，
   前奏 195 段**独立细扫就给 194.96、得分是 @205 的 1.9 倍**——证据一直都在，
   是「外推为恒速」这个默认动作把它盖掉了。

**两条硬规矩（已实现）**：

1. **可测小节占比低于 `TEMPO_MEASURED_RATIO_GATE`（默认 0.90）时，禁止报 `constant`**，
   改报 **`constant_in_measured`**，并在结论里**逐段列出不可测区间**，
   明写「这些小节的速度既没测出来、也不得外推为同一 BPM」。
   🧪 test-01 现在报：可测 66%，不可测 1–20 / 28 / 52 / 77–84 / 101–103 / 125–137。
2. **无鼓段的速度只能从别处拿**（抄谱、官方 BPM 表、人工听），
   拿到之后用 **`test_tempo_hypothesis`** 在自己的音频上逐段检验，
   每段落到四档之一：`measured`（自己扫出来且与候选一致）/ `confirmed`（候选网格
   明显优于恒速外推）/ **`adopted`（音频无法分辨，采用候选值）** / `contradicted`（候选被反证）。
   `adopted` 必须原样写进交付物，**不得升格成实测结论**。

**`test_tempo_hypothesis` 的「可判据」三条**（不满足就一律 `adopted`，这是 v1.5.1 的另一处教训——
不加这三条，test-01 的 17 段里有 9 段会被误判成 `contradicted`）：
① 段长 ≥ 4 s（**1 小节 ≈ 1.2 s 在物理上就分不开 185 与 205**）；
② 段内 onset ≥ 80；③ 两套网格里较好的那个得分 ≥ 0.35（都很低 = 这段的 onset 本身是噪声）。

🧪 test-01 的检验结果（候选 = 抄谱表 17 段）：
**实测 1（195×9 小节）、网格占优 1（205×104 小节）、分不开采用候选值 15、被反证 0**；
全曲 8 分格得分比「恒定 205」高 **+160%**；beat_this 拍点到候选拍线的中位距 49 ms（恒速 56 ms）。

**还有一条方法论教训**：v1.5 当时测过前奏，得到 194.66 / 194.85 / 194.97 三个读数，
却因为「把整个前奏（含减速段与 125 段）当一整段扫」而看到一条平坦的曲线，判成「没有分辨力」。
**正确做法是先按候选 map 切段再逐段扫**——按 9 小节的 195 段单独扫，得分比是 1.9×，一点都不含糊。
即：**不可测区间的边界本身也要先假设、再检验，不能拿一整段无结构的区间去扫。**

### 2.4.3 v1.5.2：**下拍相位**是独立的一步（`grid.downbeat_phase`）

拍网格定死之后，「**哪一拍是第 1 拍**」仍然未定：四个候选相差整数拍。
选错**不影响 note 的绝对时刻**，但整谱的强拍、小节线、乐句、配置起手全部偏一拍。
🧪 test-01 第三次翻车就在这里：我们把乐曲硬切入当成 bar1 beat1（三拍弱起），
官谱把它当成 **bar2 beat1**（前面一整小节空拍），整整差一拍。

**判据与坑**：

| 判据 | 能定到 | 坑 |
|---|---|---|
| **军鼓/拍手 backbeat**（落第 2、4 拍） | **mod 2** | ⚠️ **必须挑只有军鼓/拍手、没有 4-on-the-floor kick 的段落**。🧪 test-01 全曲平均的 `(2&4)/(1&3)` 正好是 **1.00**（抹平），在「薄 build」（抽掉 sub 与 kick、只剩 clap）那 14 s 上是 **1.46 对 0.68**。另外要**数事件**不要**平均能量**（1.46 对 1.12） |
| 下拍跟踪器（beat_this） | mod 4 | 输出不规整，要看「到各候选小节线的中位距」而不是绝对命中 |
| 和声（chroma）变化点 | mod 4 | n 小 |
| 段落进入点 | mod 4 | ⚠️ 用中位差找跃变时**平滑窗往往比一拍还长**（0.35 s 窗 vs 0.293 s 拍），🧪 精确化后 P0 与 P1 只差 19 ms，**这条分辨不到一拍** |
| **一段音乐里第一记 kick 的起振点** | mod 4 | 🧪 最硬的一条，也最容易被忽略：逐 20 ms 打印原始能量轨迹即可。test-01 的 205 段是 riser 收在 24.470 的一记 kick 上（P0 的线 24.7636 处 kick 带是 0.00） |
| 小节折叠 flux 剖面「slot0 最强」 | — | ⚠️ **不可用**：被 4-on-the-floor 与层叠抹平，🧪 在 test-01 上指向错误答案 |

**几路全分不开时**：**不要替人做主，也不要拿别人的谱面兜底**——
别人的 `&first` 未必对准过他自己的音频（🧪 test-01 的参考谱就是这种情况，
用户指出它的背景音乐与我们的并没有对齐），那只是一个**候选假设**。
正确做法是**把两种候选并列写出**（各自的证据强弱 + 音乐上的合理性），交人裁定。

**方法论共性**（与 §2.4.2 同一条）：**先按结构切段，再在对的段落上测**。
连着三次翻车（漂移 / 变速 / 下拍相位）都是同一个病根：
在一整段无结构或被污染的区间上做统计，看到平坦曲线就判「分不开」。

## 2.5 onset → 分音网格量化规则（独立节，R4）

### 2.5.1 前置事实与信息论天花板

- simai 时间模型：`{x}` 表示"一个逗号 = x 分音符"，**槽长 = 240 / BPM / x 秒**（`docs/simai-syntax.md` §1.4/§3.2）。
- 目标 BPM 区间的槽长（ms）：

| BPM | {4} | {8} | {12} | {16} | {24} | {32} | {48} |
|-----|-----|-----|------|------|------|------|------|
| 160 | 375.0 | 187.5 | 125.0 | 93.8 | 62.5 | 46.9 | 31.2 |
| 180 | 333.3 | 166.7 | 111.1 | 83.3 | 55.6 | 41.7 | 27.8 |
| 200 | 300.0 | 150.0 | 100.0 | 75.0 | 50.0 | 37.5 | 25.0 |
| 220 | 272.7 | 136.4 | 90.9 | 68.2 | 45.5 | 34.1 | 22.7 |

- 行业标准容差 ✅：`mir_eval` onset F-measure 窗 = **±50 ms**、beat F-measure 阈 = **70 ms**。
- **关键推论**：BPM 180 的 `{32}` 槽长 41.7 ms、BPM 220 的 34.1 ms，**都小于通用 onset 检测的标准容差窗**——"自动把 onset 量化到 {32}"在本项目的目标 BPM 区间**在信息论上就站不住**。`{24}`（45–62 ms）同样在噪声边缘。

### 2.5.2 量化步骤

**Step 1 拍同步重采样**：不要"固定 hop 提特征再找最近格子"，而是把 onset 包络按拍重采样，**每拍 N=48 个采样点**（= 每小节 192 点，同时整除二分族与三分族，恰好等于 simai 的 `{192}`）。依据 ✅：Yi 2023（arXiv 2311.13687）的 beat-aligned 频谱就是 hop = 1/48 拍。

**Step 2 逐小节推断最小分音**（奥卡姆 + 知识 005「采音要简」）：

```
候选族：二分族 D2 = [4, 8, 16, 32]；三分族 D3 = [12, 24]
容差：τ(d) = clamp(0.25 × slot_ms(d), 12 ms, 30 ms)
从粗到细扫描 d ∈ [4, 8, 12, 16, 24, 32]：
  r(d) = max_i min_k |p_i − k/d| × bar_ms        # 最大残差
  第一个满足 r(d) ≤ τ(d) 的 d 即为该小节的 {x}
```

**Step 3 三连 / 二分判别**：分别用 d=16 与 d=12 拟合，判为三连需**同时**满足 `rms12 ≤ τ(12)` 且 `rms12 ≤ 0.7 × rms16`（音游曲绝大多数是二分体系，三连需显著证据才能推翻默认）。

**Step 4 swing 判别（独立于三连）**：取反拍 onset 算 `s = median((p_i − p_onbeat)/(1/8))`；`s ≈ 0.5` 直、`s ≈ 0.667` 三连 swing → 改用 `{12}`/`{24}`。⚠️ swing ratio 随 tempo 升高近似线性下降（Dittmar/Müller, ISMIR 2015），160–220 BPM 下出现概率低 → **默认关闭**，仅在 `rms12 << rms16` 且 s 稳定落在 0.6–0.72 时启用。

**Step 5 `{32}` 红线**：**默认禁止自动量化产出 `{32}` 及更细。** 唯一例外需三条同时成立：(a) 该小节 onset 数 ≥ 6；(b) 来源是 **drums stem**（瞬态最锐）；(c) `{32}` 下 RMS 残差 ≤ 15 ms。
制谱侧理由同向：`{32}` 在 ST 谱里属装饰性 burst，应由 LLM 按**配置需要**主动使用（知识 003/015），而不是由量化算法"检测"出来。

**Step 6 输出**：每个 onset → `{bar, div, idx, t_ms, residual_ms, stem, strength, confidence}`；每小节 → `{bar, div, n_onset_per_stem, fit_rms_ms}`；网格串 `x...x..x....x...`（`.` 空 / `x` onset / `X` 强 onset）。

### 2.5.3 🧪 实测：这是全管线最不可靠的环节

- 量化误差（原型 v0.1，drums 轨）：平均 6.78 / 7.35 / 9.47 ms，最大 24.9–25.1 ms，"无法解释的小节" 全曲为 0。
- **但 `unresolved_bars = 0` 不等于量化准确**：原型的容差取 `max(25 ms, 1/64 小节)`，**192 BPM 下一小节 1.25 s，24 分与 32 分格子只差 ~13 ms，远小于容差**，于是"最小可解释分音"在 16/24/32 之间**基本是随机挑**。
- → **高 BPM 曲目请把网格串当"节奏轮廓"，不要当谱面分音的依据。** 这与 §2.5.1 的信息论推论完全一致，是本节最需要记住的一条。
- ❓ 待办：容差三参数（0.25 / 12 / 30）与三连阈 0.7 需在 osu2beat2025 子集上标定（见 §10）。

### 2.5.4 成熟做法的取舍

| 来源 | 实际做法 | 对我们的价值 |
|------|----------|--------------|
| **DDC** 推理端 | **根本不做音乐量化**（R22 事实更正）：写死 `_SUBDIV=192, _DT=0.01, _BPM=125`，即**伪造一个 BPM 让 1/192 小节恰好等于 10 ms 帧**，输出时间均匀网格的 .sm ✅ | ❌ 不是 snap 逻辑的参照 |
| **DDC** `beatcalc.py`/`abstime.py` | 分段 BPM/stop ↔ 时间双向换算 | ✅ 时间轴换算的干净范式，可直接照抄结构 |
| **osu! 编辑器** | beat snap divisor 11 档 1/1…1/16，含 1/3、1/6；timing 靠人工敲 T 键 ⚠️ | ✅ 佐证"候选集要同时含二分族与三分族"；❌ 无算法可抄 |
| **MIDI 量化文献** | 规则 → metrical HMM → Transformer（给定 beat 标注量化，ASAP onset F1 97.3%）⚠️ | ✅ 思路可借：网格语法合法性应作硬约束（simai 的 384 约数就是）；❌ 我们的输入是机械制作电子乐 + 已知恒定网格，规则 + 残差判据足够，上 HMM/Transformer 是过度设计 |
| **Yi 2023** | hop = 1/48 拍 beat-aligned 频谱，osu!mania 14648 谱，micro-F1 84.6% ✅ | ✅ 拍同步重采样直接采纳 |

## 3. L2 音轨层：踩音候选池

### 3.1 声源分离

- **主线 = Demucs v4**（MIT ✅ 代码+权重）——drums/bass/vocals/other 四路，鼓 SDR 10.08 泄漏少。
  - 🧪 **实测：htdemucs 在 MPS 上 RTF ≈ 0.10–0.11**（12.6–13.8 s / 2 分钟曲，未回退 CPU，首次含权重下载 40 s）。迭代期用 `htdemucs`，量产期再上 `htdemucs_ft`（慢 4 倍、质量更好）。
  - ⚠️ Demucs `separate.py` 在 CUDA 不可用时会自动选 `mps` ✅；历史上 HTDemucs 在 MPS 有复数张量兼容问题，且 macOS 26.x 上 `torch.backends.mps.is_available()` 有误报 issue（pytorch#177819）→ 实现必须带 **MPS 失败自动回退 CPU** 并记录实际设备（🧪 原型已实现）。加速备选：`demucs-mlx` / `mlx-audio-separator`（MIT ✅）。
  - **排除**：Spleeter（质量垫底）、BS-RoFormer（权重许可不明）、AudioSep（过度设计）、Open-Unmix umxl（权重 CC BY-NC-SA）。
- 内存：M4/16 GB 下建议**分阶段落盘**（分离 → 写 stem wav → 释放 → 再分析），不要四个 stem 全程驻留。

### 3.2 逐小节特征清单（v1.1 新增，R5）

L2 必须为每个小节 b 输出下列特征——它们是 L4 切轨规则表的全部输入：

| 特征 | 计算 | 用途 |
|---|---|---|
| `share_s(b)` | stem s 在小节 b 的能量占比 `E_s / ΣE` | "哪个响" |
| `n_onset_s(b)` | stem s 的 onset 数 | 可踩音数 |
| `grid_fit_s(b)` | stem s 的 onset 落在 `{8}` 网格上的比例 | 律动规整度；低 = 采样/人声长音，不适合当骨架 |
| `voiced_ratio(b)` | vocal stem 有声帧占比 | 人声进出——**切轨最强信号** |
| `vocal_notes(b)` | vocal stem 的 note 数 / 音高中位 / 最高 | 区分"人声长音"（verse）与"密集人声"（chorus） |
| `kick/snare/hihat(b)` | drums stem 三带 onset 数（§3.3） | 段落类型判别 + 骨架 |
| `bass_onsets(b)` | bass stem 的 note onset | 低频律动骨架；kick 不明显时替代 |
| `riff_sim(b, b−4/−8)` | other stem 的 chroma/CQT 小节向量余弦相似 | hook/riff 循环检测与变化点 |
| `sil_run(b)` | 最长静默长度 | 休息段 / 留白识别 |

### 3.3 鼓件三频带启发式（v1.1 新增，R6）

**一手许可证核实：ADT 预训练模型全线非商用，默认管线不能用。**

| 工具 | 许可 | 结论 |
|---|---|---|
| **ADTOF**（5 类鼓件） | **CC BY-NC-SA 4.0** ✅（LICENSE 原文） | ❌ 违反 §7.2 红线 → 排除，仅可做个人非商用插件 |
| **LarsNet**（鼓件分离 5 路） | 仓库 `license: None` ✅，README 称权重 CC BY-NC 4.0 ⚠️ | ❌ 排除 |
| **omnizart**（drum） | 代码 MIT ✅，但 `requires_dist` 含 `madmom>=0.16.1` ✅ | ❌ 排除（许可连坐 + py3.12 装不上，双杀） |
| **drumsep**（Demucs 微调） | 代码 MIT ✅，权重许可未明示 ❓ | ⚠️ 可选插件，用前需核 |

**→ 默认方案：无模型的频带启发式**（在 Demucs `drums` stem 上做，泄漏已被分离器处理掉）：

```
LOW  : 30–120 Hz         （kick 基频，峰值多在 50–80 Hz）
MID  : 120–400 Hz + 1.5–8 kHz 宽带噪声（snare：低频体 + 响弦噪声）
HIGH : 6–16 kHz          （hihat / cymbal）
判据：LOW 占比最大且 HIGH < 0.2 → kick；MID 宽带噪声显著（谱平坦度高）→ snare；
     HIGH 占比最大且衰减快(<80 ms) → closed hihat；衰减慢(>300 ms) → cymbal/crash
```

**🧪 实测强制修正（必须遵守）**：**不能各频带独立检 onset**——原型第一版这么做，结果 kick(715) 比整条 drums 轨(626) 的 onset 还多。正确做法是**先在 drums stem 上检出 onset，再按频带相对能量给每个 onset 贴标签**（一个 onset 可带多个标签），结果必然是 drums onset 的子集（实测 469/382/519）。

⚠️ 可靠性定位：这套的价值**不在转录精度，而在段落判别**——kick 消失 + hihat 密度翻倍 = 典型 build；crash 落在小节 1 拍 = 段落起点。🧪 实测混淆很多：底鼓 vs 低音 tom、军鼓 vs 拍手/边击、hihat vs 镲片与人声齿音泄漏都会混；**密集段里 kick 与 hihat 的网格串常常完全一样**。❓ 在 160–220 BPM 高密度电子鼓组上的可分性需 A/B 实测。

### 3.4 人声活动检测 VAD（v1.1 新增，R7）

- **不要用语音 VAD**：silero-vad（MIT ✅）与 pyannote（MIT ✅）都是**语音域**模型，对歌唱（长音、颤音、和声、声码器）表现未验证 ❓，且 Demucs 的 vocal stem 里本来就只剩人声。
- **推荐（零模型）**：`voiced(t) = [RMS_vocal(t) > θ] ∧ [谐波性 > θ_h]`，θ 取全曲 vocal RMS 的 P40；谐波性用 `librosa.effects.harmonic` 能量比。加滞回（进入 150 ms / 退出 300 ms）去抖。
- 🧪 **实测已知缺陷**：原型阈值取"整曲 RMS 峰值 −32 dB"，**Demucs 的 vocals 轨常混进 lead synth，导致器乐段被误判为有人声**。→ VAD 结论必须标存疑，且在 L4 切轨时要与 `vocal_notes` 交叉验证。❓ 阈值待用 RWC-Pop（有旋律标注）标定。

### 3.5 人声旋律（note 事件）

- **主线 = basic-pitch**（Apache-2.0 ✅）：直接输出 note 事件（onset/offset/pitch/confidence）+ MIDI，一份输出同时解决"人声有没有"和"人声踩哪里"。
- 🧪 **实测：装不上，当前缺席**。`basic-pitch[onnx]` 两次尝试都报 `error: resolution-too-deep`（pip 在 `narwhals` 上无限回溯，加约束仍失败），超时放弃。**已按预案降级为用 vocals stem 的 onset + 活动度代替人声 note 事件**——代价是没有音高/时值，`vocal_notes` 特征目前不可用。
  - ⚠️ 相关背景 ✅：`basic-pitch[tf]` 钉死 `tensorflow-macos<2.15.1`，而该版本无 cp312 wheel → py3.12 上 TF 后端本就装不上；Darwin 默认会拉 `coremltools`（9.0 支持 3.12 ✅）走 CoreML。
  - 后续路径：换 pip 解析策略 / 锁死版本重试 onnx 后端，或改用 **RMVPE**（Apache-2.0 ✅）。❓ 三后端（CoreML/ONNX/TF）数值是否一致需实测，若不一致固定用 ONNX（onset 时间差会直接污染量化结果）。
- **排除**：CREPE（慢）、essentia Melodia（AGPL）、pYIN（GPL）。

### 3.6 输出

四组 onset 时间序列 + §3.2 的逐小节特征表 + 鼓件标签 + VAD 曲线——这就是知识 002"切轨"的候选池。⚠️ 分离质量直接影响踩音准确度；鼓 stem onset 通常最可靠（音游谱以鼓为骨架），人声 onset 需后处理去噪。

## 4. L3 结构层：段落 / 副歌 / 高潮 / 强度

### 4.1 事实更正（R8 / R9，v1.0 有误）

1. **all-in-one 的功能标签只有 8 类**（+ start/end 哨兵）：`intro / outro / break / bridge / inst / solo / verse / chorus` ✅（`src/allin1/config.py: HARMONIX_LABELS`）。
   → **没有 pre-chorus、没有 build**。v1.0 §4 写"10 类标签"、§5.3 样例直接写 `"type": "pre_chorus"` 都是错的——**`pre_chorus` 必须由规则派生**（派生判据见 §4.4）。
2. **all-in-one 出处是 WASPAA 2023，不是 ISMIR 2023**（arXiv 2307.16425，Kim & Nam）⚠️。v1.0 §4/§7.3 两处已更正。

### 4.2 分段：三路投票

| 角色 | 选择 | 理由 |
|---|---|---|
| **主线** | `all-in-one-infer` 3.1.0（PyPI 名，MIT ✅，R20） | 许可干净、py3.12 ✅、同时给 beat/downbeat/段落，与 L1 同源 |
| **二号意见（非商用分支）** | SongFormer（CC BY 4.0 ✅，R10） | 严格边界显著更好；有 prechorus/build/quietchorus 标签。**但前端 MuQ 权重是 CC BY-NC-4.0 ✅ → 只入个人实验分支** |
| **第三票（零风险）** | libfmp/librosa SSM 重复段 | RefraiD 的现代等价物；**器乐曲与主线失效时靠它兜底** |
| **投票规则** | 边界三者取交集 + **吸附到最近下拍**；标签 chorus 需 ≥2 票 | SongFormer 帧率 8.333 Hz（120 ms/帧）✅，边界分辨率天花板就是 120 ms，必须再吸附一次 |

**🧪 关于 all-in-one 的安装实测（推翻 v2 §6.2 的预期）**

- `all-in-one-infer` 3.1.0 **可用**（模块名 `allin1_infer`），且**支持直接喂已跑好的 Demucs stems**（`create_stems_input_from_directory`），省掉一次重复分离——这是实跑发现的重要优化点。
- **`natten` 在 Apple Silicon + py3.12 上源码编译通过**，调研预期的"NATTEN 无 wheel、自编译坑"**未出现**。（但仍推荐 `all-in-one-infer`，因为它把 natten 降级成可选 extra。）
- 官方 `allin1` 1.1.0 装上但 **import 即失败**（硬依赖 madmom，而 madmom 权重是 NC 红线）→ **不要装 `allin1`**。
- 🧪 **许可风险已解除**：`all-in-one-infer` 会拖进 `madmom-infer`（BSD-2 ✅），其 `models.py` 声明会运行时下载 CC BY-NC-SA 权重。实跑后检查 **`~/.cache/madmom_infer/models` 为空** → all-in-one 只用了 madmom 的 DBN 解码器（纯 HMM 算法、无学习权重），**未触及 NC 权重**。对外发布前仍建议每次复核一次此目录。

**🧪 分段可用性体检（原型新增，必须保留）**

两条路径**都会退化**：all-in-one 在 TransientTears 上只给 3 段、最长段占全曲 63%，对制谱毫无信息量；自研 SSM 的第一版也把 13–68 小节合成一个 B 段。
→ 统一体检判据：**段数 ≥ 5 且最长段 ≤ 35% 小节**；不通过就退回 SSM，SSM 内部再对"边界数 × 簇数"自适应重试；被否决的那版保留在 `alt_segments` 供人工比对。
→ ⚠️ **段落表必须人工过目 `plot.png` 复核**，这是当前管线里第二不可靠的环节。

**"副歌 = 重复最多段"的证据（R12）**：Goto (2006) 的 RefraiD 在 **RWC-Pop 100 首中 80 首完全正确、F = 0.938** ⚠️，而 **RWC-Pop 的 100 首里有 80 首是日语 J-pop** ✅。→ 该启发式在**日语流行乐上基本成立**，v1.0 §9.1 的"命中率未知"过于保守，可下调为"J-pop 成立"。**但**：(a) RWC-Pop 是 1990s 风格，与 160–220 BPM 的 ACG/音游曲仍有域差；(b) **器乐向音游曲（东方/hardcore/EDM）常常没有"副歌"概念**，只有 build → drop，此时该启发式失效，必须走能量/drop 通道。
⚠️ 没有任何 MSA 模型公布过日语曲上的分段指标 ✅（Harmonix = 西方流行；SongFormBench 语言字段 `["en","zh"]`，**无 ja**）→ 日语域验证必须自己做（§10 A 档）。

### 4.3 日式（英文）双标签词表（v1.1 新增，R11）

**规定：分析输出一律用「日式(英文)」双标签**——知识库与 MMFC 教程用的是中/日语境，且强模型对 `サビ`/`落ちサビ` 的语义理解比 `chorus` 携带更多制谱含义。

| 日式 | 英文 | all-in-one | SongFormer 细标签 | 本项目标准标签 | 制谱含义 |
|---|---|---|---|---|---|
| イントロ | Intro | `intro` | `intro`/`instintro`/`fadein` | `intro` | 低强度；"哪个响踩哪个" |
| Aメロ | Verse | `verse` | `verse`/`slowverse` | `verse` | 中强度；踩器乐，小节尾混人声 |
| Bメロ | Pre-Chorus | ❌ 无 | `prechorus`/`build` | `pre_chorus` | 爬升；**末尾切回人声铺垫** |
| サビ | Chorus / Hook | `chorus` | `chorus`/`altchorus` | `chorus` | 高强度；vocal 曲全踩人声 |
| 落ちサビ | Quiet Chorus | ❌ | `quietchorus` | `quiet_chorus` | **减压休息段**，密度骤降 |
| 大サビ / ラスサビ | Final Chorus | `chorus` | `chorus`（靠位置判定） | `final_chorus` | 全曲峰值；配置升级（单星→双手星） |
| Cメロ | Bridge | `bridge` | `bridge`/`instbridge` | `bridge` | 转折；换轨 |
| 間奏 | Interlude | `inst`/`solo` | `interlude`/`inst`/`solo` | `interlude` | 踩采样/独奏；靠**位移**提强度 |
| ドロップ | Drop (EDM) | `chorus`/`break` | `breakdown`/`build` | `drop` | 器乐曲的"副歌"，能量票主导 |
| アウトロ | Outro | `outro` | `outro`/`vocaloutro` | `outro` | 与 intro 前后呼应 |

⚠️ 日英并非严格一一对应（`Bメロ` 在部分语境被叫 Bridge），**映射以位置 + 功能判定为准，不以词面为准**。SongFormer 的 128 类细标签需先归并到本表的标准标签。

### 4.4 `pre_chorus` / `build` 派生规则（因主线无此标签）

满足**全部三条**：① 位于 chorus 起点前 4–8 小节；② 强度曲线单调上升；③ kick 密度下降或消失 ∨ hihat 密度上升 ∨ 高频能量单调上升（riser）。
这是 MMFC 5.4 第 3 条"build 末尾切回人声"的定位前提，不能省。

### 4.5 强度曲线配方 v2（R13，替换 v1.0 的融合式）

**v1.0 的问题**：系数全是拍脑袋；**质心权重过高**——质心对失真吉他、riser 敏感，但**对高音人声同样敏感**，容易把 Bメロ 的人声爬升误判成 drop；`onset_strength` 用全曲混音，被最响音轨支配，正是 MMFC 5.4 批评的"哪个响踩哪个"。

```
1) 统一 WAV → librosa.load(sr=22050, mono) + pyloudnorm 归一 −14 LUFS（防"谁响谁高潮"）
2) 同 hop=512 提特征；offset 校验另走 hop=256@44100（§2.4），不共用
3) 逐特征 5–95 百分位截断 → min-max 归一（记为 Z(·)）
4) 融合（新式）：
   I_bar = w1·Z(loudness_bar)     # 逐小节响度
         + w2·Z(n_onset_bar)      # 各 stem 去重合并 onset 数 ——「可踩音上限」
         + w3·Z(E_drums_bar)      # 鼓能量 —— 律动骨架
         + w4·Z(voiced_ratio_bar) # 人声活动率 —— J-pop 的情绪主载体
         + w5·Z(flux_bar)         # 谱通量 —— 瞬态密度
   w = [0.00, 0.19, 0.07, 0.00, 0.74]           🧪 **v1.3 标定值**（n=40，见下）
   （v0.1–v0.3 的手写初值 [0.25, 0.30, 0.20, 0.15, 0.10] 保留为 LEGACY，可用 --fusion-weights 取回）
   相对 v1.0 的改动：onset 从"强度包络"改为"去重 onset 计数"并升为最大权重；
                     新增人声活动项；**质心项删除**（移到 drop 专用票）
5) 小节聚合：直接用 §2.2 构造出来的 downbeat，**不要用 beats[::4]**（只在恒定 4/4 且首拍对齐时成立）
6) 平滑：小节级中值滤波（窗 ≈ 2 小节）+ 高斯(σ≈1)
7) 高潮定位 = 五票投票（§4.6）
8) 映射到谱面密度（§4.7）
```

⚠️ 🧪 原型 v0.1 仍在跑 v1.0 的四项融合式（rms/onset/centroid/drums）；v0.2 起已实现本式。

**🧪 v1.4：权重复验结论 —— 不再换（n=160，标定报告 n160 §3）**

> 在 160 首上重标定得到 `[0.00, 0.168, 0.113, 0.00, 0.719]`，与现役默认几乎重合。
> **LOSO 160 折对照现役默认：−0.0015 / 82 折胜出（51.3%）/ 符号检验 p = 0.81 —— 三条判据全不过 → 维持。**
> 反过来对照 v0.3 初值仍然是 **+0.0617 / 124 折（77.5%）/ p ≈ 0** → **v1.3 那次替换被四倍样本确认正确**。
> **同权重口径的 Fisher ρ（两轮同型计算，可直接比）**：
> v0.3 初值 0.2959（n=40）→ **0.2999（n=160）**；现役 v0.4 0.3847（n=40）→ **0.3665（n=160）**。
> 单特征排序完全复现：flux 0.369 > loudness 0.286 > drums 0.225 > onset 0.191 > **voiced 0.055**。
> ⚠️ **一条 v1.3 的结论被推翻**：v1.3 说"整条融合式仍然低于 flux 单项"。n=160 上
> 融合式已经追平 flux 单项（0.3665 vs 0.3686，中位反而是融合更高 0.3514 vs 0.3460）——
> 那是 v0.3 权重太差的表现，不是"融合没用"。

**🧪 v1.3：权重标定结论 —— 换（n=40，R1，标定报告 n40 §3）**

> v1.2 的结论是"不换"（n=8：+0.027、5/8 折、p≈0.36，三条判据全不过）。
> n=40 上**三条判据同时通过**，按约定必须换。

用 40 首官方音频 × 官方谱做非负最小二乘（岭 α=1）+ **留一曲交叉验证（40 折）**，目标是逐小节官方密度：

| 换默认值的判据 | 门槛 | n=8 实测 | **n=40 实测** |
|---|---|---|---|
| CV Spearman 相对初值的提升 | ≥ 0.03 | +0.027 ❌ | **+0.0757** ✅ |
| 胜出折数比例 | ≥ 70% | 62.5%（5/8）❌ | **80.0%（32/40）** ✅ |
| 双侧符号检验 | p < 0.05 | p ≈ 0.36 ❌ | **p = 0.0002** ✅ |

| 权重方案 | 权重 (loud/onset/drums/voiced/flux) | Fisher ρ | 均值 ρ | 中位 ρ |
|---|---|---:|---:|---:|
| `legacy`（v0.1–v0.3 初值） | 0.25 / 0.30 / 0.20 / 0.15 / 0.10 | 0.296 | 0.276 | 0.290 |
| **`calibrated_n40`（v1.3 默认）** | **0.00 / 0.19 / 0.07 / 0.00 / 0.74** | **0.385** | **0.357** | **0.425** |
| `flux_only`（对照） | 0 / 0 / 0 / 0 / 1.00 | 0.393 | 0.365 | 0.412 |
| `voiced_to_flux`（n=8 的 H1+H2 猜测） | 0.25 / 0.30 / 0.20 / 0.00 / 0.25 | 0.334 | 0.308 | 0.361 |

- **n=8 的两条待验假设都被证实了方向**：**H1（`voiced` → 0）✅**（单项 ρ 0.088，五项最低，NNLS 给 0）；
  **H2（`flux` 升权）✅**，但当初猜的 0.25 **严重低估**，实测该到 **0.74**（单项 ρ 0.393，五项最高）。
- **为什么不用 `flux_only`**（Fisher ρ 还略高）：它中位更低（0.412 vs 0.425）、
  单特征无冗余、且它不是 LOSO 那个"39 首拟合 → 第 40 首泛化"过程的产物。
- ⚠️ **三条口径边界**：① 标定目标**只有逐小节密度**，`I_bar` 还被 §4.6 高潮五票与段落强度分档消费，
  那两处**从未标定**——`loudness`/`voiced` 归零只代表它们预测不了密度；
  ② 新权重让 `I_bar` 实质变成**谱通量曲线**（flux 74%），对响度不敏感，
  song sheet 的 Header 必须说明"强度"的语义变窄了；
  ③ 提升是统计意义上的：0.226 → 0.385 **仍然只是中等相关**，仍然定不了"高潮在哪一小节"。
- ⚠️ **平滑没有帮助**（n=8：raw 0.468 ≥ smoothed 0.454；**n=40：raw 0.257 ≥ smoothed 0.226**，
  差距更大）——中值+高斯平滑抹掉的正是官方谱也会有的单小节爆发（知识 031：峰值小节 ≈ 2.06 × 全曲均值）。
  目前仍保留平滑，但 `bar_raw` 必须同时输出。
- 🧪 **v1.3 的最强分层结论，v1.4 大幅下修**：v1.3 报 **器乐曲 0.399 / 人声曲 0.146（2.73×）**；
  **n=160 实测是 0.4196 / 0.2823（51 vs 109 首，1.49×）**。
  n160 §2.2 做了 2×2 分解，把三个混淆因素拆开：

  | | v0.3 权重 | v0.4 权重 |
  |---|---|---|
  | 原 40 首 | 器乐 0.425 / 人声 0.235（1.81×） | 器乐 0.475 / 人声 0.345（1.38×） |
  | 新 120 首 | 器乐 0.382 / 人声 0.262（1.46×） | 器乐 0.437 / 人声 0.321（1.36×） |

  → ① 换成不平滑口径后，同样 40 首、同样 v0.3 权重的倍数就从 2.73× 掉到 1.81×
  （**平滑对人声曲的伤害更大**）；② 换权重把它压到 1.38×（新权重救回来的主要是人声曲）；
  ③ 加样本在两套权重下都继续往下压。
  **正确表述：人声曲的可预测性比器乐曲低约三成（ρ 0.28 vs 0.42），机理（唱词句读不在音频能量里）
  仍然成立，但"几乎不管用"说过头了。**
- 🧪 **v1.4 新增：按官方 `&genre` 六曲风分层**（硬标签，不是音频侧代理）——
  オンゲキ＆CHUNITHM 0.388（13 首）> maimai 0.356（40）> ゲーム＆バラエティ 0.349（34）>
  東方Project 0.333（22）> niconico＆ボーカロイド 0.296（40）> **POPS＆アニメ 0.186（11）**。
  **曲风间的落差小于人声/器乐的落差，而且排序基本跟着各曲风的人声曲占比走**
  → **曲风不是独立的解释变量，分层结论应该按人声/器乐给，不按曲风给**。
- 🧪 **v1.4：定数与 BPM 分层完整复现**：定数越高越可预测
  （13.0–13.2 **0.283** / 13.3–13.7 0.285 / 13.8–14.2 0.377 / 14.3–14.5 **0.508**，比 n=40 更单调）；
  BPM 150–179 仍是最差的一档（0.266，57 首，与人声曲最密集的一档混淆）。

### 4.6 高潮定位：四票 → 五票（R14）

| 票 | v1.0 | v1.1 | 理由 |
|---|---|---|---|
| V1 结构票（chorus/重复段内） | 0.40 | **0.45** | 现在有两到三个模型投票，可靠性提高 |
| V2 novelty 票（SuperFlux 局部峰 ±1 小节三角窗） | 0.30 | 0.25 | 保留 |
| V3 能量票（rms_n>0.75 且局部极大） | 0.20 | 0.20 | 保留；**器乐曲 fallback 时升到 0.5** |
| V4 质心票（cent_n>0.7） | 0.10 | **0.05** | 降权，只留给 EDM drop |
| **V5 人声票（新增）** | — | **0.05** | `voiced_ratio > 0.6` 且音高中位处于全曲上四分位 → J-pop 副歌强特征 |

`climax = argmax(vote)`；前 k 个峰 = 各次副歌出现时间。
**结构层必须输出 `repeat_of` 与 `chorus_index`**——MMFC 5.4 第 7 条"第二副歌踩音相同但配置升级"要求能明确区分"第 N 次副歌"。`final_chorus / 大サビ` = 所有 chorus 段中"**最后一个 + 强度最高**"者。

五票互补原理：结构票覆盖摇滚 chorus=整段反复；novelty 票覆盖 drop/爆发瞬态；能量票覆盖响度/密度；质心票覆盖电子乐 drop；人声票覆盖 J-pop。流派差异建议两套权重预设（电子 drop vs 摇滚 chorus）。

**🧪 v1.2 降级：`climax` 只代表「音频能量高潮」，不是谱面密度峰定位器（R4，标定报告 §2.3）**

配对实测（4 小节滑动均值 argmax，与 `chart_analysis` 同口径）：

| 指标 | n=8 | **n=40（v1.3）** |
|---|---|---|
| 音频强度峰 vs 官方谱密度峰的**位置中位误差** | 33 小节 | **18 小节**（曲长中位 ~85 小节 → 仍是随机猜的量级） |
| 换 8 小节窗 | 34 小节，无改善 | 21 小节，无改善 |
| 音频强度峰落在谱面密度**前 20% 小节**里 | 4/8（50%） | **18/40（45%）** |
| **五票 climax** 落在前 20% 里 | 5/8（62.5%） | **15/40（37.5%）** |

- 🧪 **v1.3 推翻了 v1.2 的一条附带观察**：n=8 时五票 climax（62.5%）看起来优于裸强度峰（50%），
  **n=40 上反而更差（37.5% vs 45%）**。→ 正名为「音频能量高潮」是对的，
  **但不得暗示"投票机制让它更接近谱面峰"**。
- 谱面峰位置本身符合知识 031（相对位置中位偏后）；**是音频强度峰太散**。
- → 输出层已正名为 **`audio_climax_bar`**，song sheet 与 JSON 都显式写明
  "**音乐上最激烈处 ≠ 谱面最密处**"。L4 规划**不得**把它当密度峰的定位依据；
  谱面峰位置应走知识 031 的分布先验（后置、末段最强）。

### 4.7 强度 → 密度：用官方谱实测取代手写模板（R15，v1.1 最重要的一处修订）

**v1.0 的 `T=[0.20,0.45,0.30,1.00,0.10]` 作为密度目标已被 388 个官方 ST 谱实测推翻**（详见 `docs/research/official-chart-density-curves.md` §4，知识 031）。

**(1) 形状先验换成实测值**

```
T_density = [0.62, 0.77, 0.72, 0.78, 0.82]        # 结构对齐五段口径，归一到峰值=1
95% 自助 CI：[0.60–0.64, 0.75–0.79, 0.70–0.74, 0.76–0.80, 0.80–0.84]（n=388）
等分五段口径：[0.81, 0.92, 0.91, 0.97, 1.00]（相对全曲均值 [0.88,0.99,0.98,1.06,1.09]）
⚠️ 两种口径不可混用，使用时必须注明
```

- **前四段"弱 → 较强 → 较弱 → 强"的定性顺序被 388 谱完整验证**（段序 s1 < s3 < s2 < s4）；
- **第五段被推翻**：实测 s5 = 0.817 是五段最高，不是 T 说的 0.10；
- **T 的动态范围是真实值的约 4.5 倍**——拿 T 当密度目标直接驱动生成，第 5 段只会给出 1/10 峰值的密度，远低于官方谱下限。

**(2) 密度地板（映射的核心非线性）**

音频强度可以逼近 0，**谱面密度不能**——任何一小节只要还在出音，就得给出基本踩音密度。实测归一密度分位（388 谱均值）：**p10 = 0.264 / p50 = 0.514 / p90 = 0.730**；段落尺度上地板被抬高到 **≈ 0.62**（结构对齐五段的最弱段）。

```
density_norm = floor + (1 − floor) · intensity_norm
  段落尺度（给每段定基调）：floor ≈ 0.60   ← 默认未改，但见下方 n=8 分歧
  小节尺度（段内细分）：    floor ≈ 0.25
```

**🧪 v1.4（n=160，报告 n160 §4.1）**：小节地板 **0.25 第三次站住**（池化最优 0.340，
默认只差 **+0.53%** SSE）；段落尺度的池化最优从 n=40 的 0.365 变成 **0.330**，
`CALIBRATED_SECTION_FLOOR` 已更新为 0.330。
⚠️ **默认 `SECTION_DENSITY_FLOOR = 0.60` 的 SSE 代价从 +3.7% 涨到 +9.53%**（0.25 则是 +1.14%）——
**仍然不改**（0.60 来自 388 谱的段间落差统计，大样本优先，而且它是"只封不抬"的保守值），
但代价已经不小，下一轮若再涨应当重新裁定。
n=160 的 I 五分位画像（曲内均值 = 1）是 **[0.771, 0.969, 1.041, 1.093, 1.127]** ——
比 n=40 略陡，但仍然比 `floor=0.25` 模型平（"官方密度对音频强度的响应偏钝"维持）。

**🧪 v1.3：地板的 n=8 分歧解除 —— 0.125 是小样本噪声（R2，标定报告 n40 §4.1）**

| 尺度 | 388 谱给的默认 | n=8 池化最优 | **n=40 池化最优** | 默认值相对 n=40 最优的 SSE 劣化 |
|---|---:|---:|---:|---:|
| 小节尺度 | 0.25 | 0.020 | **0.350** | **+0.5%** → **保留**（比 n=8 时更站得住） |
| 段落尺度 | **0.60** | 0.125 | **0.365** | **+3.7%** → **保留**（0.25 则是 +1.2%） |

- **v1.2 的"分歧"消失了**：n=40 的段落最优 **0.365** 离 388 谱的 0.60/0.62 比离 0.125 近得多，
  **n=8 报出的 0.125 是噪声**。`intensity.CALIBRATED_SECTION_FLOOR` 已由 0.125 更新为 **0.365**；
  两个默认值（0.60 / 0.25）都不动，`--section-floor` / `--bar-floor` 仍可调。
- **两个口径仍然不能混**：知识 031 的 0.62 是**谱面自身**段间落差的统计，
  这里的 floor 要回答的是"**音频弱到什么程度时谱面才可以变稀**"。
- 实测画像（I 五分位，以曲内均值为 1）：

  | I 分位 | Q1(最弱) | Q2 | Q3 | Q4 | Q5(最强) |
  |---|---:|---:|---:|---:|---:|
  | n=8 实测 | 0.598 | 0.945 | 1.081 | 1.136 | 1.239 |
  | **n=40 实测** | **0.803** | 0.996 | 1.046 | 1.052 | **1.103** |
  | `floor=0.25` 模型预测 | 0.721 | 0.957 | 1.045 | 1.099 | 1.176 |

  🧪 **v1.3 推翻 v1.2 的"真实映射比模型稍陡"**：n=40 的真实映射比模型**更平**
  （最弱档 0.803 > 模型的 0.721，最强档 1.103 < 模型的 1.176）。
  这与 ρ 掉一半是同一件事的两个说法——**官方密度对音频强度的响应比设想中钝**。

**🧪 v1.4（n=160）：结构封顶 0.75 被确认，不动。** 155 个 intro 段的
「实际密度 / 全曲均值密度」分布是 p25 0.597 / **中位 0.784** / p75 0.975，
0.75 落在中位稍下，是个合理的保守值。⚠️ **33/155（21.3%）的 intro 比全曲均值还密**
（p90 1.073），对它们封顶本身就是错的；MYTHOS 型故障（音频强度分位 ≥0.5 而密度分位 ≤0.4）
在 187 个 intro/outro 段里只有 **6 段（3.2%）**（n=40 是 4/46 = 8.7%）——
**它是真实存在的形态，但比 n=40 看到的还要罕见。**

**🧪 v1.2 新增：intro/outro 的密度由结构封顶，不跟能量曲线走**

个案证据（主会话 2026-09-11 看音频强度 × 官方密度叠加图）：**MYTHOS 小节 8–19 的音频强度 ≈ 1.0，
官方谱却只放 4 note/小节** —— 谱师是按"这是前奏"压密度，**不跟随响度**。

```
suggested(intro/outro) ≤ cap_ratio × mean(全曲 suggested)     # v1.3 起 cap_ratio = 0.75
```

段落级与逐小节级同时封顶，**只封不抬**。

**🧪 v1.3：比例从 0.60 上调到 0.75（R3，标定报告 n40 §6.4）** —— 0.60 原本只有 MYTHOS 一个个案。
40 首实测：

| 口径 | 实测 |
|---|---|
| "音频响但谱面稀"（段内音频强度分位 ≥0.5 而谱面密度分位 ≤0.4）的 intro/outro 段 | **4 / 46** —— 真实存在但不是主流 |
| **intro 实际密度 / 全曲实际均值密度**（39 首） | p25 **0.538** / **中位 0.726** / p75 **0.940** / p90 1.083 |
| intro 比全曲均值还**密**的曲子 | **8 / 39**（最高 1.279） |

→ 0.60 落在中位以下，**系统性压过头**，改为 **0.75 ≈ 中位（0.726）**。
⚠️ ① 封顶作用在**建议**密度上，这里量的是**实际**密度，二者不等价；
② 对那 8 首 intro 比全曲还密的曲子封顶本身就是错的 —— 它是**护栏不是目标值**，
`--intro-outro-cap 0` 可关闭。

**(3) 六条官方谱密度规律（L4 直接当规则用）**

| 规律 | 实测数字 | 对生成的约束 |
|---|---|---|
| **末段最强，不减压收尾** | 曲末 10% 密度 = **1.084 × 全曲均值**；**24.2% 的谱把高潮段放在最后 10%**（最大众数）；高潮段中位位置 **0.72**，**74.7% 落在后半程** | 禁止"峰在 4/5 处再淡出"；收尾减压只发生在最后 1–2 小节 |
| **第二副歌更密但幅度小** | 后半/前半强度比中位 **1.119（≈ +12%）**，**79.6% 的谱后半更强** | 第二副歌密度 +10%~+15%，不要翻倍 |
| **"强度 ≠ 密度"** | SPICY SWINGY STYLE 第二副歌密度只 **+5.1%**，而知识 002 说它"配置升级" | 强度差异主要由**配置复杂度**（知识 003 四因素）承担，密度只是其中一个维度 |
| **休息靠低密段，不靠空小节** | 休息小节（note ≤ 1）只占全曲 **2.43%**，**35% 的谱一个都没有**；低密段每谱 **5.91 段 × 1.69 小节** | 休息段写成"密度降一档"，不要写成空小节 |
| **三明治结构是主流** | "高密 → 低密 → 高密" **61.9% 的谱至少出现一次**，每谱均值 2.41 次 | 两个副歌之间应安排一次明显的减压 |
| **约 7 个密度台阶** | 每谱密度台阶（≥4 小节同档平台）均值 **7.09、中位 7**；中位 85 小节 → **平均每 12 小节换一次密度基调** | 这是 L4 做段落规划的天然粒度 |

**(4) 绝对量级锚点：v1.2 改走 NPS（R6，标定报告 §4.2）**

```
主锚（v1.2）：note/小节 = NPS(定数) × 每小节秒数 = NPS × beats_per_bar × 60 / BPM
              NPS：13.0→5.29 / 13.5→6.13 / 14.0→7.14 / 14.5→7.70（知识 031 §7）
对照列（v1.1 旧锚）：note/小节 = 13.0→8.03 / 13.5→9.15 / 14.0→10.54 / 14.5→10.10；全库 8.96
```

- **换锚的原因**：旧锚在 **BPM ≥ 200 上系统性高估**——note/小节在高 BPM 下等价于更高的 NPS。
  n=8 口径 MAPE 21.0% → 16.5%；**n=40 口径 17.15% → 15.29%**（优势从 4.5 个百分点缩到 1.9 个）。
- 🧪 **v1.4 推翻 v1.3 的 R7：NPS 主锚全面胜出，"按 BPM 选锚"的建议撤回（报告 n160 §4.2）。**
  n=160 全样本 MAPE **NPS 12.46% vs note/小节 15.52%**；分层后 **NPS 在每一个 BPM 档
  与每一个定数档上持平或占优**：

  | BPM 段 | n | note/小节 锚 | **NPS 锚** | v1.3 的裁定 | **v1.4 的裁定** |
  |---|---:|---:|---:|---|---|
  | < 150 | 39 | 18.5% | 18.4% | 打平 | **打平（维持）** |
  | 150–179 | 57 | 11.2% | **10.5%** | note/小节 更好 | **NPS 略优 → 推翻** |
  | 180–199 | 36 | 10.6% | 10.7% | **note/小节 大幅更好** | **打平 → 推翻** |
  | ≥ 200 | 28 | 26.4% | **10.5%** | NPS 大幅更好 | **维持** |

  | 定数档 | n | note/小节 锚 | **NPS 锚** |
  |---|---:|---:|---:|
  | 13.0 / 13.5 | 26 / 79 | 13.2% / 15.4% | **12.6% / 13.6%** |
  | 14.0 / 14.5 | 43 / 12 | 17.6% / 13.7% | **10.8% / 10.1%** |

  → **v1.3 的 R7 建立在 180–199 档只有 8 首的 5.1% 上；n=160 的 36 首给出 10.6% vs 10.7%（打平）。**
  唯一的反例是 POPS＆アニメ（11 首，note/小节 11.1% vs NPS 18.0%），样本太小，只记录。
  **主锚维持 NPS，不再按 BPM 选锚。**
- 🧪 **v1.3 的修正（已被 v1.4 推翻，保留作历史）：单一锚点两头不讨好（R7）。** n=40 分层后是：

  | BPM 段 | n | note/小节 锚 MAPE | NPS 锚 MAPE | 谁更好 |
  |---|---:|---:|---:|:-:|
  | < 150 | 9 | 21.9% | 22.4% | 打平 |
  | 150–179 | 15 | 11.7% | 13.5% | **note/小节** |
  | 180–199 | 8 | **5.1%** | 13.3% | **note/小节（大幅）** |
  | **≥ 200** | 8 | **34.1%** | **12.7%** | **NPS（大幅）** |

  | 定数档 | n | note/小节 锚 | NPS 锚 |
  |---|---:|---:|---:|
  | 13.0 / 13.5 | 10 / 14 | **12.3% / 14.7%** | 17.3% / 19.3% |
  | 14.0 / 14.5 | 11 / 5 | 21.1% / 25.0% | **8.9% / 14.2%** |

  → **n=8 的诊断（高 BPM 崩掉）完全正确，但它没看到中段 BPM 上旧锚更准。**
  **v1.3 不改默认**（NPS 锚在全样本上仍占优），但正确做法是**按 BPM/定数选锚**
  或直接实现 §4.8(B) 的分位映射。
- ⚠️ **精度只到档位**：逐小节 MAE 均值 **2.96** note/小节（n=8 是 3.4、n=40 是 3.10）
  → 发给 LLM 时必须标明它是 **±3 note 的粗档**，不是逐小节目标值。
- **峰值小节 ≈ 2.06 × 全曲均值**（知识 031）——与知识 004 的 note 总数区间、知识 015 的密度红线
  一起做硬裁剪。

⚠️ 样本限制：只覆盖 ST 谱、定数 13.0–14.5、MASTER/Re:MASTER；12 级及以下与 DX 谱未测，外推需谨慎。
⚠️ 方法学坑：变点检测**必须加最小段长约束**（`min_len_frac=0.10`），否则 37.4% 的谱会被切出一个极短的第 5 段，**凭空制造"尾部渐弱"的假象**。
⚠️ k-means 轮廓系数 < 0.08 → 官方谱密度曲线是**连续谱系，没有清晰可分的"流派"**，聚类结果只能当倾向描述（三簇：全程高密平台型 40.7% / 双段递进型 33.5% / 单峰后置型 25.8%）。

### 4.8 标定协议 A–E（v1.1 新增，R16；v1.4：(E) 已完成三轮，n=160）

原始现实（v1.1 时）：`resource/official-chart/` 有 388 个官方 ST 谱但**没有对应 mp3** → 无法做"音频特征 → 谱面密度"的回归。应对：把标定拆成五块，其中三块不需要音频。
🧪 **v1.2 更新**：用户 2026-09-11 提供了 **8 首**官方曲包（音频 + 谱面），(E) 已经跑通（见下）。

- **(A) 形状先验（不需要音频）** — 已完成 ✅：388 谱逐小节密度 → 归一 → 重采样 32 bin → 变点检测五段 → `T_density`（§4.7）。顺带实证检验了知识 001 的五段走势（前四段成立、第五段推翻）。
- **(B) 分位映射：强度 → NPS（不需要配对数据）**
  ```
  d_target(b) = F_chart⁻¹( F_audio( I_bar(b) ) )
  F_audio  = I_bar 的经验分位函数；F_chart = 官方同定数谱 d_b 的经验分位函数
  ```
  这是统计降尺度的标准 quantile mapping：**只要求两边的排序结构可比，不要求逐条配对**。保证绝对 NPS 水平来自官方谱（难度校准正确），相对起伏来自音频（情绪贴合正确）。再叠加知识 004（总 note 数区间）与知识 015（密度红线）硬裁剪。
- **(C) 谱面内部结构比值（不需要音频）** — 已完成 ✅：见 §4.7(3) 六条规律；变点检测能从密度曲线直接还原乐曲段落边界（SPICY 个案 4 个边界 4/4 命中知识 002 的叙事）。
- **(D) 音频侧人工听审**：选 5–10 首目标域曲逐段听审给强度档（1–5），与 `I_bar` 的段落均值算 Spearman 相关。
  ❓ 仍未做。**目标值在 v1.4 被回调（n=160，R11 修订）**：v1.0 写"目标 ρ ≥ 0.8"，
  v1.2 按 n=8 下调到"逐段 0.5–0.6"，v1.3 按 n=40 再下调到"逐段 ρ ≈ 0.3 量级"，
  **n=160 说 0.3 又定低了** —— 实测逐段 ρ 中位 **0.49**（段数 ≥6 的 146 首 Fisher **0.53**）。
  **合格线应回调到逐段 ρ ≈ 0.45–0.55，并按曲目类型分别给（器乐曲 ~0.6 / 人声曲 ~0.45）。**
  人工听审的价值是**查错**（找音频侧明显判反的段落），不是刷相关系数。
- **(E) 配对标定 —— 🧪 已做三轮：n=8 → n=40 → n=160（2026-09-13）**，
  报告 **`docs/research/audio-chart-calibration-n160.md`**（以它为准）。**n=160 的主表**：

  | 项 | n=40 | **n=160（以此为准）** |
  |---|---|---|
  | 逐小节 ρ（**同权重口径**，v0.3 初值） | 0.2959 | **0.2999**（四倍样本纹丝不动） |
  | 逐小节 ρ（**同权重口径**，现役 v0.4） | 0.3847 | **0.3665** |
  | 逐小节 ρ（管线实测，平滑；**口径随权重变，勿跨轮直接比**） | 0.2255 | **0.3277**；负相关 **16/160（10%）** |
  | **逐段** ρ（管线实测） | 0.3202 | **0.5888**；⚠️ 段数 ≥6 的 146 首是 **0.5324**，**引用请用后者** |
  | **按曲目类型分层** | 器乐 0.399 / 人声 0.146（2.73×） | **器乐 0.4196 / 人声 0.2823（1.49×）** |
  | **按官方六曲风分层**（v1.4 新增） | — | オンゲキ 0.388 > maimai 0.356 > ゲーム 0.349 > 東方 0.333 > niconico 0.296 > **POPS 0.186** |
  | 单特征最强项 | `flux` 0.393 | **`flux` 0.369**；`voiced` **0.055**（仍近乎无预测力） |
  | 候选池 recall / precision | 0.837 / 0.550 | **0.840 / 0.549** |
  | 音频段落边界命中官方密度变点 | ±1 53.8% / ±2 71.3% | **±1 49.8% / ±2 67.3%** |
  | 音频峰 vs 谱面密度峰位置 | 中位误差 18 小节 | **18 小节**（完全一致） |
  | 权重 LOSO | 32/40 折、p=0.0002 → 换 | **对照现役 82/160 折、p=0.81 → 不换** |
  | 各 stem 归因（drums recall / 不落鼓 / 什么都不落） | 0.639 / 34.3% / 16.8% | **0.650 / 33.7% / 16.0%**（全部复现） |
  | **`piano` 轨**（六路，v1.4 新增到 160 首） | — | **recall 0.289 / precision 0.606 / lift 2.44**，recall 高于 `other`(0.268) 与 `vocals`(0.197) |
  | 骨架模型一致率 vs 常数基线 | 85.2% vs 84.4% | **84.7% vs 84.1%**（仍微胜） |
  | 结尾形态（末 8 小节） | 尾杀 57.5 / 渐弱 2.5 / 其他 40.0 | **65.6 / 1.9 / 32.5**，与 387 谱语料（65.1/1.0/33.9）**逐档误差 < 1 pt** |

  **结论一句话（v1.4 修订）**：音频强度**勉强**够用来定"这一段大概多密"，
  **完全不够用来定"高潮在哪一小节"**；它**对人声曲比对器乐曲差三成**（不是"几乎不管用"）。
  ⚠️ 切轨侧仍是代理判据，且它的失败模式在 n=160 上**更严重**
  （器乐曲《疾走あんさんぶる》的 vocals 轨拿到 recall 0.579 = 160 首最高）。
  → **n=40 提出的"下一轮最该补人声 ground truth 而不是更多曲子"被 n=160 完全证实**：
  加 120 首把密度侧的数字全都稳住了，**人声侧一条都没解决**。

- **(E) 第二轮：n=8（2026-09-11）→ n=40（2026-09-11 扩样）**（保留作历史）：
  用户共提供 40 首官方 ST 曲包（官方 mp3 + 官方 `&inote_5`），工具 `tools/calibration/`，
  报告 **`docs/research/audio-chart-calibration-n40.md`**（n=8 的旧报告已被它取代）。主要数字：

  | 项 | n=8 | **n=40（以此为准）** |
  |---|---|---|
  | 逐小节 `I_bar` vs 官方 `d_bar` Spearman | 0.454 | **0.226**（换权重后 **0.385**）；**9/40 首为负** |
  | **逐段** Spearman | 0.523 | **0.320** |
  | **按曲目类型分层**（v1.3 新增） | — | **器乐曲 0.399 / 人声曲 0.146** |
  | 单特征最强项 | `flux` 0.524 | **`flux` 0.393**；`voiced` **0.088**（仍近乎无预测力） |
  | 候选池 recall / precision | 0.821 / 0.546 | **0.837 / 0.550**（最稳的一组） |
  | 音频段落边界命中官方密度变点 | ±1 小节 53% | **±1 小节 53.8%、±2 小节 71.3%**（样本翻五倍纹丝不动） |
  | 音频峰 vs 谱面密度峰位置 | 中位误差 33 小节 | **18 小节**（仍是随机猜量级，见 §4.6） |
  | 权重 LOSO 交叉验证 | 5/8 折、p≈0.36 → 维持初值 | **32/40 折、p=0.0002 → 换**（见 §4.5） |
  | 各 stem 归因（drums recall / 不落鼓 / 什么都不落） | 0.607 / 36.2% / 18.6% | **0.639 / 34.3% / 16.8%**（全部复现） |
  | 骨架模型一致率 vs 常数基线 | v0.2 规则表 64.7% vs 82.4% ❌ | **v0.3 骨架 85.2% vs 84.4% ✅**（v0.2 复刻 62.4%） |

  **结论一句话（v1.3 修订）**：音频强度**勉强**够用来定"这一段大概多密"，
  **完全不够用来定"高潮在哪一小节"**；而且**它只对器乐曲管用，对人声曲几乎不管用**。
  ⚠️ 切轨侧仍是代理判据，且本轮找到了它的明确失败模式（器乐曲 vocals 轨泄漏被算成"踩了人声"）。
  → **下一轮最该补的不是更多曲子，是更好的人声 ground truth**（basic-pitch / RMVPE）。

> ⚠️ (A)(B)(C) 全部只使用 `resource/official-chart/` 的官方谱；不引入任何社区/自制谱作为设计或评测基准（用户规矩）。

### 4.9 强度层排除项（R17）

- **pop-music-highlighter**（TISMIR 2018）：**GPL-3.0** ✅ + TF 1.x、2018-10 停更 → **红线排除**。
- Essentia DEAM arousal：AGPL + 模型 CC BY-NC-SA → 排除。
- MERT / MuQ / music2vec 特征：权重 CC BY-NC → 排除。
- → **强度层维持"纯 librosa + pyloudnorm、零模型"的定位是正确的**，本轮没有找到许可干净又明显更好的替代品。

## 5. L4 规划层：逐段踩音计划（核心设计）

> 这一层把 MMFC 第五章的谱师决策规则化，输入全部是 L1–L3 的结构化特征 + 知识库规则。
> 🧪 **当前状态：未实现**（原型 v0.1 只到 L3）。

### 5.1 输入

- 段落列表（每段：小节范围、日式(英文)双标签、强度档位、`chorus_index`、`repeat_of`、各 stem onset 密度）
- 目标难度（→ 目标 NPS 区间与 note 总数区间，知识 004；note/小节量级见知识 031）
- 知识库规则：001（强度贴合情绪）、002（切轨）、003（配置强度四因素）、005（采音要简）、012–015（可玩性约束）、016（ST 要素黑白名单）、**031（官方谱密度曲线实测规律）**

### 5.2 「段落类型 × 特征 → **骨架轨 + 点缀轨**」规则表（v1.2 重写，R8–R10）

**v1.1 的单值 `primary_stem` 模型已被配对标定证伪**（n=8 定性、n=40 定量确认；代理判据）：

| 实测 | n=8 | **n=40（以此为准）** |
|---|---|---|
| 官方 note 落在 `drums` onset 上（recall / lift） | 0.607 / 2.64 | **0.639 / 2.73**（各 stem 最高） |
| **不**落在 drums 上 | 36.2% | **34.3%** |
| 只落 drums / 只落 vocals | 28.1% / 3.2% | **30.4% / 3.5%** |
| 什么 stem 都不落（自由发挥 / 装饰 / onset 漏检，三者无法区分） | 18.6% | **16.8%** |
| 逐段"实测最高命中轨"分布 | 85 段：drums 70 / other 10 / vocals 3 / bass 2 | **378 段：drums 319 / other 43 / vocals 9 / bass 7** |
| v1.1 单值规则表的一致率 | 64.7%（常数基线 82.4%）❌ | **62.4%**（本轮复刻，与 n=8 吻合）❌ |
| **v1.2 骨架模型的一致率** | 未测 | **85.2% top-1 / 97.9% top-2**（常数基线 **84.4%**）✅ |

🧪 **v1.4（n=160，1490 个音频段）——四项全部复现，最大移动 0.5 个百分点**：

| 项 | n=40（378 段） | **n=160（1490 段）** |
|---|---|---|
| `drums` recall / lift | 0.639 / 2.73 | **0.650 / 2.78** |
| **`piano`**（六路，v1.4 新增） | — | **0.289 / 2.44**（precision **0.606**，仅次于 drums） |
| `other` / `vocals` / `bass` recall | 0.267 / 0.182 / 0.199 | **0.268 / 0.197 / 0.196** |
| `fx`（>4 kHz 瞬态） | — | **0.057 / 1.69**，而且 **24/160 首根本没有 fx 事件** |
| 什么都不落 | 16.8% | **16.0%**（加上 piano/fx/melody 之后仍有 **12.1%**） |
| 逐段"实测最高命中轨"分布 | drums 319 / other 43 / vocals 9 / bass 7 | **drums 1253 / other 167 / bass 36 / vocals 34** |
| **骨架模型 top-1 / top-2** | 85.19% / 97.88% | **84.70% / 97.32%**（常数基线 **84.09%**）✅ |
| v1.1 单值规则表 | 62.4% | **63.6%** ❌ |
| 点缀轨命中"实测第二高轨" | 61.1% | **65.6%** |

**v1.4 新增建议（尚未实现）**：把 **`piano` 纳入点缀轨候选**（它的 recall 高于 `other` 与 `vocals`）；
⚠️ 但它的曲风差异极大（POPS＆アニメ 0.486 vs オンゲキ＆CHUNITHM 0.125），
**应按曲内实测倾向决定，不要按段落类型写死**。
**另一处缺口**：管线对器乐向曲子会产出 `drop` 标签（n=160 有 **81 段**），
而本表**没有 `drop` 这一行**，现在走兜底路径；实测形态接近 `chorus`
（drums recall 0.657、precision 0.718 全表最高）。

→ 官方谱的形态是 **"鼓骨架 + 大量非鼓的填充与装饰"**，**没有任何一段是"只踩一条轨"**。
模型改成：**`skeleton`（骨架轨）+ `accent[]`（点缀轨）+ `share`（估计占比，含 `free` 残差）+ `evidence`（依据）**。
🧪 **v1.3：这个换法被 n=40 确认是对的** —— 骨架模型（85.2%）**第一次超过常数基线**（84.4%），
赢得最多的类型正是 C3 针对的 `intro`（0.692 vs 0.615）；而 v1.1 的单值规则表在 40 首上仍然只有 62.4%
（输得最惨的是 `chorus` 0.405，因为规则 4 无条件答 `vocals`，而 74 个副歌段里 69 段实测是 drums）。
⚠️ 唯一明显输给常数基线的是 `outro`（0.571 vs 0.714，只有 7 段）→ 规则 8 标存疑（R12）。
🧪 **v1.4：R12 升级为"确认的缺陷"** —— n=160 的 **32 段**上它**仍然输 9.4 个百分点**
（0.719 vs 0.813），不再是样本量问题。**换成什么还没有证据**（32 段只有 273 个事件），
所以 `stemplan.py` 的**行为不动**，只把判定写进注释与 `suspect` 提示。
🧪 **v1.4：`intro` 仍然是骨架模型赢得最多的类型（+7.7 pt，0.768 vs 0.690，与 n=40 的 +7.7 pt 一模一样）**
→ C3「判据从能量占比换成 onset 匹配倾向」从"已验证"升级为"稳定复现"。

> ⚠️ **推理期没有谱面**：下表所有"实测/lift"只是**规则的设计依据**，运行时的判据一律是纯音频特征
> （`n_onset_*`、`grid_fit_bar_*`、`voiced_ratio`、`share_*`、`riff_sim_*`）。
> 定义**匹配倾向** `tendency_s = n_onset_s × grid_fit_bar_s`（= 该轨每小节能提供多少个"落得上格"的可踩音）。

| # | 段落类型 | 判据（纯音频） | 骨架 | 点缀 | 相对 v1.1 的变化 / 出处 |
|---|---|---|---|---|---|
| 1 | `intro` | `argmax_s tendency_s` | **匹配倾向最高轨**（通常仍是 drums） | 次高倾向轨 | **判据从能量占比换成匹配倾向**（C3：intro 的最高 lift 是 `other` 5.08 > drums 4.39，"哪个响踩哪个"的能量口径选错轨）/ 5.4-1 |
| 2 | `verse` | — | **`drums`** | `other` ＞ `vocals`（每 4 小节末 1–2 拍） | 实测 verse 的 other lift 3.49 最高 / 5.4-2 |
| 3 | `pre_chorus` | 强度单调上升 ∧（kick↓ ∨ hihat↑ ∨ 高频↑） | **`drums`** | `other`，**最后 1–2 小节换 `vocals`** | 不变 / 5.4-3 |
| 4 | `chorus`/`final_chorus` | **人声主导** = **`share_vocals ≥ 0.14`**（v1.3 换判据） | **`drums`（不变）** | 人声主导 → **`vocals` 为主**；否则 `vocals` 只作重音点缀 | **条件触发 + 存疑**（C1，见下）/ 5.4-4 |
| 5 | `interlude` | `voiced_ratio ∈ [0.12, 0.55]` ∧ `n_onset_vocals ≥ 1`/小节 | **`drums`** | **有人声采样 → `vocals` 优先** | **加强**（C4）；v1.3 修正表述见下 / 5.4-5 |
| 6 | 休息段 / `quiet_chorus` | 位于两个高强度段之间 ∧ `I_bar < P40` | **`drums`** | 非鼓里倾向最高者 | **v1.3 可以定论了**（C5：n=40 有 63 段，`quiet_chorus` 的 drums recall **0.666 为全表最高**）——落ちサビ**仍以鼓为骨架**，不是"改踩次响轨"；密度降一档但不清零（知识 031）/ 5.4-6 |
| 7 | 第 N 次副歌 | `repeat_of` 指向同为副歌家族的段 | **与被复制段相同** | 与被复制段相同 | `upgrade` 置位：靠配置升级不靠加密 / 5.4-7 |
| 8 | `outro` | — | **与 `intro` 相同** | 与 `intro` 相同 | ⚠️ 不等于减压（知识 031 末段最强）/ 5.4-8 |

**C1「副歌全踩人声」的裁定（重要；v1.3 按 n=40 重写）**

| 口径 | n=8 | **n=40** |
|---|---|---|
| vocals 命中率最高的副歌段 | 2 / 14（14.3%） | **4 / 88（4.5%）** |
| 只看**人声曲**的副歌段 | —（只有 1 首正例） | **4 / 66（6.1%）** |
| 副歌 drums recall vs vocals recall | 0.583 vs 0.200 | **0.649 vs 0.244** |
| `final_chorus` 的 vocals recall | **0.065**（= 随机） | **0.199**（与普通副歌持平） |
| 段内 `vocals lift > drums lift` 的副歌段 | — | **25 / 88（28%）** |

🧪 **v1.4（n=160，261 个副歌段）**：

| 口径 | n=40 | **n=160** |
|---|---|---|
| vocals 命中率最高的副歌段 | 4/88（4.5%） | **7/261（2.7%）** |
| 副歌 drums recall vs vocals recall | 0.649 / 0.244 | **0.692 / 0.270** |
| `final_chorus` 的 vocals recall | 0.199 | **0.267**（与普通副歌 0.270 持平，v1.3 的推翻被再次确认） |
| 段内 `vocals lift > drums lift` 的副歌段 | 25/88（28%） | **95/261（36.4%）** |

→ **「副歌全踩人声」作为无条件规则第三次被否（14.3% → 4.5% → 2.7%）**；
但**「人声作为重音点缀」的宽松口径信号反而更强了（28% → 36.4%）** ——
正是现行工具（骨架 drums + 点缀 vocals）在做的事。
⚠️ **口径说明**：n=160 的 261 个副歌段**全部落在人声曲上**（器乐曲 0 段），
因为管线对判为器乐向的曲子改用 `drop` / `interlude` 词表、不产出 `chorus`。

- **n=40 把它按得更死**：比例从 14% 掉到 4.5%，**而且限定在人声曲上也只有 6.1%** ——
  n=8 那个"只在人声主导曲上成立"的降级式辩解**没有被支持**。
- 🧪 **v1.3 推翻 v1.2 的一条子结论**：`final_chorus` 的 vocals lift 0.97（= 随机）是 4 段样本的噪声，
  n=40 上大副歌的 vocals recall 回到 0.199。**"大副歌最不踩人声"不成立。**
- **但结论仍然只降级、不推翻**：ground truth 是"官方 note 是否落在 Demucs vocals 轨的 librosa onset
  ±30 ms 内"这个**代理**，而 n=40 找到了它的**明确失败模式**——器乐曲《超絶！Superlative》
  的 vocals 轨是纯泄漏，却拿到 0.339 的 vocals recall。代理会**制造**假的"踩人声"。
- → 规则维持**条件性 + 存疑，待人工听审 / basic-pitch 复核**；**不得当硬约束**。
- 🧪 **v1.3 换判据（R4，R8 相关）**：v1.2 的 `voiced_ratio ≥ 0.45 ∧ onset比 ≥ 0.65`
  **在 40 首上几乎不工作**——只在 42/357 段点亮、其中只有 16 段正确，**Youden J = 0.053**。
  357 个段落上以"段内 `vocals lift > drums lift`"为标签扫阈值：

  | 判据 | 最优阈值 | Youden J | AUC |
  |---|---:|---:|---:|
  | `voiced_ratio`（v1.2 第一条） | 0.918 | 0.217 | 0.635 |
  | `n_onset_vocals/n_onset_drums`（v1.2 第二条） | 0.129 | 0.174 | 0.586 |
  | **`share_vocals`（v1.3 新判据）** | **0.142** | **0.243** | **0.644** |
  | v1.2 的固定 0.45/0.65 | — | **0.053** | — |

  `0.65` 这个 onset 比阈值比实测最优（0.13）**高了 5 倍**，它当初是为了让规则在唯一已知正例上
  能触发而凑出来的。**更硬的换判据理由见 §9-A.3'：`voiced_ratio` 连"这首是不是人声曲"都分不开。**
  ⚠️ 新判据**仍然是弱预测器**（AUC 0.644、precision 0.40 vs 基线 0.289），只配给点缀轨排序。
- 🧪 **v1.4：阈值被 160 首独立复现，不动。** 在 **1401 个**段落（正例 380 = 27.1%）上重扫：

  | 判据 | 最优阈值 | Youden J | AUC |
  |---|---:|---:|---:|
  | **`share_vocals`** | **0.1424** | **0.2836** | **0.6464** |
  | **现役固定 0.14** | 0.14 | **0.2767** | — |
  | `voiced_ratio` | 0.744 | 0.2034 | 0.6146 |
  | v1.2 的旧固定 0.45/0.65 | — | **0.0091** | — |

  **n=160 扫出的最优 0.1424 与 n=40 的 0.142 撞在小数点后三位**，现役值与最优只差 0.007
  → **不换**。旧判据的 Youden 从 0.053 掉到 0.0091 —— **彻底确认它不工作。**
  ⚠️ 但 AUC 仍只有 0.646、precision 0.386（基线 0.271），**仍然是弱预测器，不得当硬约束**。

**C4「间奏踩人声采样」的表述修正（v1.3，R13）**

n=8 说"interlude 的 vocals lift 最高（3.18 > drums 3.11）"，读成了"间奏该踩人声"。
n=40 的数字是：`interlude` 的 vocals **recall 0.075 是全表最低**，
而 vocals **precision 0.673 是全表最高**。→ v1.3 的表述是"间奏里的人声采样稀少，
但一旦出现几乎必被采用"（高价值稀有资源），不是"间奏该踩人声"。

🧪 **v1.4 再下调一次（n=160，361 个间奏段）**：
`interlude` 的 vocals **recall 0.125**（仍是主要段落类型里最低，只有 `outro` 0.103 更低），
但 **precision 0.561 只排第三**（`final_chorus` 0.573、`bridge` 0.667/2 段更高）。
→ **"间奏人声稀少"成立；"一旦出现几乎必被采用"不再显著强于其它段落，这半句要去掉。**
规则本身（有人声采样时点缀优先给 vocals）仍然成立，但它不再有"高价值稀有资源"的加成。

🧪 **v1.4 推翻另外两条"全表最高"**（n=160 逐段落类型表，报告 n160 §5.5）：
① **`other` 最强的段落类型是 `intro`（0.359），不是 `pre_chorus`（0.318）** —— v1.3 说反了；
② **`quiet_chorus` 的 drums recall 0.682 略低于 `chorus` 的 0.692** ——
主张（落ちサビ仍以鼓为骨架）维持，"全表最高"的措辞去掉。
**C3 维持**：`intro` 的 drums recall **0.571 是全表最低**、`other` **0.359 是全表最高**。

**通用规则（与段落无关）**

- **切轨触发点**：点缀轨改变且持续 ≥ 2 小节 → 候选切点；`voiced_ratio` 跨越 0.5 → **强**切点。一律吸附到小节线（或 4 小节乐句边界）。
- **降级规则**：骨架轨 `n_onset_s(b) < 2` 或 `grid_fit_s(b) < 0.5` → 换成匹配倾向最高的可用轨；
  点缀轨同样过一遍可用性，全被否时兜底取一条可用的非骨架轨。
- **估计占比**：骨架 + 点缀按匹配倾向归一，整体 × (1 − 0.19)，余下 `free = 0.19` 是实测
  "什么都不落"的那一部分（n=8 实测 18.6%、**n=40 实测 16.8%**；差别无意义，常数不动）。
  ⚠️ 逐段占比**从未被验证**，只是量级提示。
- **单轨警告**（反新手护栏，知识 002/005）：若全曲「骨架 ∪ 点缀」只有 1 条轨且曲长 > 90 s → **报警**。
  ⚠️ 已知例外：MMFC 5.6 提到《金星》整张谱只踩一条音轨且评价很高 → **这是警告不是硬错误**。
- ⚠️ **不要把"骨架 = drums"读成"整段只踩鼓"**：36.2% 的官方 note 不在鼓上，这正是点缀轨与
  `free` 残差存在的意义。

### 5.3 决策步骤（规则化，可被 LLM 执行也可被程序执行）

1. **结构映射**（v1.2 改写）：按段落类型给强度基线，再与强度曲线交叉验证——
   - **`intro` / `outro`：以结构为准，密度基线由结构封顶，不跟能量曲线走**
     （§4.7：MYTHOS 前奏音频强度 ≈ 1.0，官方谱只放 4 note/小节）；
   - **其余段落：以曲线为准**，但与结构基线明显不符时**标人工复核**（不再"一律以曲线为准"）。
2. **踩音目标选择**：按 §5.2 规则表逐段定 **骨架轨 + 点缀轨 + 切点**（不是单值主踩轨）。
3. **密度分配**：强度档位 → `density_norm = floor + (1−floor)·intensity_norm`（§4.7）→ **NPS 锚**
   （定数 → NPS → × 每小节秒数，§4.7(4)）或分位映射（§4.8 B）→ 分音密度与每小节 note 数；
   总量控制在知识 004 的区间内。**密度基调按约每 12 小节一个台阶规划**（知识 031）。
   ⚠️ 建议 note/小节 是 **±3 note 的粗档**（逐小节 MAE 3.4），不是逐小节目标值。
4. **配置强度升级手段**（知识 003）：同踩音下用 note 种类（tap→hold→each→slide）、位移、错位微调强度；**第二副歌优先用配置升级而非加密**（知识 031"强度≠密度"）。
5. **休息与可玩性检查**（知识 012–015）：Hold 尾留空、叠键/外键/撞尾红线、Touch 不适用（ST 谱）。
6. **输出 song sheet 双格式**（§5.4）。

### 5.4 输出：song sheet 双格式（R18，替换 v1.0 的 charting_plan.json）

**双格式同源**：`song_sheet.md`（给 LLM 读）与 `song_analysis.json`（给程序/校验器读）由同一份数据渲染，**禁止两边手改**。

**为什么是"逐小节逐 stem 网格串"**：① 与 simai 同构——LLM 看到的 `x...x...x...x...` 和它要写的 `1,,,,5,,,,` 在时间轴上一一对应，不需要心算换算（最大收益）；② 符合 ChatMusician 的结论（文本原生 + 高压缩 + 显式重复结构）⚠️；③ 天然可视，人也能一眼复核。
（反例：LLark 把音频 embedding 直接投给 LLM ⚠️ → 与本项目 AGENT §8 路线相反，不采纳。Loop Copilot 的"Global Attribute Table"⚠️ → 对应我们的 Header + Section table，采纳。）

**四条硬约束**

- **(a) 网格分音跟随该小节的量化结论**，不能固定 16；每行必须显式写 `div=16`，否则 LLM 会按 16 分理解一个三连小节。
- **(b) 每行自包含**：`bar 号 | 段落标签 | 强度档 | div | 各轨网格串`。LLM 处理长表格时会丢上下文，绝不能靠"上一行说过"。
- **(c) 字符集与制谱直觉一致**：`x` = onset（该踩的音）、`X` = 重音/强拍、`-` = 延音持续（hold 候选）、`.` = 空。
- **(d) 轨数上限 4**：kick+snare 合并为 `drum`、`vocal`、`bass`、`other/hook`。3 分钟 / BPM 180 ≈ 135 小节 × ~100 字符 ≈ 14 K 字符，可接受；铺 8 条轨会诱导 LLM 采密（违反知识 005）。
- 另：Header 必须**显式声明"网格是候选池不是谱面"**，并给出目标 note 总数区间（知识 004）。

**样例**

```markdown
# SONG SHEET — <title>
## 0. Header
bpm: 180 (const)   offset(&first): 1.234 s   bars: 1..136   time-sig: 4/4
offset_check: φ* = +4 ms（OK, 方式 onset-fit, 搜索窗 ±0.40 拍）   bpm_drift: 1.2e-5 /s (OK)
structure: vote(...)   **音频能量高潮** bar: 96 ⚠️ 音乐最激烈处 ≠ 谱面最密处（中位误差 33 小节）
target: ST Master 定数 13.5 → note 总数 700–850（知识 004）
密度锚（主）: NPS 6.13 × 每小节秒数 1.333 → 8.17 note/小节（知识 031 §7）
密度锚（对照）: 定数→note/小节 9.15 ⚠️ 高 BPM 下该口径系统性高估
密度地板: 段落 0.60 / 小节 0.25（n=8 标定的段落最优是 0.125，样本太小未采纳）
结构封顶: intro/outro 建议密度 ≤ 4.9 note/小节（= 全曲均值 × 0.60）
分音使用统计: {8}=61%  {16}=33%  {12}=4%  {24}=2%
⚠️ 下面的网格是**候选池**，不是谱面。按知识 005「删到不能再删」筛选。
📏 官方谱只采用候选池约 **55%**（8 首实测 precision 0.546 / recall 0.821）。
⚠️ 高 BPM 下 {16}/{24}/{32} 的判定不可靠，网格串只当节奏轮廓用。

## 1. Sections
| bars   | 段落 (日/英)        | intensity | 建议密度 | 建议note/小节 | **骨架** | **点缀**    | 估计占比 | repeat_of | 备注 |
|--------|--------------------|-----------|---------|--------------|---------|-------------|---------|-----------|------|
| 1–8    | イントロ (intro)     | 0.18      | {8}     | 4.9（封顶）   | drum    | hook        | drum .55/hook .26/free .19 | — | 匹配倾向而非"哪个响" |
| 9–24   | Aメロ (verse)       | 0.42      | {8}     | 6.5          | drum    | hook＞vocal | drum .53/hook .19/vocal .09/free .19 | — | 人声长音，尾拍混 |
| 25–32  | Bメロ (pre_chorus)  | 0.31→0.78 | {8}→{16}| 7.4          | drum    | hook→vocal  | — | — | kick 在 29 小节消失；31–32 点缀切人声 |
| 33–48  | サビ (chorus #1)    | 0.91      | {16}    | 8.0          | drum    | **vocal**＞hook | drum .53/vocal .15/hook .13/free .19 | — | **人声主导**（voiced 0.82、vocals/drums onset 密度比 0.71）⚠️存疑 |
| 49–56  | 間奏 (interlude)    | 0.55      | {8}     | 6.9          | drum    | **vocal采样** | — | — | 间奏人声 precision 0.715；强度靠位移（知识 003-2） |
| 57–64  | 落ちサビ (quiet_ch.)| 0.30      | {8}     | 5.9          | drum    | hook        | — | — | **休息段** rest:true（降档不清零）⚠️存疑 |
| 65–80  | ラスサビ (chorus #2)| 1.00      | {16}    | 8.2          | drum    | **vocal**＞hook | — | 33–48 | **upgrade: 单星→双手星**；密度仅 +12% |
| 81–88  | アウトロ (outro)     | 0.55      | {8}     | 4.9（封顶）   | drum    | hook        | — | intro | 前后呼应；**不减压**，收尾只在最后 1–2 小节 |

## 2. Bars（逐小节）
bar 33 | サビ | I=0.90 | div=16 | drum  X...x...X...x...
                                | vocal .x.x..x..x.x....
                                | bass  x.......x.......
                                | hook  ................
       | note: crash@1拍(段落起点); vocal 入
```

**JSON 侧**（`song_analysis.json`，供校验器/生成器）
- `sections[]`：在 v1.0 `charting_plan` 字段基础上增加 `chorus_index` / `repeat_of` / `upgrade` / `rest` / `labels_ja`，
  **v1.2 起增加 `skeleton_stem` / `accent_stems[]` / `accent_share{}`（含 `free`）/ `plan_evidence[]`**；
  `primary_stem` / `secondary_stem` 保留为**已弃用的兼容字段**（= 骨架 / 第一条点缀）；
- 新增 `bars[]`：`{bar, div, intensity, onsets: {drum:[idx...], vocal:[...], bass:[...], hook:[...]}, notes:[...]}`；
- 🧪 原型 v0.1 已产出的 `analysis.json` 字段（`offset_check` / `quantize_stats` / `structure.alt_segments` / `timings_sec` / `tools` 等）保留，作为可复核证据链。

## 6. 与项目现有资产的对接

- **知识库**：001（强度贴合情绪，**已于 2026-09-11 修订**）、002（切轨）、003（配置强度四因素）、004（难度 NPS 校准）、005（采音要简）、012–015（可玩性约束）、016（ST 要素黑白名单）、**031（官方谱密度曲线实测规律，本管线 §4.7 的全部数字出处）**。
- **数据**：`resource/official-chart/` 388 个官方 ST 谱（定数 13.0–14.5）——已用于 §4.8(A)(C) 的无音频标定；
  **`official-mp3/` 8 首官方 ST 曲包**（用户 2026-09-11 提供，音频与 maidata **均不入库**）——已用于 §4.8(E) 的配对标定。
- **工具**：`tools/chart_analysis/`（simai 解析器 + 密度统计，是官方谱侧 `F_chart` 的来源）、`tools/audio_analysis/`（本管线 L1–L3 的原型实现，见 §11）、**`tools/calibration/`**（音频×官方谱配对标定：对齐/相关/峰值/地板拟合/切轨命中/权重 LOSO/结尾形态，产出 `docs/research/audio-chart-calibration.md`）。
- **预览**：本地预览库（http://127.0.0.1:8099/list.html）用于结构/强度标注的可视化复核。

## 7. 工具选型总表、许可证策略与 prior art

### 7.1 四层主线（全 MIT/Apache/ISC）

| 层 | 主线 | 许可 | 备选/打底 | 排除（理由） |
|----|------|------|-----------|-------------|
| L1 节拍 | **beat_this**（MPS 走 Python API） | MIT ✅ | librosa 1.0；all-in-one-infer | madmom（模型 NC + py3.12 装不上）、essentia/tempo-cnn（AGPL） |
| L2 分离 | **Demucs v4 htdemucs / htdemucs_ft** | MIT ✅ | demucs-mlx / mlx-audio-separator（MIT） | Spleeter、BS-RoFormer（权重不明）、AudioSep、umxl（NC） |
| L2 人声 note | **basic-pitch** ⚠️ 🧪 **当前装不上，暂缺席** | Apache-2.0 ✅ | RMVPE（Apache-2.0）；**当前降级 = vocals stem onset** | CREPE（慢）、Melodia（AGPL）、pYIN（GPL） |
| L2 鼓件 | **三频带启发式（零模型）** | — | drumsep（代码 MIT，权重待核 ❓） | **ADTOF（CC BY-NC-SA ✅）、LarsNet（权重 NC ⚠️）、omnizart（连坐 madmom ✅）全部排除** |
| L3 结构 | **`all-in-one-infer` 3.1.0**（PyPI 名，MIT ✅，py3.12 ✅，natten 可选，🧪 可直接吃已分离 stems） | MIT ✅ | 自研 SSM 退路（必须有）；SongFormer（非商用分支） | 原版 `allin1`（Darwin 强制依赖 natten；import 需 madmom）、MERT/music2vec（NC）、DeepChorus（无 LICENSE）、msaf（低维护） |
| L3/L4 强度 | **librosa + pyloudnorm 配方**（§4.5） | ISC ✅ | — | **pop-music-highlighter（GPL-3.0 ✅）**、madmom features（NC）、pychorus（弃用） |

### 7.2 许可证策略（开源项目红线）

- 默认依赖只允许 **MIT / Apache-2.0 / ISC / BSD**；**AGPL/GPL 与 CC BY-NC(-SA) 权重一律不进默认管线**（可作为个人非商用实验的 optional 插件）。
- **❓ 待用户决策：CC BY 4.0 是否纳入白名单**（影响 SongFormer 代码/权重）。注意即便放开，SongFormer 的前端 **MuQ 权重仍是 CC BY-NC-4.0** ✅，所以它**只能进个人非商用分支**；其另一半前端 MusicFM 是 MIT+Apache-2.0 ✅。
- **madmom-infer 运行时下载风险** ✅：`all-in-one-infer` → `madmom-infer`（BSD-2 ✅），后者 `models.py` 声明会运行时从 `CPJKU/madmom_models` 下载 **CC BY-NC-SA** 权重。🧪 **实测已跑一次并检查 `~/.cache/madmom_infer/models` 为空**（all-in-one 只用纯 HMM 的 DBN 解码器）→ 当前未触及 NC 权重；**对外发布前仍需复核一次**，若非空则只能走 `--no-allin1` 的纯 SSM 路线。
- 待复核项 ❓：MDX23C/Kim_Vocal_2 权重（分发源无 LICENSE）、drumsep 权重、miaChartGen2 的 HF 权重。
- 已明确排除并记录：**ADTOF（CC BY-NC-SA 4.0 ✅）、LarsNet（`license: None` ✅ / README 称 NC ⚠️）、omnizart（requires_dist 含 madmom ✅）、pop-music-highlighter（GPL-3.0 ✅）**。

### 7.3 本机环境速查（M4 / 16 GB / macOS 26 / py3.12 / torch 2.14）

🧪 实测装上且可用：`librosa 1.0.0` / `torch 2.14.0`（MPS 可用）/ `demucs 4.1.0` / `all-in-one-infer 3.1.0` / `natten`（源码编译通过）/ `pyloudnorm` / `matplotlib` / `scipy` / `numpy 2.5.3` / `pytest`。
🧪 实测装不上：`basic-pitch[onnx]`（pip `resolution-too-deep`）；`allin1` 1.1.0（装上但 import 失败）。
⚠️ 其他一手事实：`librosa 1.0` requires_python ≥ 3.12 且已移除 `beat.tempo` ✅；`torchaudio ≥ 2.11` 解 mp3 需 `torchcodec` ✅（已被 §2.1 统一转 WAV 绕开）。

### 7.4 prior art

- **Goldgom/miaChartGen2**（gitcode 镜像存活；GitHub 原仓已 404）：检索到的**唯一 maimai 专用"MP3→simai"生成式项目**。EnCodec 24 kHz token + BeatTokenizer（librosa/beat_this 双后端）+ simai tokenizer → 75 Hz 帧网格 → **5-Stage 级联 Transformer**（骨架 → Hold 时长 → Slide 路径 → Break → EX）→ 密度/类型偏置采样。**无 LICENSE、README 声明"仅学术/个人学习"、不含 checkpoint** → 只借鉴设计思路，不取代码。
- **DDC**（MIT）：⚠️ **事实更正（R22）——DDC 不做音乐量化**（伪造 BPM=125 让 1/192 小节 = 10 ms 帧），它**不是 snap 逻辑的参照**；可抄的是 `BeatCalc`/`abstime` 的时间轴换算结构。
- **Yi 2023**（arXiv 2311.13687）：1/48 拍 beat-aligned 频谱、每 2 拍 96 位置 token、osu!mania 14648 谱 / 3166 曲、micro-F1 84.6% ✅ → **本项目"拍同步重采样"的直接依据**（§2.5 Step 1）。
- 其他参照：ARG/softchart-v15（MIT，7.94M 小模型）、TaikoNation（乐句模式=结构分段）、audio2chart、Mug-Diffusion、ChartGenEval；LLM 侧 ChatMusician / LLark / Loop Copilot / MusicAgent（§5.4 已引）。
- **我们的差异化点**（经全库检索确认）：现有开源项目**没有任何一个做"显式声源分离 → 分轨踩音"**；结构分段（日式双标签 + chorus_index）+ 强度曲线 + 官方谱密度先验 + 知识库规则校验是四层相对 prior art 的增量。

## 8. 技术路线决策：显式特征分析 vs 端到端（2026-09-10，用户决策，v1.1 未变更）

- **用户判断**："全频谱直接进 transformer 有点扯；对 mp3 进行分析，寻找采音、分段以及情绪强度才是正确的路径。"
- **决策**：**特征分析先行、生成器后置**——L1–L3 确定性提取，L4 规则+规划（注入知识库硬约束），生成端只消费结构化特征；不采用 EnCodec 全频谱 token 端到端路线。
- **理由**：① 硬约束可保证（零报错/可玩性/无理红线必须显式校验）；② 可解释可复核（每层输出可人工查看修正）；③ 数据效率（端到端需海量谱面，本项目仅 388 官方 ST 谱）；④ 幻觉可控（LLM 只接触结构化特征）；⑤ 差异化（无人做分轨踩音）。
- **保留的开放点**：生成端可采用"以显式特征为条件的小型生成模型"（借鉴 miaChartGen2 的分阶段骨架→属性设计，输入换成我们的特征），仍属显式分析路线，并保留规则硬校验兜底。

## 9. 存疑 / 待定（v1.4 刷新）

> 已解决项已从 v1.0 清单删除：域外数据"无处可寻"（→ §10 已有 RWC-Pop + osu2beat2025）、CPU/设备 RTF 未知（→ §1 已有实测）、NATTEN 自编译坑（→ 实测编译通过）、madmom NC 权重风险（→ 实测缓存为空）。

**A. 最不可靠的四个环节（🧪 原型实测，按可靠性由低到高）**

1. **高 BPM 下的分音选择近乎随机**：192 BPM 时 24 分与 32 分格子只差 ~13 ms，远小于容差 → 16/24/32 之间基本是随机挑；`unresolved_bars=0` 不代表量化准确（§2.5.3）。这是全管线第一不可靠环节。
2. **结构标签必须人工对图复核**：两条分段路径都会退化（all-in-one 曾只给 3 段、最长段占 63%；SSM 第一版把 56 个小节并成一段），已加"段数 ≥5 且最长段 ≤35%"体检 + SSM 自适应重试兜底（§4.2）。
3. **人声 VAD 受 synth 泄漏影响**：Demucs vocals 轨常混进 lead synth，器乐段被误判为有人声（§3.4）。
   🧪 **v1.3 把它从"第三不可靠"提到与 A.1 并列 —— 它是一个坏掉的探测器，不只是有噪声（R8）**：
   `vocal_activity` 用**相对 dB 阈值**，vocals 轨近乎静音时底噪也会超阈，于是
   **器乐曲《超絶！Superlative》拿到 `voiced_ratio = 1.000`**（它的 vocals 能量占比只有 0.003）。
   40 首上的交叉表：用能量占比判出 12 首器乐曲，VAD **把其中 10 首判成人声曲，一首都没判反方向**。
   🧪 **v1.4（n=160）：51 首器乐曲里 VAD 判错 31 首（61%），仍然一首都没判反方向** ——
   比例下降但性质不变，**R8（加绝对能量门）仍然该做**。
   → ① `voiced_ratio` **不能当"这首是不是人声曲"的判据**（§5.2 规则 4 已换成 `share_vocals`）；
   ② VAD 必须加**绝对能量门**；③ §4.5 的 `voiced` 项单项 ρ 只有 0.088，
   与这个故障是同一回事（权重已被标定判为 0）。
4. **鼓件分解是频带启发式不是转录**：密集段里 kick 与 hihat 的网格串常常完全一样（§3.3）。
5. **（v1.2 新增，v1.3 加重）人声侧的 ground truth 只有代理，需人工听审**：§5.2 C1「副歌全踩人声」
   被降级为条件性，靠的是"官方 note 是否落在 vocals 轨 onset ±30 ms 内"这个代理判据 ——
   而 A.3 已把 vocals 轨的 synth 泄漏列为最不可靠环节之一，人声 onset 数本身还构成命中率天花板。
   **v1.3 找到了这个代理的明确失败模式**：器乐曲《超絶！Superlative》的 vocals 轨是纯泄漏，
   却拿到 **vocals recall 0.339**——代理会**制造**假的"谱师踩了人声"。
   **这条结论必须由人工听审复核**，或等 basic-pitch / RMVPE 装上后拿真正的人声 note 事件复验。
   → **v1.3 的判断：下一轮最该补的不是更多曲子，是更好的人声 ground truth**
   （n=40 已经把"密度侧"的样本量问题解决了；卡住的全部是人声侧代理）。
   🧪 **v1.4：这条判断被 n=160 完全证实，而且失败模式更严重。**
   加 120 首之后，密度侧的数字（ρ、权重、地板、锚点、归因、结尾）**全部稳住**，
   **人声侧一条都没解决**；器乐曲《疾走あんさんぶる》的 vocals 轨拿到
   **recall 0.579 —— 160 首里最高的一首**，比《超絶！Superlative》的 0.339 还离谱。
   160 首里 vocals 天花板 ≥ 0.7 的 12 首中有 **4 首是器乐曲**。
   **「再加曲子」这条路已经走到头，下一轮必须是人声 ground truth。**
5'. **（v1.3 新增，🧪 v1.4 大幅下修）强度管线对人声曲差三成**：v1.3 报 器乐 0.399 / 人声 0.146
   （2.73×），**n=160 实测是 0.4196 / 0.2823（51 vs 109 首，1.49×）**。
   n160 §2.2 的 2×2 分解显示：v1.3 那个倍数被**"平滑口径"与"v0.3 权重"双重放大**
   （同样 40 首、同样 v0.3 权重，换成不平滑口径后就只剩 1.81×；再换成 v0.4 权重剩 1.38×）。
   机理（人声曲密度跟唱词句读走，而句读不在响度也不在谱通量里）**仍然成立**，
   但**"几乎不管用"的说法必须撤回**，它**不再是与人声 ground truth 并列的瓶颈**，
   只是一个约三成的系统性劣势。

**B. 未标定 / 未实现**

6. **强度融合权重已换成标定值（v1.3），🧪 v1.4 复验后不再换**：`w=[0.00,0.19,0.07,0.00,0.74]`。
   40 首 LOSO 三条判据全过（+0.076、32/40 折、p=0.0002，§4.5）。**H1/H2 均被证实**。
   **n=160 重标定得到 `[0,0.168,0.113,0,0.719]`，与现役几乎重合；LOSO 160 折对照现役默认是
   −0.0015 / 82 折（51.3%）/ p = 0.81 → 三条判据全不过，维持。**
   对照 v0.3 初值仍然全过（+0.0617 / 124 折 / p ≈ 0）→ **v1.3 那次替换被四倍样本确认正确**。
   ⚠️ **但标定目标只有密度**：`I_bar` 还被 §4.6 高潮五票与段落强度分档消费，
   那两处**从未标定**，把 `loudness`/`voiced` 归零对它们的影响 ❓ **未测**。
   投票权重 `[0.45,0.25,0.20,0.05,0.05]` **仍然完全未标定**。
   (D) 人工听审 ❓ 仍未做，目标值已二次下调到逐段 ρ≈0.3（器乐 ~0.47 / 人声 ~0.25）。
7. **（v1.2 引入，v1.3 修正）几个阈值的依据强度** ：
   - `intro/outro` 结构封顶比例 **0.60 → 0.75** ✅ 已按 n=40 修正（实测 intro/全曲密度比中位 0.726）；
   - 「人声主导」判据 **0.45/0.65 → `share_vocals ≥ 0.14`** ✅ 已按 n=40 换判据
     （旧判据 Youden J 只有 0.053），但新判据 **AUC 0.644 仍属弱预测器** ❓；
   - 段落 floor 的 0.60↔0.125 **分歧解除** ✅：n=40 池化最优 0.365，0.125 是 n=8 噪声，默认 0.60 不动；
   - **（v1.3 新增）曲目类型分层的分界 `share_vocals ≥ 0.10`** ❓：音频侧代理，已知 1 处错分
     （《僕は空気が嫁ない》），而"器乐 vs 人声"这个核心分层结论对它敏感。
   - 🧪 **v1.4：`share_vocals ≥ 0.14` 的阈值被 160 首独立复现**（1401 段扫出的 Youden 最优
     **0.1424**，与 n=40 的 0.142 撞在小数点后三位），**不换**；但 AUC 仍只有 0.646，
     **依旧是弱预测器**。
   - 🧪 **v1.4：`INTRO_OUTRO_CAP_RATIO = 0.75` 与 `BAR_DENSITY_FLOOR = 0.25` 双双确认**；
     ⚠️ **段落 floor 默认 0.60 的 SSE 代价从 3.7% 涨到 9.5%**（n=160 池化最优 0.330），
     仍不改（388 谱大样本优先），但下一轮若再涨应重新裁定。
   - 🧪 **v1.4 新增待办：`stemplan` 的 `outro` 规则 8 已确认劣于常数基线**（32 段 −9.4 pt），
     但**换成什么还没有证据**；`drop` 段落类型（n=160 有 81 段）**规则表里没有对应行**。
8. 量化容差三参数（`0.25 / 12 ms / 30 ms`）与三连阈 `0.7` ❓ → 在 osu2beat2025 子集上标定。
9. swing 在 160–220 BPM 音游曲里到底出不出现 ❓ → 默认关闭；先统计目标曲库 offbeat 相位分布。
10. 强度配方 v2（§4.5 融合式）与 §4.7 的密度映射 **已在 v0.2/v0.3 实现**；
   **§4.8(B) 的分位映射 `F_chart⁻¹∘F_audio` 仍未实现**（当前只有地板线性映射 + NPS 锚）；
   L4 规划层的其余部分未实现（§11）。

**C. 域与数据**

11. **域外泛化**：所有模型基准都是西方流行乐；🧪 **v1.4 已跑 160 首官方日系曲 + 3 首测试曲**
   （定数 13.0–14.5、BPM **79–266**，官方 `&genre` 六类各 11–40 首：
   maimai 40 / niconico＆ボーカロイド 40 / ゲーム＆バラエティ 34 / 東方Project 22 /
   オンゲキ＆CHUNITHM 13 / POPS＆アニメ 11）。
   **样本量这一侧已经彻底够了**——从 40 到 160 没有改变任何一个默认值；
   剩下的瓶颈不是样本量（见 A.5）。
12. **器乐曲（无副歌）**："副歌=重复最多段"失效，高潮 fallback 为能量峰值（能量票权重升到 0.5）。
   🧪 v1.3 补充：**器乐曲反而是强度管线表现最好的一类**（逐小节 ρ 0.399 vs 人声曲 0.146）。
13. **官方 maimai 音频**：🧪 **已有 160 首**（用户 2026-09-11 分两批提供 40 首、
   2026-09-13 经 AWMC-MaiArchive 再提供 120 首，`official-mp3/`，不入库），
   配对标定 §4.8(E) **已完成三轮**（n=8 → n=40 → n=160），
   报告 `docs/research/audio-chart-calibration-n160.md`。
   n=160 足以**确认**默认权重与阈值（本轮一个都没改）；
   **仍未覆盖 12 级及以下、DX 谱、MASTER 之外的难度**。
14. **密度先验的样本边界**：知识 031 只覆盖 ST 谱定数 13.0–14.5，12 级及以下与 DX 谱外推需谨慎；
   🧪 v1.3：n=8 曾记录"配对曲的密度分位比全库更尖"（峰归一 p10/p50/p90 = 0.155/0.454/0.678
   vs 全库 0.264/0.514/0.730），**n=40 上这个偏差明显收窄**（峰归一的 I 五分位画像
   0.412/0.502/0.518/0.522/0.551 已回到平坦）→ 那也是 n=8 的样本偏差，**知识 031 不需要修订**。

**D. 许可与工程**

13. **CC BY 4.0 是否纳入白名单** ❓ **需用户决策**（影响 SongFormer 能否进管线；即便放开其 MuQ 权重仍是 NC）。
14. drumsep 权重许可 ❓；MDX23C/Kim_Vocal_2 权重 ❓；miaChartGen2 HF 权重 ❓。
15. basic-pitch 装不上的问题需解决（或换 RMVPE）；三后端数值一致性 ❓。
16. SongFormer 能否在 M4 MPS/CPU 上跑 ❓（官方只给 cuda 示例）。
17. 变速（`--bpm-changes`）仍是实验性：网格只在小节边界换 BPM，变速点不落小节线则其后全错 ❓。
   🧪 **v1.4 把它的优先级上调**：变速曲从 n=40 的 3/40 涨到 **n=160 的 11/160（6.9%）**，
   其中《東方妖々夢 ～the maximum moving about～》自带**逐小节 tempo map（117 段变速）**。
   标定脚本的**小节级等效 BPM 前处理**能把小节边界对齐到 0.002 ms（160/160），
   但它**仍然不是管线功能**，而且**小节内部的分音判定仍然是错的**
   —— 对 東方妖々夢 而言等于全曲的分音都不可信。
18. 🧪 **v1.4 新增：基准 BPM 不能取 `&wholebpm`**，要取**谱面正文首个 `(BPM)`**。
   160 首里有 3 首两者不一致，其中《ウッーウッーウマウマ(ﾟ∀ﾟ)》的 `&wholebpm=164` 而正文是
   `164.73`，按 164 跑会让 66 个小节只有 6 个对得上（对齐误差 434 ms）。
   管线应在两者不一致时告警 ❓ **未实现**。

## 10. 评测数据与协议（v1.1 新增，R23）

> 原则重申：**谱面设计参考只用 `resource/official-chart/` 的 388 个官方 ST 谱**。下面的外部数据集**只用于音频分析层（节拍/offset/分段/副歌）的工程验证**，不作为谱面设计基准。

### 10.1 RWC-Pop（2026-02 起已开放在线下载）

- AIST 官网 ✅：**"The RWC Music Database has been available for online download since February 2026."**，指向 `https://zenodo.org/communities/rwc-music/`（DOI 10.5281/zenodo.18656623，**CC BY-NC 4.0**，`RWC-P.zip` 4.07 GB）；标注在 `github.com/rwc-music/rwc-annotations`。
- 内容 ✅：Popular 100 首 = **日语歌词的日本流行乐 80 首** + 英语 20 首；AIST Annotation 提供 **beat structure、melody line、chorus sections、结构段落标签**（intro / verse A / verse B / pre-chorus / chorus A / chorus B / post-chorus / bridge / ending）。
- **这是目前唯一一个"日语流行 + 结构/副歌/节拍标注 + 音频可合法获取"的数据集**，直接解决 v1.0 §9.1 的"域外泛化未知"。
- ⚠️ 限制：CC BY-NC 4.0 → **不得入仓库、不得商用、不得再分发**；本机评测用途没问题。
- 对标基线：RefraiD 在 RWC-Pop 上 100 中 80 正确、F=0.938 ⚠️。

### 10.2 osu2beat2025（Osu2MIR, ISMIR 2025 LBD）

- 仓库 `ziyunliu4444/osu2mir` ✅（无 LICENSE 字段 ❓；论文 CC BY 4.0）。
- 内容 ✅：**741 条用户标注 / 708 首不同音频**的 metered beats（含 downbeat），文件名带 MD5 + BeatmapSetID；**音频不随附**，需自行从 `osu.ppy.sh/beatmapsets/<id>` 下载 `.osz` 解出 mp3 并校验 MD5（合法免费）。
- **曲风正是本项目的域**（anime、Vocaloid、video game music）⚠️。
- 质量筛选经验（可直接复用）✅：只用"单个未继承 timing point"或"多点间隔 ≥ 5 s"的图。
- 用途限定：验证 §2.4 的 offset 相位搜索、BPM 漂移检测、§2.5 的量化残差、beat F1。**不涉及谱面设计**（osu! 谱是社区自制谱）。

### 10.3 三档评测协议

| 档 | 目的 | 数据 | 指标 |
|---|---|---|---|
| **A** 日语结构/副歌 | 验证分段与"副歌=重复最多段" | RWC-Pop 80 首日语（本机，NC） | 边界 HR.5F / HR3F、chorus 段 IoU、副歌命中率（对标 RefraiD 80/100） |
| **B** 域内节拍/offset/量化 | 验证 §2.4 相位搜索与 §2.5 量化 | osu2beat2025 的"单 timing point"子集 | `|φ*|` 分布、BPM 漂移估计误差、beat F1(70 ms)、量化残差分布 |
| **C** 谱面密度基准 | 难度/强度校准 | **仅 388 官方 ST 谱**（无音频） | `T_density`、分位映射表、chorus/verse 比值（§4.8 A/B/C）——**已完成，见知识 031** |

### 10.4 明确不投入的数据

- **BOF / BMS 曲包**：① 逐年逐作者授权条款不一致 ❓；② 谱面是社区自制谱 → 按用户规矩不作谱面参考；③ 音频侧价值已被 osu2beat2025 更干净地覆盖。→ **不投入**。
- **Harmonix Set**：不含音频，只能当标签体系参考（MIT ✅）。**SALAMI**：曲风杂、音频需自取，优先级低。**SongFormBench**：CC BY 4.0 ✅ 但**无日语**（语言字段 `["en","zh"]` ✅）。

## 11. 当前实现状态（v1.4 刷新）

### 11.1 `tools/audio_analysis`（原型 **v0.5**，2026-09-12；v0.1 见 note 035、v0.2 见 note 038、v0.3 见 note 041、v0.4 见 note 042、v0.5 见 note 043；**n=160 复验见 note 044**）

用法：`python -m tools.audio_analysis --audio <mp3> --bpm <BPM> --first <秒> [--level 13.5] --out out/<song>/`
输出：`track.44k.wav` / `stems/*.wav` / `song_analysis.json` / `song_sheet.md` / `plot.png`（旧名 `analysis.json` / `song-sheet.md` 同时写出保持兼容）。
测试：`tests/test_audio_analysis.py` **111 用例** + `tests/test_calibration.py` **73 用例**（全仓 `pytest tests/` **226 通过**），**全部用 numpy 合成音频**（仓库不放真实音频，AGENT 准则 5）。

**已实现**

| 模块 | 对应本文 | 状态 |
|---|---|---|
| `decode.py` | §2.1 统一解码 + ffprobe `start_time` | ✅ |
| `grid.py` | §2.2 网格构造（含负 first、实验性变速）+ §2.4 offset 校验（onset-fit，整拍歧义分组取峰，**v0.3 修掉 ±半拍反拍假警报 + 四档判词**） | ✅ |
| `stems.py` | §3.1 Demucs 四轨（MPS 优先、失败回退 CPU） | ✅ |
| `onsets.py` | §3.2 逐 stem onset + §3.3 鼓件频带启发式 + §3.4 人声 VAD | ✅（零模型启发式） |
| `quantize.py` | §2.5 全套：1/48 拍重采样、**每小节单一分音**、`τ=clamp(0.25·slot,12,30)`、三连门、**`{32}` 红线**、未落格 onset 如实统计 | ✅（实测未落格 7–19%，见下） |
| `features.py` | §3.2 逐小节特征清单（share / n_onset / grid_fit / voiced_ratio / kick-snare-hihat / riff_sim / sil_run） | ✅ |
| `structure.py` | §4.2 三路边界投票（all-in-one-infer + novelty + 重复段，吸附小节线）+ §4.3 日式(英文)双标签 + §4.4 `pre_chorus` 派生 + `chorus_index` / `repeat_of` / `upgrade` / `rest` / `final_chorus` / `quiet_chorus` / 器乐向 `drop` | ✅（标签须人工对图复核） |
| `intensity.py` | §4.5 五项融合式 + §4.6 五票（v0.3 正名 `audio_climax_bar`）+ §4.7 密度地板映射（v0.3：NPS 主锚 + `--section-floor`/`--bar-floor` + intro/outro 结构封顶） | ✅ **v0.4：权重换成 n=40 标定值 `[0,0.19,0.07,0,0.74]`；`CALIBRATED_SECTION_FLOOR` 0.125→0.365；`INTRO_OUTRO_CAP_RATIO` 0.60→0.75**。🧪 **n=160 复验：权重、0.75、`BAR_DENSITY_FLOOR=0.25` 全部维持；`CALIBRATED_SECTION_FLOOR` 0.365→0.330（记录用）** |
| `stemplan.py` | §5.2 v0.3 骨架轨 + 点缀轨 + 估计占比 + 依据 + 降级 / 单轨警告 | ✅ **v0.4：人声主导判据从 `voiced≥0.45 ∧ onset比≥0.65` 换成 `share_vocals ≥ 0.14`**（旧判据 n=40 的 Youden J 只有 0.053）；新判据 AUC 0.644，**仍存疑**。🧪 **n=160：阈值 0.14 被复现（最优 0.1424）不换；规则 8（`outro`）确认劣于常数基线 9.4 pt，注释已改、行为不动** |
| `tracks.py` / `sheet.py` | §5.4 song sheet 双格式：4 轨 `drum/vocal/bass/hook`、字符集 `X x - .`、行自包含、Header 硬约束与分音统计 | ✅ |

**v0.2 实测（3 首测试曲，BPM 148–192）**：`{32}`/`{24}` 从输出中消失（红线拦下 9–21 小节/曲）；分音以 `{16}` 为主（92–100%）；**未落格 onset 6.9% / 13.1% / 18.6%**——v0.1 报的"全部可解"是容差过宽造成的假象；78–87% 的小节走"找不到合法分音、压到上限"分支，因为四轨并集每小节中位 15–22 个 onset、非鼓轨定位误差 15–33 ms（上游解法是 basic-pitch 给出人声 note 事件，不是放宽容差）。强度曲线 v0.1 的 raw/smoothed 偏离根因是**双重归一**（bar_raw 未归一、平滑后又 min-max），已修并加回归测试。

**v0.3 实测（11 首：8 首官方曲包 + 3 首测试曲，复用缓存 stems）**：
两个 offset 假警报消失（Signature −229.0 → **+0.6 ms**、麒麟 −129.8 → **+10.6 ms**），
Mare Maris 的 +27.9 ms 仍报出但改判为"疑似 MP3 解码延迟"，其余 9 首变化 ≤ 0.4 ms；
122 段里骨架 = drums 的有 114 段（93.4%），8 段因鼓 onset 太少/不落格而降级；
副歌「人声主导」条件只在 **幻想のサテライト** 的副歌上点亮（其余 7 首一次没点）；
NPS 锚 MAPE 22.5% vs 旧 note/小节锚 29.3%（本机口径）；9/11 首触发 intro/outro 结构封顶。
明细见 `tools/audio_analysis/README.md` §5.7。

**🧪 v0.4 实测（40 首官方曲包，复用缓存 stems，RTF ≈ 0.3、34 s/首、串行 23 min/轮）**：
40/40 跑通；谱面侧 40/40 零 error；**谱面小节起点 vs 音频网格最大对齐误差 0.000 ms（40/40）**；
offset 校验 **零假警报**（`|delta| > 15 ms` 只有 2 首，均判为 MP3 解码延迟且与 φ\* 同向）；
3 首变速曲（Xevel / Jack-the-Ripper◆ / 咲キ誇レ常世ノ華）用**小节级等效 BPM** 前处理全部对齐
（后两首的变速点落在小节中间，见标定报告 n40 §1.2 与 R10）。
明细见 `tools/audio_analysis/README.md` 与 `docs/research/audio-chart-calibration-n40.md`。

**🧪 n=160 复验实跑（2026-09-13，120 首新曲 + 原 40 首重跑，M4 / MPS）**：
**160/160 跑通**（一首因 HF Hub 503 失败、重试即过）；四路管线均值 **41 s/首**、
120 首串行 **83 min**；六路分离只留 `piano` 另加 19 s/首、120 首 **37 min**；
**谱面小节起点 vs 音频网格最大对齐误差 0.002 ms（160/160）**；
谱面解析 **160/160 零 error / 零 warning**；强度复算最大绝对差 **5.0 × 10⁻⁵（160/160）**；
`out/calib/` 最终 **4.9 GB / 160 首**（均值 31 MB/首）。
**11 首变速曲**（含一首 117 段的逐小节 tempo map）用小节级等效 BPM 前处理全部对齐。
offset 校验 `|delta| > 15 ms` 的 15 首里 **14 首判为 MP3 解码延迟、1 首（火炎地獄 +45.2 ms）
建议人工复核**；`|φ*| ≤ 25 ms` 158/160。
明细见 `docs/research/audio-chart-calibration-n160.md`。

**未实现清单**

- §4.8(B) 分位映射 `F_chart⁻¹∘F_audio`（当前只有 §4.7 的地板线性映射 + NPS 锚）；(D) 人工听审校验 ❓；
  **(E) 配对标定已完成三轮（n=8 → n=40 → n=160）**；
- **basic-pitch / RMVPE 人声 note 事件（§3.5）——v1.3 判定它是下一轮的第一优先级**，
  它同时是 C1（副歌踩人声）复验与 §9-A.3 VAD 修复的前置；SongFormer 二号意见（§4.2）；
- **VAD 的绝对能量门（R8）未做** —— `voiced_ratio` 目前是个坏掉的探测器（§9-A.3）；
- **小节级等效 BPM 前处理（R10）只在标定脚本里，不是管线功能**；
- 器乐向阈值（voiced 能量占比 0.18）、chorus 护栏、器乐曲强度分档阈值只在 3 首上试过，无实证；
- v0.4 的新阈值（结构封顶 0.75、人声主导 `share_vocals ≥ 0.14`、曲目类型分界 0.10）
  有 n=40 + **n=160** 的依据（两轮扫出的最优阈值撞在小数点后三位）但**仍是弱预测器 / 有已知错分**；
- 🧪 **n=160 新增的两处缺口**：① `stemplan` 规则表**没有 `drop` 这一行**（n=160 有 81 段，走兜底）；
  ② **`piano` 尚未纳入点缀轨候选**（它的 recall 0.289 高于 `other` 0.268 与 `vocals` 0.197）；
- 🧪 **基准 BPM 应取谱面正文 `(BPM)` 而不是 `&wholebpm`**（3/160 首不一致），管线未加告警。

### 11.2 `tools/chart_analysis`（已完成，2026-09-11）

simai 解析器 + 逐小节密度统计 + 变点分段 + k-means + 五段模板检验；`validate/summary/analyze/chart` 四个子命令；`tests/test_chart_analysis.py` 42 用例。
**用途**：官方谱侧的 ground truth 提取器——§4.7 的 `T_density`、密度地板、六条密度规律、§4.8(B) 的 `F_chart` 全部由它产出。解析覆盖率 387/388 = 99.74%，与 manifest 官方 note 总数完全一致 376/387 = 97.16%（|Δ| ≤ 3 占 100%）。
