# tools/chart_check — 谱面检查器（验收工具）

一份 `maidata.txt` 进，**中文报告 + JSON** 出。它不生成谱面，只把 `AGENT.md` 的四条
验收标准拆成可逐条核对的清单：能机检的给结论，不能机检的给**复核清单**。

```
python3 -m tools.chart_check <maidata.txt> [--level 13.5]
    [--analysis song_analysis.json] [--json out.json] [--inote 5] [--quiet]
```

- `--level` 缺省时从 `&lv_N` 粗取（`13+` 记作 13.7）。
- `--analysis` 缺省时找 `maidata.txt` 同目录的 `song_analysis.json`；没有就跳过采音层。
- 退出码：有**错误**返回 1，否则 0。

## 七层与依据

| 层 | 做什么 | 依据 |
|----|--------|------|
| **语法** | `simai_parser` 严格解析 + 零报错安全子集 + ST 黑名单，逐条报错/警告（带**行列**） | `docs/simai-syntax.md` §6、`docs/simai-error-checking.md` §2/§4/§8、`docs/st-chart-elements.md` §三 |
| **手序** | `hands.assign()` → 不可行 / 无理 / 蹭 / 出张落点 / 侧边双押与引导类型 / Hold 尾 / 叠键 | `docs/hand-sequencing.md` §4，知识 008–012、015、064、077、079、080、082 |
| **配置** | `configs_hand.detect_all()` → 按小节顺序的配置构成、种类数、主料/点缀；段落分区对照 | 知识 017–063（定义）、071/073/074（对照，**agent 观察**） |
| **密度** | note 总数 / NPS / 逐小节曲线 / 五段形状 / 密度地板 / 末段形态 / 后半比前半 | 知识 004、031（§1/§2/§4/§5/§7/§10）、001 |
| **采音** | 逐段 全踩 / 舍音 / 留白 与骨架轨对照；标「该有音却整段留白」「采空音密集」 | 知识 068、081、086；三词切点复用 `calibration.sampling.THRESHOLDS` |
| **深度** | 跑在最后、只消费前五层的 stats：手序难度 / note 种类占比 / 主料构成 与**同定数档官谱分布**对照，合成一条「**写得太浅？**」复核提示 | 知识 093（+ 073 / 078 / 088） |
| **外部规则** | 换一套无理口径：**MiaCode / MaiMuriDX 的静态无理检测**（外键 / 撞尾 / 叠键），即 Visual Maimai 无理面板真正会弹的那些 | MiaCode `MURI_DETECTION_SPEC` §3–§9；`docs/simai-error-checking.md` §8.5；`docs/research/miacode-vm-research.md` §A.6 |

### 第七层：外部规则（2026-09-20 新增）

前六层用的是**本项目自己的口径**：`hands.py` 的无理按 388 官谱校准，很宽。
第七层换成**社区工具的口径**——用户把 `samples/test-01` 导进 **Visual Maimai** 后
「提示了很多无理」，而 `hands.py` 报 0，差的就是这一层。

- **实现方式是复用不是重推**：规则逐条翻译自上游 **Starrah/MaiMuriDX**
  `judge.py::StaticMuriChecker.check`（46–197 行），常量取它的 `core.py` + `config.json`；
  slide 几何 `data/slide_geometry.json`（600 形状 + 8 wifi 的 A 区进入时刻）由它的
  `slides.py::SlideInfo` 直接导出。MiaCode 的 5 类 `MuriKind` 与 MaiMuriDX 同源。
- **对齐验证**：`out/calib` **160 首官谱 + 本项目 test-01（修正前后）**逐条与
  MaiMuriDX 的静态输出比对，**命中种类与条数完全一致**。
- **只覆盖静态三类**：`SlideHeadTap`（外键）/ `TapOnSlide`（撞尾）/ `Overlap`（叠键）。
  `SlideTooFast`（内屏）与 `MultiTouch`（多押）在 spec §3 里**只有运行时分析、没有静态参考**，
  本层不复现那套 180 TPS 手势模拟 → **本层 0 命中 ≠ MiaCode 面板 0 条**。
- **级别按 spec §6 原样映射**（Muri→错误、Warning→警告），**不放宽**；
  另给一条 `MURI-TOTAL` 汇总，把官谱基线印在旁边。

**官谱基线**（`out/calib` 40 首 ST Re:MASTER，定数 13.0–14.5，同一套检测）：

```
总命中 151 条 / 40 首｜中位 3｜均值 3.8｜p90 10｜最大 21（ユビキリ）｜13 首为 0
每百 note：中位 0.365 / 均值 0.523 / 最大 3.271
```

**官谱自己也命中**，所以不是「有一条就写坏了」；但**生成端的验收目标是 0**，
留下来的每一条都要写明理由（test-01 修正前 24 条 = 每百 note 2.61，比 40 首里最差的还多）。

### 本轮新增的两组提示（2026-09-20）

- **密度层 `DEN-NOTEMIX` / `DEN-NOTEMIX-SECTION`**（知识 088）：note 种类配比
  （slide / hold / each / break 占比）分别对照 **388 谱的定数档分布**与
  **160 首的段落级分布**（日式标签）。它补的是 `e2e-trial-01` 的缺口 **G1**——
  那一轮 note 总数与 NPS 都在区间内，却差着三倍 slide 与十倍 hold，而五层一条都没报。
- **深度层 `DEP-SHALLOW`**（知识 093）：补缺口 **G6**。⚠️ **它触发得并不稀罕**——
  五首官谱基线里 **ぽっぴっぽー（3 条）与 コトバ・カラフル（2 条）也会触发**，
  所以它**只是复核清单**：同时踩到 ≥2 条时回头看一眼，**偏离要能说出理由**，
  不是"必须改"。三个对照量里 **手序难度不是难度代理**（知识 078），
  **不得当目标优化**（会撞上知识 064 / 066）。

## 三个级别怎么读

- **错误**：必须修。语法层的错误 = 会让 SimaiSharp / MajdataEdit / MiaCode 报错或
  ST 谱不该出现的要素；手序层的错误 = **不可行**与**绝对级无理**。
- **警告**：要看一眼。含**硬级无理**——`docs/hand-sequencing.md` §6.2 要求
  `budget.muri`（含硬级）为 0，但 388 官谱自己也踩到几十次（知识 077：
  Hold 尾多押 74 / 超速 24 / 外键 13 / 撞尾 7），记成错误等于宣布官谱不合格。
  写谱时**硬级尽量清零**，清不掉要写明理由。
- **提示**：复核清单，**不判对错**。出张、侧边双押、软级的蹭、配置构成、密度对照、
  采音方式都在这一档。

## 两条边界（报告里也会印）

1. **外部三检待接**：`docs/simai-error-checking.md` §10-4 要求
   **SimaiSharp lint + MajdataEdit SyntaxCheck + MiaCode strict** 三方实跑，
   本机没有装这三个工具——语法层是**按它们的规则交集实现的内部校验**，
   **不能代替真机跑谱**。
2. **手序只有一套**：无理计数依赖 `hands.py` 选出的**一套**手序，而合法手序不止一套
   （知识 065）。「绝对无理 0」读作「**存在一套合法手序**」，不读作「任何手序都合法」。
   配置层与采音层的对照基准（知识 068–087）是 **agent 观察条目、未经用户确认**。

## 官方谱基线（2026-09-20）

五首官方 ST MASTER（`out/calib/<曲名>/maidata.txt`，带各自的 `song_analysis.json`）：

| 曲目 | 定数 | 语法错误 | 不可行 | 绝对无理 | 硬级无理 | 出张 ‰ | 出张/slide | 侧边双押（无引导） | 配置种类/主料 | note | NPS | slide 占比 | hold 占比 | 深度旗标 |
|------|-----:|---------:|-------:|---------:|---------:|-------:|-----------:|-------------------:|--------------:|-----:|----:|----:|----:|----:|
| ぽっぴっぽー | 13.3 | 0 | 0 | 0 | 0 | 27.6 | 0.514 | 4（0） | 11 / 6 | 507 | 5.13 | 7.3% | 7.1% | 3 |
| ダンスロボットダンス | 13.3 | 0 | 0 | 0 | 0 | 27.3 | 0.219 | 3（0） | 18 / 9 | 826 | 5.74 | 28.1% | 1.8% | 1 |
| コトバ・カラフル | 13.7 | 0 | 0 | 0 | 0 | 12.3 | 0.076 | 7（0） | 12 / 5 | 575 | 6.58 | 9.2% | 0.5% | 2 |
| 緋色のDance | 13.7 | 0 | 0 | 0 | 0 | 12.1 | 0.192 | 0（0） | 11 / 9 | 484 | 4.76 | 47.3% | 0.0% | 1 |
| ジングルベル | 14.2 | 0 | 0 | 0 | **10** | 29.0 | 0.158 | 10（0） | 22 / 10 | 737 | 7.87 | 16.8% | 8.6% | 0 |

读法：

- **语法零错、不可行 0、绝对无理 0** 在五首上全部成立——这就是检查器的合格线；
- **硬级不是零**：ジングルベル 的 10 次是 6 次 Hold 尾多押 + 4 次超速，与知识 077
  在 388 谱上的分布一致 → 硬级只能当警告；
- 出张 12–29 ‰，与知识 079 的官谱中位 21.1 ‰ 同量级；
- 侧边双押 0–10 次，**八型引导全部判得出**（无引导 0），与知识 082「388 谱 685 次里
  只剩 1 次判不出」一致；
- 配置种类 11–22 类、主料 5–10 类，与知识 073（40 类透镜下中位 15 类 / 主料中位 4 类）
  同量级，低定数谱偏少；
- **slide 占比 7.3%–47.3%、hold 占比 0.0%–8.6%**——**官谱自己的离散度就有五六倍**
  （緋色のDance 一根 hold 都没有）。所以密度层的 note 种类对照与深度层的旗标
  **都只是位置描述**，"低于 Q1"在官谱里同样常见（知识 088 / 092 / 093）。

复现：

```
for s in ぽっぴっぽー ダンスロボットダンス コトバ・カラフル 緋色のDance ジングルベル; do
  python3 -m tools.chart_check "out/calib/$s/maidata.txt" \
      --analysis "out/calib/$s/song_analysis.json" --quiet
done
```

## 单测

```
PYTHONPATH=tools python3 -m pytest tests/test_chart_check.py -q
```

素材全部是自写的合成片段（不复制官方谱原文）。容易误报的三条各配了负例：
`{8}…,{16}…` **不是**连写两个分音标记、`3<3` 绕一整圈**不是**几何违规、
星星头绝赞 `1b-5[…]` **不是**滑条绝赞。
