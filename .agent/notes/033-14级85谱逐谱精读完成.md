# 033 - 14 级谱面逐谱精读完成（实际 87 个文件）

- **日期**：2026-09-10
- **状态**：✅ 完成（进度表 86 谱 + 账外 1 谱 = **本地 14 级文件 87 个全部读完**）

## 背景

用户要求：**「你必须去读每个谱，必须逐个谱去看。先去看 14 级的这些谱子，好好理解一下配置强度与难度分布。」**
重点在"读"，不是统计检索——因为配置强度与难度分布的判断，只能来自逐谱原文。

## 做了什么

1. 按定数从 14.5 递减推进，**逐谱读完整原文**（`resource/official-chart/` 下 85 个 ST 谱面文件），
   每谱记录四要素：**配置构成 / 强度难度分布 / 可复用手法 / 存疑点**，全部追加进 `docs/research/level14-readings.md`。
2. 覆盖情况（**逐项核对后的实际数量，早期表头数字有误**）：
   **14.5（12）+ 14.4（12）+ 14.3（12）+ 14.2（15）+ 14.1（13）+ 14.0（22）= 86 谱**，
   另补读账外 1 谱（`917-東方妖々夢`，逐槽 BPM 标记特例）→ **共 87 个谱面文件**。
   其中 5 首同时读了 MAS 与 Re:MAS（503 FFT、393 Panopticon、204 System “Z”、977 ジングルベル、141 カゲロウデイズ）。
3. 每组读完追加**组小结**，并做**跨谱候选通用手法**滚动登记（累计 46+ 条）。
4. 过程中发现并修正自身错误：把 `[512:1]`/`[1024:1]` 读成"超长 hold / 缓段"，
   按项目自己的槽长公式 `[x:y] = (240/BPM/x)×y 秒` 可知 **x 越大时长越短**，
   故它们是**毫秒级"零长 hold"（断奏打点）**。已在文中逐条更正并单列「时长读法更正」一节。

## 关键结论（写进 `level14-readings.md`）

- **BPM × 分音补偿律**（六组无例外）：低 BPM 谱用高分数值（24/32/48/64/96 分）造**读谱型难度**；
  高 BPM 谱用音阶/子弹/极短星造**手速型难度**。
- **证据最强的通用装置**：自环星（10 谱）、短 hold 链 / 极短 hold 链（10+ 谱）、星链/星链条（10+ 谱）、
  双星齐奏（全区间）、轴固定螺旋（9+ 谱）、长条上打点（5 谱）。
- **数据格式边界案例**：`[200#8:1]`（BPM 限定时长）、浮点时长 + 多段变速（Halcyon）、
  单行无换行文件（FLOWER Re:MAS / Sun Dance）、`{1},{2},{4},{8},{16}` 分音连写（分からない）、
  `{128}` 极端留白（Caliburne）。

## ⚠️ 重要限定（用户指出）

本笔记与 `level14-readings.md` 中**所有「星星（slide）」相关的归纳（自环星、星链、星-单交替、双星齐奏、往返星等）都属于未定性内容**：
用户尚未讲授「如何判断星星的配置」，这些条目只能算「读到了什么」的原文记录，**不得作为制谱结论、不得写入 `.agent/knowledge/`**。
非星星类观察（分音/BPM 补偿律、长条与极短 hold、双押、子弹、跳拍、数据格式）不受此限定。

## 待办 / 未决

- **用户已决定**：跨谱通用手法先读完再一起整理；但**星星类须等用户讲授判断标准后**才能评估，故知识库整理应先做非星星部分或整体等待。
- **账目已结**：`917-東方妖々夢` 已补读（见上）；仅**定数**仍未确认（本地无等级映射），但不影响"逐谱读完"这一目标。
- `631-アージェントシンメトリー-remas.txt` 存在但未单独精读（进度表按一首计）。
- 若干装置待用户定性：自环星（起止同键）的手感与写法意图、轴固定螺旋是否归入"轴交互"、
  长星上打点的实机判定、`{96}`/`{228}` 等极细分音的解析器支持。

## 关联

- `docs/research/level14-readings.md`（主产出）
- `.agent/notes/032-配置自学-官方谱扫描.md`（同题材的统计扫描，本文是"逐谱精读"版）
- `.agent/knowledge/`（待新增：14 级通用装置条目）
