# 032 — 官方谱踩音归因实测（官方 note 落在哪条 stem 上）

- **日期**：2026-09-11
- **要点**：8 首官方 ST MASTER 谱与其官方音频配对实测——**官方谱的踩音形态是「鼓骨架 + 大量非鼓填充 + 约两成自由发挥」**：
  drums 命中率 0.607（各 stem 最高，lift 2.64），但 **36.2% 的 note 不落在鼓上**、**18.6% 什么 stem 都不落**；
  逐段"最高命中轨"85 段里 **70 段是 drums** —— **"永远答 drums"这个常数基线（82.4%）打败了按段落类型切轨的规则表（64.7%）**。
- **详情**：

  ### 1. 判据（**代理，不是事实**，引用时必须连判据一起引）

  ```
  官方 note 按时间去重成「时间槽」（同刻 each/双押算一个踩音事件）
  对每个时间槽，看 ±30 ms 内是否有 drums / bass / other / vocals 的 onset（量化前的原始秒数）
  recall     = 该 stem 能解释的官方时间槽比例
  precision  = 该 stem 的 onset 中被官方采用的比例
  chance     = 同数量 onset 均匀撒开的期望命中率 1 − exp(−λ·2τ)；lift = recall / chance
  ```

  ⚠️ **必须看 lift**：鼓轨 onset 天然最多（479–631 个 vs 人声 61–333 个），只比 recall 的话鼓永远赢。

  ### 2. 全曲层面（按时间槽数加权，8 曲汇总）

  | stem | recall | precision | 随机基线 | **lift** |
  |---|---:|---:|---:|---:|
  | `drums` | **0.607** | **0.670** | 0.232 | **2.64** |
  | `other` | 0.273 | 0.610 | 0.132 | 2.29 |
  | `bass` | 0.202 | 0.519 | 0.107 | 1.89 |
  | `vocals` | 0.167 | 0.539 | 0.080 | 1.94 |

  **归因分布（8 曲均值）**：至少被一条 stem 解释 **81.4%**；**什么都不落 18.6%**；
  落在 drums 上（含重合）63.8%；落在 vocals 上（含重合）17.4%；
  **只落 drums 28.1%**、**只落 vocals 3.2%**、同时落 drums 与 vocals 12.7%、**不落 drums 36.2%**。

  ### 3. 逐段落类型（recall / 关键 lift）

  | 段落类型 | 段数 | recall(drums) | recall(vocals) | prec(vocals) | 该类型最高 lift |
  |---|---:|---:|---:|---:|---|
  | `chorus` | 14 | 0.583 | 0.200 | 0.542 | drums 2.51 |
  | `verse` | 17 | 0.572 | 0.171 | 0.564 | **other 3.49** |
  | `interlude` | 19 | 0.574 | 0.137 | **0.715** | **vocals 3.18** |
  | `quiet_chorus` | 13 | 0.630 | 0.163 | 0.606 | **vocals 2.94** |
  | `final_chorus` | 4 | 0.678 | **0.065** | 0.294 | drums 2.52 |
  | `pre_chorus` | 8 | 0.549 | 0.190 | 0.429 | bass 3.28 / other 3.27 |
  | `intro` | 8 | 0.657 | 0.234 | 0.292 | **other 5.08** |

  - **间奏的人声 precision 0.715 / lift 3.18 是全表最亮的一条**：间奏里人声 onset 少，但一出现就几乎必被采用（支持知识 002 的"间奏踩人声采样"）。
  - **`final_chorus` 的 vocals lift 0.97 = 随机水平**：大副歌反而最不踩人声。
  - **intro 的最高 lift 是 `other` 而不是 drums** → "前奏哪个响踩哪个"的**能量判据选错了轨**。

  ### 4. 候选池有效率（支撑知识 005）

  管线量化落格后的候选池（平均 828 格/曲）：**recall 0.821 / precision 0.546**；
  逐曲 precision 0.234（最稀的谱）–0.695（最密的谱）→ **官方谱只从候选里挑走约一半**。

  ### 5. 与规则表的一致率（85 段）

  | 口径 | `stemplan.py` v0.2 规则表 | **常数基线（永远答 drums）** |
  |---|---:|---:|
  | 命中率最高轨 top-1 | **64.7%** | **82.4%** |
  | 命中率前二 top-2 | 81.2% | — |
  | lift 最高轨 top-1 | 32.9% | 38.8% |

  主要错配：`hook`(other) 建议 → 实测 drums（11 段）、**`vocal` 建议 → 实测 drums（9 段）**、
  bass 建议 → drums（4 段）、drum 建议 → other（3 段）。
  → **单值"主踩音轨"模型被证伪**，`tools/audio_analysis/stemplan.py` 已改为
  **骨架轨（默认 drums）+ 点缀轨 + 估计占比 + 依据**。

  ### 6. 人声侧的天花板（为什么 C1 只能"存疑"）

  逐曲"人声 onset 数 / 官方时间槽数"给出人声 recall 的上限：MYTHOS 0.104（器乐曲，低是正确的）、
  Xevel 0.179、麒麟 0.183、Signature 0.245、激唱 0.273、Mare Maris 0.440、幻想 0.616、Back 2 Back 1.089。
  **Back 2 Back（天花板 1.089，实测 0.221）与 幻想（0.616 / 0.331）说明：即使人声 onset 充足，
  官方 note 也没有主要踩在人声上。** 但反过来，人声 onset 检测本身不可靠，
  **这不是"谱师不踩人声"的证明**。

- **理由**：这是本项目**唯一一份同时握有官方音频与官方谱的配对证据**。
  复现实现：`tools/calibration/stemhit.py`（切轨匹配与 lift）+ `chartpair.py`；
  谱面侧解析 8/8 零错且 note 总数与 `resource/official-chart/` 转录版完全一致（两条数据来源互证）。
- **来源**：`docs/research/audio-chart-calibration.md` §5（2026-09-11，agent 实测报告）；
  音频来自用户提供的 8 首官方 ST 曲包（`official-mp3/`，**不入库**）。
- **置信度**：**存疑 / 经验**（⚠️ **2026-09-11 第二轮已按 n=40 分项调整，见文末修订记录 D/置信度调整**：
  鼓/归因/候选池侧上调为 `经验`，**人声侧维持 `存疑`**）。三条硬限制：
  1. **n = 8**，分布不均（2 首偏器乐、1 首变速、BPM 128–230、定数只覆盖 13.5–14.5）；
  2. **ground truth 是代理**（"note 是否落在某 stem 的 librosa onset ±30 ms 内"），
     而 Demucs vocals 轨混 lead synth、鼓件分解是频带启发式 —— **人声侧结论尤其需要人工听审
     或 basic-pitch / RMVPE 拿到真正的人声 note 事件后复验**；
  3. **鼓与人声重合的 12.7% 无法区分归属**，且 18.6% 的残差里混着"谱师自由发挥"与"onset 漏检"，本实测无法分离二者。
  → 这些数字可以用来**设计规则与排先后**，**不得当作硬约束或验收判据**。
- **关联**：知识 **002**（切轨；本条目是其 2026-09-11 修订的证据）、**005**（采音要简；候选池 precision 0.546）、
  **031**（密度规律，来自 388 谱全量，与本条目冲突时以 031 为准）、003（配置强度四因素，本次完全没测）；
  `docs/audio-analysis.md` §4.8(E) / §5.2、`tools/audio_analysis/stemplan.py`、`tools/calibration/`。

## 修订记录

- **2026-09-11（第二轮，n=8 → n=40）**：用户再提供 32 首官方 ST 曲包，样本扩到 **40 首**
  （定数 13.0–14.5、BPM 100–260、六类 `&genre`）。依据
  `docs/research/audio-chart-calibration-n40.md` §5（工具 `tools/calibration/`，新增 `strata.py`）。
  **原文一律保留，只加本条修订记录。**

  ### A. 全部核心数字复现，只有小幅移动（n=8 → n=40）

  | 项 | n=8 | **n=40** |
  |---|---:|---:|
  | `drums` recall / lift | 0.607 / 2.64 | **0.639 / 2.73** |
  | `other` recall / lift | 0.273 / 2.29 | **0.267 / 2.30** |
  | `bass` recall / lift | 0.202 / 1.89 | **0.199 / 1.93** |
  | `vocals` recall / lift | 0.167 / 1.94 | **0.182 / 2.16** |
  | 什么都不落 | 18.6% | **16.8%** |
  | 只落 drums / 只落 vocals | 28.1% / 3.2% | **30.4% / 3.5%** |
  | 不落 drums | 36.2% | **34.3%** |
  | 候选池 recall / precision | 0.821 / 0.546 | **0.837 / 0.550** |
  | 逐段"最高命中轨"分布 | 85 段：drums 70 / other 10 / vocals 3 / bass 2 | **378 段：drums 319 / other 43 / vocals 9 / bass 7** |

  → **「鼓骨架 + 大量非鼓填充 + 约两成自由发挥」这个心智模型在 5 倍样本上完全站住。**
  这是本条目最核心的主张，可以**从"存疑"上调到"经验"**（见 D 的置信度裁定）。

  ### B. 规则表一致率：v0.2 的"输给常数基线"已被 v0.3 的模型换法解决

  | 模型 | n=8（85 段） | **n=40（378 段）** |
  |---|---:|---:|
  | v0.2 单值规则表 | 64.7% | **62.4%**（本轮用 `strata.plan_v02_primary` 复刻，与 n=8 吻合） |
  | **v0.3 骨架轨 top-1** | 未测 | **85.2%**（top-2 **97.9%**） |
  | 常数基线（永远答 drums） | 82.4% | **84.4%** |

  → **v0.3 的骨架模型第一次超过常数基线**（85.2% vs 84.4%），赢得最多的类型是
  `intro`（0.692 vs 0.615，正是"判据从能量占比换成 onset 匹配倾向"针对的类型）。
  v0.2 输得最惨的是 `chorus`（0.405，因为它无条件答 vocals，而 74 段副歌里 69 段实测是 drums）。
  ⚠️ **`outro` 是 v0.3 唯一明显输给常数基线的类型**（0.571 vs 0.714，仅 7 段）→ 标存疑。

  ### C. 三条被 n=40 推翻的 n=8 子结论

  1. **「`final_chorus` 最不踩人声」不成立**：n=8 报的 vocals recall 0.065 / lift 0.97（= 随机）
     是 **4 段样本的噪声**；n=40 的 14 段给出 recall **0.199**，与普通副歌持平。
  2. **「间奏该踩人声」是误读**：n=8 看到 interlude 的 vocals lift 最高就读成"该踩人声"。
     n=40 的 84 段给出 vocals **recall 0.075（全表最低）**、**precision 0.673（全表最高）**。
     → 正确表述是**"间奏里的人声采样稀少，但一旦出现几乎必被采用"**（高价值稀有资源）。
  3. **「副歌全踩人声只在人声主导曲上成立」这个降级式辩解没有被支持**：
     88 个副歌段里只有 **4 段** vocals 命中最高（n=8 是 14 段里 2 段）；
     **限定在人声曲上也只有 4/66（6.1%）**。
     （更宽松的口径仍有信号：段内 `vocals lift > drums lift` 的副歌段有 25/88 = 28%
     —— 人声在副歌里**作为重音点缀**是常态，**作为骨架**才是少数。）

  ### D. 代理判据的**明确失败模式**（这是本轮对置信度最重要的一条）

  器乐曲《超絶！Superlative》的 Demucs `vocals` 轨是**纯 lead synth 泄漏**
  （能量占比 `share_vocals` 只有 **0.003**），却拿到 **vocals recall 0.339**
  —— **代理会凭空制造"谱师踩了人声"**。
  同一首曲子的管线 `voiced_ratio` 是 **1.000**（相对 dB 的 VAD 在近乎静音的轨上全判成有人声）。
  40 首上，VAD 把 **10/12 首器乐曲判成人声曲，一首都没判反方向**。

  → **人声侧的全部结论（本条目 §3 的 vocals 列、§6 的天花板表、C1）依旧只能标"存疑"**，
  必须由人工听审或 basic-pitch / RMVPE 的真人声 note 事件复验。
  **鼓/贝斯/器乐侧的结论则可以上调**（它们不依赖这个失败模式）。

  ### E. 逐段落类型（n=40，378 段；与原文 §3 的表对照）

  | 段落类型 | 段数 | recall(drums) | recall(vocals) | prec(vocals) |
  |---|---:|---:|---:|---:|
  | `chorus` | 74 | 0.649 | 0.244 | 0.540 |
  | `interlude` | 84 | 0.624 | **0.075**（全表最低） | **0.673**（全表最高） |
  | `verse` | 75 | 0.637 | 0.184 | 0.513 |
  | `quiet_chorus` | 63 | **0.666**（全表最高） | 0.224 | 0.578 |
  | `intro` | 39 | **0.524**（全表最低） | 0.152 | 0.412 |
  | `final_chorus` | 14 | 0.663 | 0.199 | 0.496 |
  | `pre_chorus` | 18 | 0.578 | 0.193 | 0.464 |

  - `intro` 的 drums recall 最低、`other` 最高（0.393）→ **原文「intro 不该按能量占比选轨」维持**；
  - `pre_chorus` 的 `other` 最高（0.431）→ 维持；
  - **`quiet_chorus` 现在可以定论了**（63 段）：落ちサビ **仍以鼓为骨架**（drums recall 全表最高），
    不是"改踩次响轨" —— 原文 §3 里那条"13 段样本、不支持也不反对"的存疑**解除**。

- **置信度调整（2026-09-11 第二轮）**：
  - **鼓骨架 / 归因分布 / 候选池有效率 / 骨架+点缀模型**：由 `存疑 / 经验` → **`经验`**
    （n=40、跨六类曲风复现，且复现误差极小）。⚠️ 仍不是 `确信`：ground truth 依旧是代理，
    段落类型标签来自未经人工听审的自动分段。
  - **人声侧的一切**（vocals recall/precision/lift、C1、间奏踩人声）：**维持 `存疑`**，
    理由见 D（代理有明确失败模式）。
  - **引用规矩不变**：这些数字可以用来**设计规则与排先后**，**不得当硬约束或验收判据**。

- **2026-09-13（第三轮，n=40 → n=160）**：用户经 AWMC-MaiArchive 再提供 120 首官方 ST 曲包，
  样本扩到 **160 首**（定数 13.0–14.5、BPM **79–266**、**官方六曲风各 11–40 首**）。
  依据 `docs/research/audio-chart-calibration-n160.md` §5（工具 `tools/calibration/`，
  本轮新增官方 `&genre` 六曲风分层）。**原文与前两条修订记录一律保留，只加本条。**

  ### A′. 核心数字第三次复现，移动幅度更小（n=40 → n=160）

  | 项 | n=8 | n=40 | **n=160** |
  |---|---:|---:|---:|
  | `drums` recall / lift | 0.607 / 2.64 | 0.639 / 2.73 | **0.650 / 2.78** |
  | `other` recall / lift | 0.273 / 2.29 | 0.267 / 2.30 | **0.268 / 2.23** |
  | `bass` recall / lift | 0.202 / 1.89 | 0.199 / 1.93 | **0.196 / 1.95** |
  | `vocals` recall / lift | 0.167 / 1.94 | 0.182 / 2.16 | **0.197 / 2.20** |
  | 什么都不落 | 18.6% | 16.8% | **16.0%** |
  | 只落 drums / 只落 vocals | 28.1% / 3.2% | 30.4% / 3.5% | **31.2% / 3.4%** |
  | 不落 drums | 36.2% | 34.3% | **33.7%** |
  | 候选池 recall / precision | 0.821 / 0.546 | 0.837 / 0.550 | **0.840 / 0.549** |
  | 逐段"最高命中轨"分布 | 85 段：70/10/3/2 | 378 段：319/43/9/7 | **1490 段：drums 1253 / other 167 / bass 36 / vocals 34** |

  → **「鼓骨架 + 大量非鼓填充 + 约两成自由发挥」在 20 倍于首轮的样本上第三次站住。**
  **drums 在六个曲风上全部是第一名，无一例外**（recall 0.616–0.707、lift 2.60–3.20）。

  ### B′. 新入表的第六条轨：`piano`（六路 htdemucs_6s）

  | 轨 | 曲数 | recall | precision | 随机基线 | lift |
  |---|---:|---:|---:|---:|---:|
  | `drums` | 160 | 0.650 | 0.649 | 0.236 | **2.78** |
  | **`piano`** | 160 | **0.289** | **0.606** | 0.114 | **2.44** |
  | `other` | 160 | 0.268 | 0.564 | 0.122 | 2.23 |
  | `vocals` | 160 | 0.197 | 0.553 | 0.088 | 2.20 |
  | `bass` | 160 | 0.196 | 0.496 | 0.098 | 1.95 |
  | `fx`（>4 kHz 瞬态） | **136** | 0.057 | 0.396 | 0.038 | 1.69 |

  - **`piano` 的 recall 高于 `other` 与 `vocals`，precision 仅次于 drums，lift 2.44 与
    40 首那轮（`stem-refinement-n40.md`）完全一致** → 「六路分离里只有 piano 是净增益」
    这条结论**从 40 首升级到 160 首**。
  - ⚠️ **piano 的曲风差异极大**：POPS＆アニメ recall **0.486** / niconico 0.341 /
    東方 0.325 / ゲーム 0.271 / maimai 0.252 / **オンゲキ＆CHUNITHM 只有 0.125**
    → **该不该把 piano 当点缀轨，应按曲内实测倾向决定，不要按段落类型写死。**
  - **`fx` 很弱**（recall 0.057、lift 1.69），而且 **24/160 首根本没有 fx 事件**。
  - 加上 piano / fx / melody 之后，四路解释不了的 16.97% 只收回 28.8%，**仍有 12.1% 落不到任何轨**；
    扣掉随机基线后只有 `piano`（0.75）与 `fx`（1.01）算真收回，`melody`(0.38) / `pnote_vocals`(0.33)
    是灌水 —— 与 `stem-refinement-n40` 的结论一致。

  ### C′. 三条 n=40 的"全表最高"措辞被推翻（逐段落类型，1490 段）

  | 段落类型 | 段数 | drums | other | vocals | **piano** | prec(vocals) |
  |---|---:|---:|---:|---:|---:|---:|
  | `interlude` | 361 | 0.613 | 0.276 | **0.125** | 0.287 | 0.561 |
  | `verse` | 355 | 0.655 | 0.257 | 0.203 | 0.266 | 0.536 |
  | `chorus` | 215 | **0.692** | 0.224 | 0.270 | 0.302 | 0.552 |
  | `quiet_chorus` | 183 | 0.682 | 0.230 | 0.269 | 0.294 | 0.555 |
  | `intro` | 155 | **0.571**（最低） | **0.359**（最高） | 0.153 | 0.331 | 0.477 |
  | `drop`（新） | 81 | 0.657 | 0.279 | 0.161 | 0.228 | 0.550 |
  | `pre_chorus` | 58 | 0.589 | 0.318 | 0.154 | 0.266 | 0.445 |
  | `final_chorus` | 46 | 0.663 | 0.243 | 0.267 | 0.323 | **0.573** |
  | `outro` | 32 | 0.612 | 0.187 | 0.103 | 0.212 | 0.475 |

  1. **❌「`pre_chorus` 的 other 全表最高」不成立**：0.318，输给 `intro` 的 **0.359**。
  2. **❌「`interlude` 的 vocals precision 全表最高（0.673）」不成立**：**0.561 排第三**
     （`final_chorus` 0.573 更高）。→ **"间奏人声稀少"仍成立（recall 0.125，主要类型里最低），
     但"一旦出现几乎必被采用"这半句要去掉**（这是本条目第三次修订这句话了）。
  3. **⚠️「`quiet_chorus` 的 drums recall 全表最高」措辞要改**：0.682 略低于 `chorus` 的 0.692。
     **主张（落ちサビ仍以鼓为骨架）维持，"最高"两字去掉。**
  4. **✅ C3 完整维持**：`intro` 的 drums recall **全表最低**、`other` **全表最高**。
  5. **⚠️ 新出现的段落类型 `drop`（81 段，来自器乐向曲子的标签词表）在 `stemplan.py`
     的规则表里没有对应行**，现在走兜底路径；实测形态接近 `chorus`。

  ### D′. 规则表一致率：骨架模型继续微胜，`outro` 规则确认是缺陷

  | 模型 | n=40（378 段） | **n=160（1490 段）** |
  |---|---:|---:|
  | v0.2 单值规则表 | 62.4% | **63.6%** |
  | **v0.3 骨架轨 top-1 / top-2** | **85.2% / 97.9%** | **84.7% / 97.3%** |
  | 常数基线（永远答 drums） | 84.4% | **84.1%** |
  | 点缀轨命中"实测第二高轨" | 61.1% | **65.6%** |

  - **`intro` 仍是骨架模型赢得最多的类型：+7.7 pt（0.768 vs 0.690），与 n=40 的 +7.7 pt 完全一致。**
  - **⚠️ `outro` 规则 8（与 intro 同轨）从"存疑（7 段）"升级为"确认的缺陷"**：
    n=160 的 **32 段**上它仍然输常数基线 **9.4 个百分点**（0.719 vs 0.813）。
    **换成什么还没有证据**（32 段只有 273 个事件），所以工具**行为不动**，只把判定写进注释。

  ### E′. 代理判据的失败模式在更大样本上**更严重**（人声侧维持"存疑"的理由）

  - 器乐曲 **《疾走あんさんぶる》** 的 Demucs `vocals` 轨拿到 **recall 0.579 —— 160 首里最高的一首**，
    比 n=40 找到的《超絶！Superlative》(0.339) 还离谱。
  - 160 首里 vocals 天花板 ≥ 0.7 的 12 首中 **4 首是器乐曲**；
    **人声曲的实测 vocals recall 最高只有 0.489**（ジンギスカン）。
  - 能量 VAD 把 **31/51 首器乐曲判成人声曲，仍然一首都没判反方向**（n=40 是 10/12）。
  → **人声侧的一切结论维持 `存疑`**；**鼓 / 归因 / 候选池 / 骨架+点缀 / piano 侧维持 `经验`**
  （n=160 复现，且不依赖这个失败模式）。

  ### F′. 一个实现缺陷（候选池的 φ 补偿）已修

  候选池有效率原本拿**做过 φ\* 解码延迟补偿**的官方时间槽去比"量化落格后的网格"。
  但网格锚在用户给的 `&first` 上，与官方 note 本来就同一个时钟，再平移一次反而把 note 推离网格。
  n=40 时最大 |φ\*| 只有 25 ms、被 30 ms 容差盖住；n=160 里 **L4TS2018（φ\* = −37.5 ms）
  的候选池 recall 被压到 0.003** 才暴露。修正后 pool recall 0.8328 → **0.8396**。
  → **本条目 §4 与前两轮的候选池数字属于轻微低估，量级不影响任何结论。**

  - ⚠️ **口径边界**：全部数字来自 **n=160**（定数 13.0–14.5、BPM 79–266、全 ST MASTER），
    且为代理判据。与 388 谱全量统计（知识 031）冲突时以 388 谱为准。
    `melody` / `pnote_*`（basic-pitch 有音高轨）**只有原 40 首有**，
    涉及它们的数字仍以 `docs/research/stem-refinement-n40.md` 为准。
