# 015 — 速度与密度红线（官谱实测上界）

- **日期**：2026-09-10
- **要点**：note 速度存在物理上限，官谱最快配置即为人类极限参考；生成器密度不得超过这些红线。
- **详情**（官谱实测数据）：
  - 最快同键纵连：白系 177.6BPM 的 32 分（间隔 42.2ms，≈23.7 击/秒）；
  - 最快扫键：怒锤 200BPM 的 64 分（间隔 18.75ms = 1.125 帧）；白日舞 102BPM 的 96 分圈（24.51ms）；
  - NPS 换算公式：**NPS = (分音分母/4) × BPM/60**（如 150BPM 八分 = 5 击/秒；180BPM 十六分 = 12 击/秒）；
  - 交互等效实例：222BPM 的 24 分 ≡ 333BPM 的 16 分 = 22.2 键/秒（短爆发上限）；201BPM 的 24 分长交互 = 20.1 键/秒（Latent Kingdom 14.9，全曲仅一次）；
  - 同一判定区每秒最多约 30 次判定（叠键物理下限见知识 009）。
- **理由**：超速无理（超出人类极限）是五大无理之外的独立红线；官谱实测数据是可引用的权威上界。
- **来源**：Maimai 无理综述（三）（六）
- **置信度**：确信（官谱实测数据部分）/ 经验（判定区 30 次/秒为社区实测）
- **关联**：知识 004（密度校准）、009。

## 修订记录

- **2026-09-10**：修正 NPS 公式——原"(分音分母) × BPM / 60"少了 ÷4，正确为 **(分音分母/4) × BPM/60**（条目内示例数值本就按正确公式计算，仅公式文字有误）；补充交互等效实例（222×24分 ≡ 333×16分，见知识 018）。
